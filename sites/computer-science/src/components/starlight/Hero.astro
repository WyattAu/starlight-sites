---
/**
 * Hero override, renders nothing.
 *
 * Subject landing pages render the cinematic SubjectHero from the
 * PageFrame override instead. The stock Hero is suppressed so hero
 * frontmatter (tagline/actions) can drive SubjectHero without Starlight
 * also rendering its own hero panel inside the content column.
 */
---

<!-- Suppressed: SubjectHero is rendered by the PageFrame override. -->
