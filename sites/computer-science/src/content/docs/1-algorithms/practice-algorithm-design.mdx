---

<!-- Breadcrumb Schema for SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{"name": "Home", "url": "https://wyattau.com"}, {"name": "computer-science", "url": "https://computer-science.wyattau.com"}, {"name": "1 Algorithms", "url": "https://computer-science.wyattau.com/1-algorithms"}, {"name": "Practice Algorithm Design", "url": "https://computer-science.wyattau.com/1-algorithms/practice-algorithm-design"}]
}
</script>
title: "Algorithm Design -- Practice Problems"
description: "10 MCQ questions covering divide and conquer, dynamic programming, greedy algorithms, backtracking, and the Master theorem. Select an option to check your"
tableOfContents: false
---

<!-- Breadcrumb Schema for SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{"name": "Home", "url": "https://wyattau.com"}, {"name": "computer-science", "url": "https://computer-science.wyattau.com"}, {"name": "1 Algorithms", "url": "https://computer-science.wyattau.com/1-algorithms"}, {"name": "Practice Algorithm Design", "url": "https://computer-science.wyattau.com/1-algorithms/practice-algorithm-design"}]
}
</script>

import PracticeProblem from "@components/PracticeProblem";

## Algorithm Design -- Practice Problems

10 MCQ questions covering divide and conquer, dynamic programming, greedy algorithms, backtracking,
and the Master theorem. Select an option to check your answer and view the explanation.

## Intuition

**A toolbox of problem-solving strategies:** Algorithm design paradigms are like different tools in a workshop — divide-and-conquer is the saw (split the problem), dynamic programming is the memory palace (remember what you've solved), and backtracking is the trial-and-error approach (try, undo, try again).

**Why it matters:** Knowing which paradigm fits a problem is the first step to solving it efficiently. The wrong paradigm leads to exponential time; the right one can make the difference between feasible and infeasible.

**The key insight:** Dynamic programming works when subproblems overlap — if you're solving the same subproblem twice, remember the answer instead of recomputing it. This transforms exponential algorithms into polynomial ones.

---

<!-- Breadcrumb Schema for SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{"name": "Home", "url": "https://wyattau.com"}, {"name": "computer-science", "url": "https://computer-science.wyattau.com"}, {"name": "1 Algorithms", "url": "https://computer-science.wyattau.com/1-algorithms"}, {"name": "Practice Algorithm Design", "url": "https://computer-science.wyattau.com/1-algorithms/practice-algorithm-design"}]
}
</script>

## Divide and Conquer and the Master Theorem

<PracticeProblem client:only="solid-js" question={"What is the time complexity of Strassen\'s matrix multiplication for multiplying two $n \\times n$ matrices?&quot; options={[ '$O(n^{\\log_2 7}) \\approx O(n^{2.807})$', '$O(n^3)$', '$O(n^2)$', '$O(n^{\\log_2 8}) = O(n^3)$', ]} correctAnswer={0} explanation=&quot;Strassen\'s algorithm divides each matrix into four $n/2 \\times n/2$ blocks and computes 7 products (instead of the naive 8). The recurrence is $T(n) = 7T(n/2) + O(n^2)$. By the Master theorem with $a=7$, $b=2$, $n^{\\log_2 7} \\approx n^{2.807}$ dominates $f(n) = O(n^2)$, giving $T(n) = O(n^{\\log_2 7})$."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"Consider the recurrence $T(n) = 4T(n/2) + n^2 \\log n$. Applying the Master Theorem, which case applies and what is the solution?&quot; options={[ 'Case 2 ($k=1$): $T(n) = \\Theta(n^2 \\log^2 n)$', 'Case 1: $T(n) = \\Theta(n^2)$', 'Case 3: $T(n) = \\Theta(n^2 \\log n)$', 'Case 2 ($k=0$): $T(n) = \\Theta(n^2 \\log n)$', ]} correctAnswer={0} explanation=&quot;$a=4$, $b=2$, so $n^{\\log_b a} = n^{\\log_2 4} = n^2$. Here $f(n) = n^2 \\log n = n^{\\log_b a} \\cdot \\log^k n$ with $k=1$. This is Master Theorem Case 2: $T(n) = \\Theta(n^{\\log_b a} \\log^{k+1} n) = \\Theta(n^2 \\log^2 n)$."} difficulty="hard"  />

<PracticeProblem client:only="solid-js" question={"Merge sort on an array of $n$ elements has which recurrence and overall time complexity?&quot; options={[ '$T(n) = 2T(n/2) + O(n)$; $O(n \\log n)$', '$T(n) = 2T(n/2) + O(1)$; $O(n)$', '$T(n) = T(n/2) + O(n)$; $O(n)$', '$T(n) = 2T(n/2) + O(n \\log n)$; $O(n \\log^2 n)$', ]} correctAnswer={0} explanation=&quot;Merge sort divides into two halves (2 recursive calls of size $n/2$) and merges them in $O(n)$ time. The recurrence $T(n) = 2T(n/2) + O(n)$ is Master Theorem Case 2, giving $T(n) = \\Theta(n \\log n)$. It is a stable sort but requires $O(n)$ extra space."} difficulty="easy"  />

## Dynamic Programming and Greedy Algorithms

<PracticeProblem client:only="solid-js" question={"What is the time complexity of the Longest Common Subsequence (LCS) algorithm using dynamic programming for two sequences of lengths $m$ and $n$?&quot; options={['$O(mn)$', '$O(2^{m+n})$', '$O(\\min(m,n) \\log \\max(m,n))$', '$O((m+n) \\log(m+n))$']} correctAnswer={0} explanation=&quot;The DP table $c[i][j]$ has $(m+1)(n+1)$ entries, each computed in $O(1)$ by the recurrence $c[i][j] = c[i-1][j-1]+1$ if $x_i = y_j$, else $\\max(c[i-1][j], c[i][j-1])$. Total time is $O(mn)$, space is $O(mn)$ which can be optimized to $O(\\min(m,n))$ by keeping only two rows."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"For the 0/1 knapsack problem with $n$ items and capacity $W$, the DP solution has time complexity $O(nW)$. Why is this called pseudopolynomial?&quot; options={[ 'Because $W$ is a numeric value that may be exponential in the input size', 'Because the algorithm does not always find the optimal solution', 'Because the space complexity is exponential in $n$', 'Because the recurrence is not well-defined', ]} correctAnswer={0} explanation=&quot;The input encoding of $W$ requires $\\log W$ bits. An algorithm polynomial in the numeric value $W$ is exponential in the input size $\\log W$. Thus $O(nW)$ is polynomial in $n$ and $W$ but not in the actual bit-length of the input, making it pseudopolynomial. The 0/1 knapsack is NP-complete, but this DP is efficient when $W$ is small."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Given coin denominations $\\{1, 3, 4\\}$ and target amount 6, why does the greedy algorithm fail to find the optimal solution?&quot; options={[ 'Greedy picks 4+1+1 (3 coins) instead of the optimal 3+3 (2 coins)', 'Greedy picks 4+4 (2 coins) instead of the optimal 3+1+1+1 (4 coins)', 'Greedy picks 3+3 (2 coins) instead of the optimal 4+1+1 (3 coins)', 'The greedy algorithm does not fail; it always finds the optimal solution', ]} correctAnswer={0} explanation=&quot;The greedy approach picks the largest coin first: $4 + 1 + 1 = 3$ coins. The optimal solution uses two 3-value coins: $3 + 3 = 2$ coins. Greedy fails because these coin denominations are not canonical. Dynamic programming correctly finds $dp[6] = 2$ by checking all subproblems."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Which algorithm paradigm does Huffman coding use, and what property guarantees its optimality?&quot; options={[ 'Greedy: optimal substructure and greedy choice property', 'Dynamic programming: overlapping subproblems', 'Divide and conquer: independent subproblems', 'Backtracking: exhaustive search with pruning', ]} correctAnswer={0} explanation=&quot;Huffman coding uses a greedy strategy: repeatedly merge the two trees with the smallest frequencies. Optimality is proven via the greedy choice property (merging the two least-frequent characters is always part of an optimal solution) and optimal substructure (the remaining subtrees must also be optimal). The proof uses an exchange argument."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"In the edit distance (Levenshtein distance) problem between two strings of lengths $m$ and $n$, what is the space-optimised complexity?&quot; options={[ '$O(mn)$ time, $O(\\min(m,n))$ space', '$O(\\min(m,n))$ time, $O(1)$ space', '$O(mn)$ time, $O(mn)$ space', '$O((m+n)!) $ time, $O(\\max(m,n))$ space', ]} correctAnswer={0} explanation=&quot;The full DP table is $O(mn)$ time and $O(mn)$ space. Since $dp[i][j]$ depends only on the current row $dp[i-1][j]$, $dp[i][j-1]$, and the diagonal $dp[i-1][j-1]$, we can reduce to two rows of length $\\min(m,n)$, achieving $O(mn)$ time and $O(\\min(m,n))$ space."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Kruskal\'s algorithm for finding a Minimum Spanning Tree uses which data structure for efficiently detecting cycles, and what is the overall time complexity?&quot; options={[ 'Union-Find with path compression and union by rank; $O(E \\log E)$', 'Hash table; $O(E^2)$', 'Priority queue; $O(E \\log V)$', 'Adjacency list; $O(V^2)$', ]} correctAnswer={0} explanation=&quot;Kruskal\'s sorts edges by weight ($O(E \\log E)$), then iterates through them using Union-Find to check if two endpoints are in the same set ($O(\\alpha(V)) \\approx O(1)$ per operation with path compression and union by rank). Total: $O(E \\log E) = O(E \\log V)$ since $E \\leq V^2$."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"In the N-Queens problem solved by backtracking, what is the worst-case time complexity and what does backtracking prune compared to brute force?&quot; options={[ '$O(n!)$ worst case; backtracking prunes partial placements that lead to mutual attacks', '$O(2^n)$ worst case; backtracking only prunes diagonal conflicts', '$O(n^n)$ worst case; no pruning occurs', '$O(n^2)$ worst case; backtracking finds the solution in polynomial time', ]} correctAnswer={0} explanation=&quot;Brute force generates all $n^n$ possible board configurations. Backtracking incrementally places queens row by row and prunes whenever a queen conflicts with already-placed queens (column, diagonal checks). In the worst case, this is still $O(n!)$ (placing one queen per row), but in practice the pruning eliminates most invalid branches early."} difficulty="hard"  />

---

<!-- Breadcrumb Schema for SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{"name": "Home", "url": "https://wyattau.com"}, {"name": "computer-science", "url": "https://computer-science.wyattau.com"}, {"name": "1 Algorithms", "url": "https://computer-science.wyattau.com/1-algorithms"}, {"name": "Practice Algorithm Design", "url": "https://computer-science.wyattau.com/1-algorithms/practice-algorithm-design"}]
}
</script>

## Additional Algorithm Design Questions

<PracticeProblem client:only="solid-js" question={"Consider the activity selection problem where each activity has a start time $s_i$ and finish time $f_i$. Which greedy strategy yields an optimal solution and why?&quot; options={[ 'Always pick the activity with the earliest finish time', 'Always pick the activity with the latest start time', 'Always pick the activity with the shortest duration', 'Always pick the activity that starts earliest', ]} correctAnswer={0} explanation=&quot;The greedy choice property for activity selection is: picking the activity with the earliest finish time leaves maximum room for subsequent activities. This can be proved by exchange argument: if an optimal solution does not include the earliest-finishing activity, we can replace the first activity in it with the earliest-finishing one without reducing the count. Sorting by finish time gives $O(n \\log n)$ time."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"The closest pair of points problem in the plane can be solved in $O(n \\log n)$ time using a divide-and-conquer approach. What is the key step that avoids the naive $O(n^2)$ pairwise comparison?&quot; options={[ 'Only points within a strip of width $2d$ around the dividing line need to be checked, where $d$ is the minimum distance in each half', 'A hash table maps points to their nearest neighbour in $O(1)$ time', 'All points are projected onto a line and sorted for linear scanning', 'A Voronoi diagram is constructed in $O(n \\log n)$ time', ]} correctAnswer={0} explanation=&quot;After recursively finding the minimum distance $d$ in the left and right halves, the critical step examines the strip of width $2d$ centred on the dividing line. Within this strip, at most 7 points (per side) need to be checked for each point because of the $d \\times 2d$ packing argument. Sorting the strip points by $y$-coordinate takes $O(n \\log n)$ total, and the strip scan is $O(n)$ per level, giving the $O(n \\log n)$ overall bound."} difficulty="hard"  />

<PracticeProblem client:only="solid-js" question={"Which algorithm paradigm is most natural for solving the travelling salesman problem exactly, and what is its time complexity for $n$ cities?&quot; options={[ 'Dynamic programming (Held--Karp algorithm) at $O(n^2 2^n)$', 'Greedy nearest-neighbour at $O(n^2)$ with optimal solution', 'Divide and conquer at $O(n \\log n)$', 'Linear programming at $O(n^3)$ with guaranteed optimality', ]} correctAnswer={0} explanation=&quot;The Held--Karp algorithm uses dynamic programming over subsets. Define $dp[S][i]$ as the minimum cost path visiting every city in subset $S$ exactly once, ending at city $i$. There are $2^n$ subsets and $n$ ending cities, giving $O(n^2 2^n)$ time and $O(n 2^n)$ space. This is a significant improvement over the $O(n!)$ brute-force approach but still exponential."} difficulty="hard"  />

<PracticeProblem client:only="solid-js" question={"The master method cannot solve $T(n) = 2T(n/2) + n \\log n$ directly because $f(n) = n \\log n$ falls into a gap. Which extension resolves this?&quot; options={[ 'The extended master theorem (Case 2 with $f(n) = \\Theta(n^{\\log_b a} \\log^k n)$) gives $T(n) = \\Theta(n \\log^2 n)$', 'The Akra--Bazzi method, which handles non-polynomial $f(n)$', 'Substitution method with a carefully guessed bound', 'The recursion tree method gives an exact closed form', ]} correctAnswer={0} explanation=&quot;For $T(n) = 2T(n/2) + n \\log n$, we have $a=2$, $b=2$, so $n^{\\log_b a} = n$. Since $f(n) = n \\log n = n^{\\log_b a} \\log^1 n$, this is Case 2 of the extended Master Theorem with $k=1$, giving $T(n) = \\Theta(n \\log^{1+1} n) = \\Theta(n \\log^2 n)$. The standard Master Theorem (without the $\log^k$ extension) cannot handle this case."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"In a weighted interval scheduling problem where each job has a start time, finish time, and weight, what recurrence expresses the optimal solution?&quot; options={[ '$OPT(j) = \\max(w_j + OPT(p(j)),\\; OPT(j-1))$ where $p(j)$ is the latest non-overlapping predecessor', '$OPT(j) = \\max(OPT(j-1),\\; OPT(j-2) + w_j)$', '$OPT(j) = w_j + OPT(j-1)$', '$OPT(j) = \\max_{k \\leq j}(w_k + OPT(k))$', ]} correctAnswer={0} explanation=&quot;For each job $j$ (sorted by finish time), the optimal value is either: (1) include job $j$, gaining $w_j$ plus the optimal value of the latest non-overlapping predecessor $p(j)$; or (2) exclude job $j$, taking $OPT(j-1)$. Computing $p(j)$ for all jobs via binary search takes $O(n \\log n)$, and the DP itself runs in $O(n)$ after preprocessing, giving $O(n \\log n)$ overall."} difficulty="medium"  />

## Common Mistakes

**Assuming greedy algorithms always give optimal solutions:** Greedy works for some problems (activity selection, Huffman) but fails for others (0/1 knapsack, TSP). Always verify the greedy choice property.

**Forgetting that dynamic programming requires optimal substructure:** DP works when the optimal solution contains optimal solutions to subproblems. Not all problems have this property.

**Confusing divide-and-conquer with dynamic programming:** Divide-and-conquer splits problems into independent subproblems. DP handles overlapping subproblems with memoisation. Don't confuse the two paradigms.
