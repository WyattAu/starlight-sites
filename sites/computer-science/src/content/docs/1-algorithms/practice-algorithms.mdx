---

title: "University CS Practice: Algorithms"
description: "10 MCQ questions covering algorithm design paradigms, complexity analysis, divide-and-conquer, dynamic programming, greedy algorithms, and amortized"
tags: [University, Computer Science, Practice]
tableOfContents: false
---


import PracticeProblem from "@components/PracticeProblem";

## University CS -- Algorithms Practice Problems

10 MCQ questions covering algorithm design paradigms, complexity analysis, divide-and-conquer,
dynamic programming, greedy algorithms, and amortized analysis. Select an option to check your
answer and view the explanation.

## Intuition

**Different strategies for different problems:** Algorithm design paradigms are like different strategies for solving a jigsaw puzzle — divide-and-conquer splits the puzzle into sections, dynamic programming remembers which pieces you've already tried, and greedy algorithms grab the most obvious piece first.

**Why it matters:** The right algorithm can mean the difference between a solution that takes seconds and one that takes centuries. Understanding these paradigms gives you a toolbox for approaching any computational problem.

**The key insight:** Greedy algorithms are fast but not always optimal — they work when local choices lead to global optima (like Huffman coding) but fail when they don't (like the coin change problem with non-standard denominations).

---


## Worked Examples

### Example 1: Master Theorem — Solving Recurrences

**Problem:** Solve $T(n) = 4T(n/2) + n^2$ using the Master Theorem.

**Solution:**

Identify parameters: $a = 4$, $b = 2$, $f(n) = n^2$

Compute $\log_b a = \log_2 4 = 2$

Compare $f(n) = n^2$ with $n^{\log_b a} = n^2$:

Since $f(n) = \Theta(n^2) = \Theta(n^{\log_b a})$, we are in **Case 2** of the Master Theorem.

$$T(n) = \Theta(n^2 \log n)$$

**Verification:** The recursion tree has $\log_2 n$ levels. At each level $i$, there are $4^i$ subproblems of size $n/2^i$, each costing $(n/2^i)^2 = n^2/4^i$. Total work per level: $4^i \times n^2/4^i = n^2$. Summing over $\log_2 n$ levels: $T(n) = n^2 \log_2 n = \Theta(n^2 \log n)$. ✓

---


### Example 2: Dynamic Programming — 0/1 Knapsack

**Problem:** Given items with weights $[2, 3, 4, 5]$ and values $[3, 4, 5, 6]$, capacity $W = 8$, find the maximum value.

**Solution:**

**DP Table Construction:**

| | $w=0$ | $w=1$ | $w=2$ | $w=3$ | $w=4$ | $w=5$ | $w=6$ | $w=7$ | $w=8$ |
|---|---|---|---|---|---|---|---|---|---|
| 0 items | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| Item 1 (w=2, v=3) | 0 | 0 | 3 | 3 | 3 | 3 | 3 | 3 | 3 |
| Item 2 (w=3, v=4) | 0 | 0 | 3 | 4 | 4 | 7 | 7 | 7 | 7 |
| Item 3 (w=4, v=5) | 0 | 0 | 3 | 4 | 5 | 7 | 8 | 9 | 9 |
| Item 4 (w=5, v=6) | 0 | 0 | 3 | 4 | 5 | 7 | 8 | 9 | 10 |

**Recurrence:** $dp[i][w] = \max(dp[i-1][w], dp[i-1][w-w_i] + v_i)$ if $w_i \leq w$

**Answer:** $dp[4][8] = 10$ (items 2 and 4: weight $3+5=8$, value $4+6=10$)

**Space-optimized version:** Use 1D array of size $W+1$, iterate items in outer loop, weights in reverse inner loop. Space: $O(W)$ instead of $O(nW)$.

---


### Example 3: Greedy — Activity Selection

**Problem:** Given activities with start and finish times: $[(1,4), (3,5), (0,6), (5,7), (3,9), (5,9), (6,10), (8,11), (8,12), (2,14), (12,16)]$, find the maximum number of non-overlapping activities.

**Solution:**

**Greedy strategy:** Sort by finish time, then select activities that don't overlap with the last selected.

| Activity | Start | Finish | Selected? |
|----------|-------|--------|-----------|
| (1,4) | 1 | 4 | ✓ |
| (3,5) | 3 | 5 | ✗ (overlaps with (1,4)) |
| (0,6) | 0 | 6 | ✗ (overlaps with (1,4)) |
| (5,7) | 5 | 7 | ✓ (starts at 5 ≥ 4) |
| (3,9) | 3 | 9 | ✗ (overlaps with (5,7)) |
| (5,9) | 5 | 9 | ✗ (overlaps with (5,7)) |
| (6,10) | 6 | 10 | ✗ (overlaps with (5,7)) |
| (8,11) | 8 | 11 | ✓ (starts at 8 ≥ 7) |
| (8,12) | 8 | 12 | ✗ (overlaps with (8,11)) |
| (2,14) | 2 | 14 | ✗ (overlaps with (8,11)) |
| (12,16) | 12 | 16 | ✓ (starts at 12 ≥ 11) |

**Maximum activities:** 4 → $\{(1,4), (5,7), (8,11), (12,16)\}$

**Key insight:** The greedy choice (earliest finish time) leaves maximum room for remaining activities. This is provably optimal by exchange argument.

---


## Algorithm Design Paradigms and Complexity

<PracticeProblem client:only="solid-js" question={"Which algorithm design paradigm does Merge Sort belong to?&quot; options={['Divide and Conquer', 'Dynamic Programming', 'Greedy', 'Backtracking']} correctAnswer={0} explanation=&quot;Merge Sort divides the array into two halves, recursively sorts each half, and then merges the two sorted halves. The divide step is trivial (splitting at the midpoint), the conquer step is the recursive calls, and the combine step is the merge operation. This is the classic divide-and-conquer pattern."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"What is the time complexity of finding the $k$-th smallest element in an unsorted array of $n$ elements using the expected-time QuickSelect algorithm?&quot; options={[ '$O(n)$ expected, $O(n^2)$ worst case', '$O(n \\log n)$ expected', '$O(n)$ worst case', '$O(\\log n)$ expected', ]} correctAnswer={0} explanation=&quot;QuickSelect uses a random pivot and recurses on only one partition. Expected number of comparisons is $O(n)$ (similar analysis to expected Quicksort, but recursing on only one side). However, with a bad pivot (e.g., always smallest element), it degrades to $O(n^2)$. The median-of-medians variant guarantees $O(n)$ worst case."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Consider the recurrence $T(n) = 3T(n/2) + n^2$. Using the Master Theorem, what is the asymptotic bound for $T(n)$?&quot; options={[ '$\\Theta(n^2)$', '$\\Theta(n^{\\log_2 3})$', '$\\Theta(n^2 \\log n)$', '$\\Theta(n^{\\log_2 3} \\log n)$', ]} correctAnswer={0} explanation=&quot;Here $a = 3$, $b = 2$, so $d = \\log_b(a) = \\log_2 3 \\approx 1.585$. Since $f(n) = n^2 = \\Omega(n^c)$ where $c = 2 > d = 1.585$, and $a f(n/b) = 3(n/2)^2 = 0.75 n^2 \\leq k \\cdot n^2$ for $k = 0.75 < 1$, we are in Case 3: $T(n) = \\Theta(f(n)) = \\Theta(n^2)$."} difficulty="hard"  />

## Divide and Conquer and Dynamic Programming

<PracticeProblem client:only="solid-js" question={"What is the time complexity of the matrix chain multiplication problem solved by dynamic programming for $n$ matrices?&quot; options={['$O(n^3)$', '$O(n^2)$', '$O(2^n)$', '$O(n^4)$']} correctAnswer={0} explanation=&quot;The DP approach uses a table $m[i,j]$ for all $1 \\leq i \\leq j \\leq n$, giving $O(n^2)$ entries. Each entry is computed by minimizing over $O(n)$ split points, taking $O(n)$ time per entry. Total time: $O(n^2 \\times n) = O(n^3)$. Space complexity is $O(n^2)$ for the table and another $O(n^2)$ for storing the optimal split points."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"The Floyd-Warshall algorithm for all-pairs shortest paths on a graph with $n$ vertices has a time complexity of:&quot; options={['$O(n^3)$', '$O(n^2 \\log n)$', '$O(n^2)$', '$O(nm)$ where $m$ is the number of edges']} correctAnswer={0} explanation=&quot;Floyd-Warshall uses three nested loops: for each intermediate vertex $k$ ($1$ to $n$), for each pair of vertices $(i, j)$, update $d[i][j] = \\min(d[i][j], d[i][k] + d[k][j])$. Total: $n \\times n \\times n = O(n^3)$. It handles negative edge weights but not negative cycles. Works on dense graphs with adjacency matrix representation."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"The 0/1 knapsack problem has optimal substructure and overlapping subproblems. What is the space-optimised DP solution\'s complexity?&quot; options={[ '$O(nW)$ time, $O(W)$ space', '$O(nW)$ time, $O(nW)$ space', '$O(2^n)$ time, $O(n)$ space', '$O(n^2)$ time, $O(n)$ space', ]} correctAnswer={0} explanation=&quot;The standard DP table is $dp[i][w]$ of size $O(n \\times W)$, giving $O(nW)$ time and space. Since $dp[i][w]$ depends only on $dp[i-1][...]$ (the previous row), we can reduce to a 1D array of size $W$, giving $O(nW)$ time and $O(W)$ space. Note: this is pseudopolynomial since $W$ is a numeric value, not the input size."} difficulty="medium"  />

## Greedy Algorithms and Amortized Analysis

<PracticeProblem client:only="solid-js" question={"Which greedy strategy is used in Huffman coding to construct an optimal prefix-free code?&quot; options={[ 'Repeatedly merge the two trees with the smallest frequencies', 'Repeatedly merge the two trees with the largest frequencies', 'Build a balanced binary tree from all characters', 'Assign fixed-length codes to all characters', ]} correctAnswer={0} explanation=&quot;Huffman\'s algorithm builds the tree bottom-up: maintain a min-priority queue of trees keyed by frequency, repeatedly extract the two minimum-frequency trees, merge them, and insert back. This greedy strategy produces an optimal prefix-free code. The proof uses exchange argument: any optimal solution can be transformed into the Huffman solution without increasing cost."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Dijkstra\'s algorithm fails to find shortest paths when the graph contains negative edge weights. Which algorithm should be used instead?&quot; options={['Bellman-Ford algorithm', &quot;Kruskal\'s algorithm&quot;, 'BFS', 'A* search algorithm']} correctAnswer={0} explanation=&quot;The Bellman-Ford algorithm correctly handles negative edge weights (but not negative cycles reachable from the source). It relaxes all edges $|V|-1$ times, guaranteeing shortest paths if no negative cycles exist. Time complexity is $O(VE)$. Dijkstra\'s greedy choice fails with negative edges because a shorter path through unvisited vertices may be discovered later."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"In amortized analysis, what is the amortized cost of a sequence of $n$ push and pop operations on a dynamic array that doubles its size when full (starting from size 1)?&quot; options={[ '$O(1)$ per operation', '$O(\\log n)$ per operation', '$O(n)$ per operation', '$O(\\sqrt{n})$ per operation', ]} correctAnswer={0} explanation=&quot;Using the aggregate method: total cost of $n$ operations is at most $cn$ (each element is pushed once, popped once, and copied at most $O(\\log n)$ times across resizings). By the accounting or potential method, each push charges $O(1)$ amortized cost (3 units: 1 for the push, 2 saved toward the next resize). Amortized cost per operation is $O(1)$, even though individual operations may cost $O(n)$."} difficulty="hard"  />

## Common Mistakes

**Confusing worst-case with amortised analysis:** Worst-case is the cost of the single most expensive operation. Amortised is the average cost per operation over a sequence. Don't confuse the two.

**Assuming greedy algorithms always give optimal solutions:** Greedy algorithms work for some problems (Huffman, MST) but fail for others (0/1 knapsack). Always check if greedy choice property holds.

**Forgetting that Big-O is an upper bound:** O(n) means the algorithm is at most linear. It doesn't mean it's exactly linear. Don't confuse O(n) with Θ(n).
