---

title: "Machine Learning -- Practice Problems"
description: "10 MCQ questions covering linear/logistic regression, decision trees, neural networks, loss functions, regularization, bias-variance tradeoff, and"
tableOfContents: false
---

<!-- Breadcrumb Schema for SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{"name": "Home", "url": "https://wyattau.com"}, {"name": "computer-science", "url": "https://computer-science.wyattau.com"}, {"name": "3 Theory", "url": "https://computer-science.wyattau.com/3-theory"}, {"name": "Practice Machine Learning", "url": "https://computer-science.wyattau.com/3-theory/practice-machine-learning"}]
}
</script>

import PracticeProblem from "@components/PracticeProblem";

## Machine Learning -- Practice Problems

10 MCQ questions covering linear/logistic regression, decision trees, neural networks, loss
functions, regularization, bias-variance tradeoff, and evaluation metrics. Select an option to check
your answer.

## Intuition

**Learning from examples like a child:** Machine learning is like teaching a child to recognise cats — you show thousands of examples, they gradually learn the pattern, and eventually they can spot cats they've never seen. The algorithm finds patterns in data that humans might miss.

**Why it matters:** ML powers recommendation systems, medical diagnosis, autonomous vehicles, and language models. Understanding it explains why some models overfit, why more data in most cases helps, and how bias enters automated decisions.

**The key insight:** All models are wrong, but some are useful — the goal isn't perfection but finding the right balance between memorising training data (overfitting) and missing real patterns (underfitting).

---

<!-- Breadcrumb Schema for SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{"name": "Home", "url": "https://wyattau.com"}, {"name": "computer-science", "url": "https://computer-science.wyattau.com"}, {"name": "3 Theory", "url": "https://computer-science.wyattau.com/3-theory"}, {"name": "Practice Machine Learning", "url": "https://computer-science.wyattau.com/3-theory/practice-machine-learning"}]
}
</script>

## Supervised Learning and Model Fundamentals

<PracticeProblem client:only="solid-js" question={"In linear regression with ordinary least squares, the closed-form solution is $\\mathbf{w}^* = (X^T X)^{-1} X^T \\mathbf{y}$. What assumption must hold for this solution to be valid?&quot; options={[ '$X^T X$ must be invertible (features must not be linearly dependent)', 'The number of features must equal the number of samples', 'All features must be binary', 'The target variable must be categorical', ]} correctAnswer={0} explanation=&quot;The closed-form solution requires $(X^T X)^{-1}$ to exist, meaning $X^T X$ is full rank. This fails when features are linearly dependent (multicollinearity) or when $n < d$ (more features than samples). In these cases, use gradient descent, regularized regression (ridge: adds $\\lambda I$ to $X^T X$), or dimensionality reduction."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"The logistic regression loss function is binary cross-entropy: $\\mathcal{L} = -\\frac{1}{n}\\sum[y_i \\log \\hat{y}_i + (1-y_i)\\log(1-\\hat{y}_i)]$. What is the gradient with respect to $\\mathbf{w}$?&quot; options={[ '$\\nabla_{\\mathbf{w}} \\mathcal{L} = \\frac{1}{n} \\sum_{i=1}^{n} (\\hat{y}_i - y_i) \\mathbf{x}_i$', '$\\nabla_{\\mathbf{w}} \\mathcal{L} = \\frac{1}{n} \\sum_{i=1}^{n} (y_i - \\hat{y}_i)^2 \\mathbf{x}_i$', '$\\nabla_{\\mathbf{w}} \\mathcal{L} = \\frac{1}{n} \\sum_{i=1}^{n} \\hat{y}_i (1 - \\hat{y}_i) \\mathbf{x}_i$', '$\\nabla_{\\mathbf{w}} \\mathcal{L} = \\frac{1}{n} \\sum_{i=1}^{n} (\\hat{y}_i - y_i) \\mathbf{x}_i \\hat{y}_i (1 - \\hat{y}_i)$', ]} correctAnswer={0} explanation=&quot;The gradient of binary cross-entropy with respect to $\\mathbf{w}$ is $\\frac{1}{n} \\sum (\\hat{y}_i - y_i) \\mathbf{x}_i$. This elegant result arises because the derivative of the log-sigmoid cancels with the sigmoid derivative. The update rule is $\\mathbf{w} \\leftarrow \\mathbf{w} - \\alpha \\cdot \\frac{1}{n} \\sum (\\hat{y}_i - y_i) \\mathbf{x}_i$."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"In a decision tree, which splitting criterion measures the reduction in entropy after a split?&quot; options={[ 'Information gain: $\\text{Gain}(S, A) = H(S) - \\sum_{v} \\frac{|S_v|}{|S|} H(S_v)$', 'Gini impurity: $1 - \\sum p_k^2$', 'Mean squared error reduction', 'Chi-squared statistic', ]} correctAnswer={0} explanation=&quot;Information gain is defined as $\\text{Gain}(S, A) = H(S) - \\sum_{v \\in \\text{Values}(A)} \\frac{|S_v|}{|S|} H(S_v)$ where $H(S) = -\\sum p_k \\log_2 p_k$ is the entropy. The feature and threshold that maximize information gain are chosen for the split. Gini impurity ($1 - \\sum p_k^2$) is an alternative criterion that is faster to compute."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"What is the key difference between bagging (Random Forest) and boosting (Gradient Boosting)?&quot; options={[ 'Bagging trains independent models in parallel on bootstrap samples and averages predictions; boosting trains models sequentially, each correcting the errors of the previous', 'Bagging uses only one model; boosting uses many', 'Both are identical in methodology', 'Bagging corrects errors sequentially; boosting trains independently', ]} correctAnswer={0} explanation=&quot;Bagging (Bootstrap Aggregating) trains each model independently on a random bootstrap sample of the data, then aggregates predictions (majority vote for classification, average for regression). This reduces variance. Boosting trains models sequentially: each new model fits the residuals (negative gradient) of the ensemble so far, reducing bias."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"In backpropagation for a multi-layer neural network, how is the gradient at layer $l$ computed?&quot; options={[ &quot;Using the chain rule: $\\delta[l] = (W[l+1]^T \\delta[l+1]) \\circ \\sigma'(z[l])$&quot;, 'By directly differentiating the loss with respect to $W[l]$ without intermediate layers', 'Using numerical differentiation for each weight', 'By random gradient estimation', ]} correctAnswer={0} explanation=&quot;Backpropagation applies the chain rule layer by layer from output to input. The error signal $\\delta[L]$ at the output layer is computed from the loss gradient. For hidden layers: $\\delta[l] = (W[l+1]^T \\delta[l+1]) \\circ \\sigma\'(z[l])$ where $\\circ$ is element-wise multiplication and $\\sigma\'$ is the activation derivative. Weight gradients: $\\nabla W[l] = \\delta[l] \\cdot a[l-1]^T$."} difficulty="hard"  />

## Regularization, Evaluation, and Bias-Variance

<PracticeProblem client:only="solid-js" question={"L1 regularization (Lasso) produces sparse solutions because it drives some weights to exactly zero. L2 regularization (Ridge) shrinks weights but rarely to zero. Which regularization promotes sparsity and why?&quot; options={[ 'L1: the absolute value penalty creates sharp corners at zero in the loss landscape, making zero-weight solutions optimal for some features', 'L2: the squared penalty creates sharp corners at zero', 'Both L1 and L2 produce equally sparse solutions', 'Neither produces sparsity; only dropout does', ]} correctAnswer={0} explanation=&quot;The L1 penalty $\\lambda \\sum |w_j|$ has a diamond-shaped constraint region with corners on the axes. The loss function\'s elliptical contours often touch a corner first, setting some weights to exactly zero. L2's circular constraint region touches contours at non-zero weights, shrinking but rarely zeroing them. This makes L1 useful for feature selection."} difficulty="hard"  />

<PracticeProblem client:only="solid-js" question={"On an imbalanced dataset with 99% negative examples, a model predicting all negatives achieves 99% accuracy. Which metric is more appropriate?&quot; options={[ 'F1 score (harmonic mean of precision and recall) or AUC-ROC', 'Accuracy (it correctly reflects overall performance)', 'Mean squared error', 'Training loss', ]} correctAnswer={0} explanation=&quot;Accuracy is misleading on imbalanced data: predicting all negatives gives 99% accuracy but learns nothing useful. Precision ($\\frac{TP}{TP+FP}$) measures how many positive predictions are correct. Recall ($\\frac{TP}{TP+FN}$) measures how many actual positives are found. F1 ($2 \\cdot \\frac{P \\cdot R}{P+R}$) balances both. AUC-ROC measures ranking quality across all thresholds."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"The expected error of a model decomposes as $\\text{Error} = \\text{Bias}^2 + \\text{Variance} + \\text{Irreducible Error}$. A model with high training accuracy but low validation accuracy is exhibiting:&quot; options={[ 'High variance (overfitting)', 'High bias (underfitting)', 'Low irreducible error', 'Optimal bias-variance tradeoff', ]} correctAnswer={0} explanation=&quot;High training accuracy with low validation accuracy indicates overfitting: the model has memorized training data (including noise) but fails to generalize. This corresponds to high variance. Remedies include more training data, reducing model complexity, adding regularization (L1/L2/dropout), and early stopping. High bias (underfitting) shows high error on both training and validation sets."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Dropout regularization during training randomly sets neuron activations to zero with probability $p$. At test time, how are the activations adjusted?&quot; options={[ 'No dropout is applied; activations are scaled by $(1-p)$ during training to compensate', 'Dropout is applied with the same probability $p$', 'All activations are multiplied by $p$', 'Dropout is applied with probability $1-p$', ]} correctAnswer={0} explanation=&quot;During training, dropout sets each activation to zero with probability $p$ and scales surviving activations by $1/(1-p)$ (inverted dropout). This ensures the expected activation magnitude remains the same. At test time, no dropout is applied and no scaling is needed (since training already compensated). This creates an implicit ensemble of $2^n$ thinned networks."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"The Universal Approximation Theorem states that a feedforward neural network with a single hidden layer can approximate any continuous function on compact subsets of $\\mathbb{R}^d$. What is the key caveat?&quot; options={[ 'The theorem guarantees existence but not learnability; the hidden layer may need exponentially many neurons', 'The theorem only applies to classification, not regression', 'It requires at least 10 hidden layers', 'It only works with ReLU activation', ]} correctAnswer={0} explanation=&quot;The theorem proves that a sufficiently wide single hidden layer can approximate any continuous function to arbitrary precision. However, it says nothing about whether gradient descent can find the correct weights, how many neurons are needed (potentially exponential), or practical training time. Deeper networks can represent functions more efficiently (exponentially fewer parameters for certain function classes)."} difficulty="hard"  />

## Cross-References

- **[Algorithms](../../1-algorithms/index.md):** Optimisation algorithms used in machine learning.
- **[Discrete Mathematics](../../1-discrete-mathematics/index.md):** Mathematical foundations for statistical learning theory.
