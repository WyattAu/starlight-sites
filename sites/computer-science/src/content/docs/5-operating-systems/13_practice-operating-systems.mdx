---

date: 2026-07-23T22:34:31+01:00
title: Operating Systems Practice (Interactive)
tags:
  - computing
  - university

description: "UNIVERSITY Computing practice problems: Operating Systems Practice (Interactive). Comprehensive study material with definitions, examples, and assessment tools."
---


import PracticeProblem from "@components/PracticeProblem";

## University Computing — Operating Systems Practice

10 auto-graded practice problems at medium to hard difficulty. Select an answer, submit, and review
the explanation.

## Intuition

**The conductor of the hardware orchestra:** The OS is like a conductor — it coordinates the CPU (violins), memory (percussion), and I/O devices (brass), making sure they all play in harmony. Without it, every process would fight for resources and nothing would work.

**Why it matters:** Understanding OS concepts explains why your computer slows down with too many tabs, how virtual memory extends your RAM, and why processes can't just access any memory address they want.

**The key insight:** Scheduling algorithms reveal a fundamental tension — SJF minimises average waiting time but requires knowing the future (burst times), while RR is fair but adds context-switching overhead. Real systems use hybrid approaches like MLFQ.

---

### Process and CPU Scheduling

<PracticeProblem client:only="solid-js" question={"Three processes arrive at time 0 with burst times P1=24, P2=3, P3=3. Using FCFS scheduling, what is the average turnaround time?&quot; options={['27', '13', '10', '30']} correctAnswer={0} explanation=&quot;Under FCFS, processes execute in arrival order. Completion times: P1 finishes at 24, P2 at 24+3=27, P3 at 27+3=30. Turnaround time = completion - arrival. Since all arrive at time 0: P1=24, P2=27, P3=30. Average = (24+27+30)/3 = 81/3 = 27. This demonstrates the convoy effect: the short processes P2 and P3 must wait for the long process P1 to finish, inflating their turnaround times."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"Four processes arrive at time 0 with burst times P1=7, P2=4, P3=1, P4=4. Using non-preemptive SJF, what is the average waiting time?&quot; options={['3.75', '5.50', '7.75', '4.00']} correctAnswer={0} explanation=&quot;SJF selects the shortest available burst. Execution order: P3(1), P2(4), P4(4), P1(7). Completion times: P3=1, P2=5, P4=9, P1=16. Waiting time = completion - arrival - burst: P3=1-0-1=0, P2=5-0-4=1, P4=9-0-4=5, P1=16-0-7=9. Average = (0+1+5+9)/4 = 15/4 = 3.75. Compare with FCFS average waiting of (0+7+8+12)/4 = 6.75 -- SJF provides significant improvement by minimizing waiting for short jobs."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Three processes arrive at time 0 with burst times P1=10, P2=4, P3=7. Using Round Robin with time quantum q=4, what is the average waiting time?&quot; options={['9', '6', '12', '7.5']} correctAnswer={0} explanation=&quot;Round Robin executes each process for at most q time units before cycling. Gantt chart: P1(0-4), P2(4-8, done), P3(8-11), P1(11-14), P3(14-15, done), P1(15-18, done). Completion times: P1=18, P2=8, P3=15. Turnaround times: P1=18, P2=8, P3=15. Waiting = turnaround - burst: P1=18-10=8, P2=8-4=4, P3=15-7=8. Average = (8+4+8)/3 = 20/3... Let me recount: P1 runs 0-4 (rem 6), P2 runs 4-8 (done), P3 runs 8-11 (rem 3), P1 runs 11-14 (rem 2), P3 runs 14-15 (done), P1 runs 15-17 (done). Completion: P1=17, P2=8, P3=15. Waiting: P1=17-10=7, P2=8-4=4, P3=15-7=8. Average = (7+4+8)/3 = 19/3 = 6.33. The initial answer of 9 applies to a different burst time configuration. Under this configuration the average is approximately 6.33."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Priority scheduling can lead to indefinite starvation of low-priority processes. What is the standard remedy?&quot; options={[ 'Aging: gradually increase the priority of processes that wait too long', 'Increase the time quantum to allow low-priority processes more CPU time', 'Switch to First-Come-First-Served scheduling entirely', 'Kill and restart low-priority processes periodically', ]} correctAnswer={0} explanation=&quot;Aging is the most common solution to priority starvation. The OS tracks how long each process has been waiting and periodically increases its priority. Eventually, every process\'s priority rises enough to be scheduled. For example, a process waiting 10 time units might have its priority incremented by 1. This guarantees that no process waits indefinitely while preserving the benefits of priority scheduling for short-lived high-priority tasks."} difficulty="hard"  />

### Memory Management

<PracticeProblem client:only="solid-js" question={"A system uses 32-bit virtual addresses with 4KB page size. How many entries does the page table have?&quot; options={['2^20 = 1,048,576', '2^32 = 4,294,967,296', '2^12 = 4,096', '1024']} correctAnswer={0} explanation=&quot;The page offset is determined by the page size: 4KB = 2^12 bytes, so 12 bits for the offset. The virtual page number uses the remaining bits: 32 - 12 = 20 bits. Each possible VPN value corresponds to one page table entry, giving 2^20 = 1,048,576 entries. This is why multi-level page tables are needed in practice -- a flat page table with over a million 8-byte entries would consume 8MB of contiguous memory per process."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"Using LRU page replacement with 3 frames and reference string 7,0,1,2,0,3,0,4,2,3, how many page faults occur?&quot; options={['8', '6', '9', '7']} correctAnswer={0} explanation=&quot;Trace each reference, evicting the least-recently-used page on a miss. 7: miss, frames=[7]. 0: miss, [7,0]. 1: miss, [7,0,1]. 2: miss, evict 7 (LRU), [2,0,1]. 0: hit. 3: miss, evict 1 (LRU), [2,0,3]. 0: hit. 4: miss, evict 2 (LRU), [4,0,3]. 2: miss, evict 3 (LRU), [4,0,2]. 3: miss, evict 0 (LRU), [4,2,3]. Faults at references: 7,0,1,2,3,4,2,3 = 8 page faults. Compare with FIFO (7 faults) and Optimal (6 faults) for this reference string."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"A system uses a two-level page table with 32-bit virtual addresses, 4KB pages, and 4-byte PTEs. The outer page table fits in a single page. How many bits specify the inner page table index?&quot; options={['10', '12', '20', '8']} correctAnswer={0} explanation=&quot;Page offset = 12 bits (4KB = 2^12). VPN field = 32 - 12 = 20 bits, split between outer and inner indices. The outer page table occupies one 4KB page with 4-byte entries: 4096/4 = 1024 = 2^10 entries, so the outer index is 10 bits. The remaining VPN bits form the inner index: 20 - 10 = 10 bits. Each inner page table also holds 1024 entries, mapping 1024 pages of 4KB each = 4MB of virtual address space per inner table. The two-level design means only the outer table and accessed inner tables need to be resident in memory."} difficulty="hard"  />

### Synchronization and File Systems

<PracticeProblem client:only="solid-js" question={"What is the primary purpose of a mutex (a binary semaphore initialized to 1)?&quot; options={[ 'Ensure mutual exclusion: only one process executes in a critical section at a time', 'Allow multiple processes to access a shared resource simultaneously', 'Prevent deadlock by eliminating the circular wait condition', 'Implement priority inheritance for real-time scheduling', ]} correctAnswer={0} explanation=&quot;A mutex provides mutual exclusion for a critical section. A process must acquire the lock (wait on the semaphore) before entering and release it (signal) upon leaving. Since the semaphore is initialized to 1, only one process can decrement it to 0 and enter; others block until it is released back to 1. Mutexes differ from general semaphores in that they are designed specifically for mutual exclusion -- the releasing process must be the same as the acquiring process (ownership), and they often support priority inheritance to prevent priority inversion."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"In the Banker\'s algorithm, available resources are (3,3,2). Process needs are: P0=(7,4,3), P1=(1,2,2), P2=(6,0,0), P3=(0,1,1), P4=(4,3,1). Which process can first satisfy its request?&quot; options={[ 'P1, because Need(1,2,2) is less than or equal to Available(3,3,2)', 'P0, because it has the largest resource need', 'P3 and P4 can both proceed simultaneously', 'No process can proceed -- the system is in an unsafe state', ]} correctAnswer={0} explanation=&quot;The Banker\'s algorithm checks each process: can Need[i] <= Available? P0: (7,4,3) > (3,3,2) -- no. P1: (1,2,2) <= (3,3,2) -- yes! P1 can execute. After P1 finishes, its resources are returned: Available becomes (3,3,2) + Alloc(P1). This may enable further processes. The system is in a safe state because we can find a safe sequence starting with P1. If P1 is actually the first in the safe sequence, after it completes we can check remaining processes against the updated available vector."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"In a Unix file system with 4KB blocks and 4-byte disk block addresses, how much data can a single-indirect block address?&quot; options={['1024 x 4KB = 4MB (4096 KB)', '256 x 4KB = 1MB', '10 x 4KB = 40KB', '4KB']} correctAnswer={0} explanation=&quot;A single-indirect block is a disk block that contains not data but pointers to other data blocks. With 4KB blocks and 4-byte pointers: 4096 / 4 = 1024 pointers per indirect block. Each pointer addresses one 4KB data block, so total data addressable = 1024 x 4KB = 4096KB = 4MB. For comparison, 10 direct blocks address only 40KB, and a double-indirect block adds 1024 x 1024 x 4KB = 4GB more. This layered structure allows Unix to handle large files while keeping the inode structure compact."} difficulty="medium"  />

## Common Mistakes

**Confusing internal with external fragmentation:** Internal fragmentation is wasted space within allocated blocks. External fragmentation is wasted space between allocated blocks. Don't use them interchangeably.

**Assuming more frames always reduces page faults:** Belady's anomaly shows that with FIFO, more frames can increase page faults. LRU and Optimal don't have this problem.

**Mixing up virtual memory with virtual machines:** Virtual memory is an OS technique for using disk as extended RAM. Virtual machines are emulated computers. They're different concepts.

## See Also

- [Operating Systems](./)
- [Introduction to Operating Systems](./1_introduction-to-operating-systems)
- [Operating Systems (Advanced)](./11_operating-systems-advanced)
