---

date: 2026-07-23T22:34:31+01:00
title: "C++ Flashcards: Error Handling"
description: "PROGRAMMING 5_function_architecture flashcards: C++ Flashcards: Error Handling. Comprehensive study material with definitions, examples, and assessment tools."
tags:
  - C++
  - Flashcards
---


import FlashcardDeck from "@components/FlashcardDeck";
import { flashcards1, flashcards2, flashcards3, flashcards4 } from '@/data/flashcards/5_function_architecture/2_error_handling/flashcards-error-handling-data.js';

## C++ -- Error Handling Flashcards

20 interactive flashcards. Press Space to flip, rate 1-4 to schedule next review.

---

## Intuition

Error handling is about graceful recovery when things go wrong. Exceptions let you separate the "happy path" from error recovery — throw when something fails, catch where you can recover. Think of stack unwinding as a domino effect: when an exception is thrown, each local object's destructor runs as the call stack collapses, cleaning up resources automatically. Modern C++ also offers `std::optional` for "may not have a value" and `std::expected` (C++23) for "may have an error."

## Common Pitfalls

- **Throwing in destructors**: If a destructor throws during stack unwinding from another exception, `std::terminate` is called immediately — always mark destructors `noexcept` or catch internally.
- **Exception specifications**: The old `throw()` syntax is deprecated — use `noexcept` instead, and understand that `noexcept` functions that do throw will call `std::terminate`.
- **Catching by value**: Catching exceptions by value causes slicing — always catch by reference (`catch (const std::exception& e)`).

---

<FlashcardDeck client:only="solid-js"   deckId="cpp-error-handling"
  title="C++ Error Handling Flashcards"
  description="Interactive flashcards on C++ exceptions, std::exception hierarchy, error codes vs exceptions, std::optional, std::expected, stack unwinding, and safety."
  cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]}
 />

## Cross-References

- **[Lambdas](../1_function_mechanics/3_lambdas.md):** Lambda expressions that can be used in error handling patterns.
- **[Memory Management](../../resource_management/1_ownership_and_raii/):** RAII and smart pointers that provide exception-safe resource management.
- **[Object-Oriented Programming](../../object_oriented/):** OOP patterns for error handling hierarchies.
