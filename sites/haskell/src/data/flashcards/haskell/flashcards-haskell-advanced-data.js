export const flashcardData = [
  {
    id: 'haskell-advanced-functor-001',
    front: 'What is the Functor type class and what laws must an instance satisfy?',
    back: 'Functor defines fmap :: (a -> b) -> f a -> f b, which applies a function inside a context. Laws: identity (fmap id = id) and composition (fmap (f . g) = fmap f . fmap g). Every Functor must satisfy both laws for predictable behavior.',
    tags: ['haskell', 'type-classes', 'functor'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-advanced-applicative-002',
    front: 'How does Applicative extend Functor and what does it add?',
    back: 'Applicative adds pure :: a -> f a and (<*>) :: f (a -> b) -> f a -> f b. pure lifts a value into the context. <*> applies a wrapped function to a wrapped value. Unlike Monad, Applicative cannot make the second argument depend on the first, enabling parallel computation.',
    tags: ['haskell', 'type-classes', 'applicative'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-advanced-monad-003',
    front: 'What does Monad add beyond Applicative and what is bind (>>=)?',
    back: 'Monad adds (>>=) :: m a -> (a -> m b) -> m b and return (= pure). Bind chains monadic computations where each step can depend on the result of the previous step. This enables sequencing of effects and is the basis for do notation.',
    tags: ['haskell', 'type-classes', 'monad'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-advanced-monadtrans-004',
    front: 'What problem do monad transformers solve and how does MonadTrans work?',
    back: 'MonadTrans provides lift :: Monad m => m a -> t m a to embed one monad inside another. Transformers like StateT, ExceptT, and ReaderT stack capabilities. The type class MonadTrans defines lift; specific transformers add their own operations (get, put, throwError).',
    tags: ['haskell', 'monad-transformers'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-advanced-foldable-005',
    front: 'What is the Foldable type class and what does foldMap do?',
    back: 'Foldable defines foldr, foldl, foldMap, and other traversal functions for structures containing elements. foldMap :: (Foldable t, Monoid m) => (a -> m) -> t a -> m applies a function and combines results with the Monoid instance. It generalizes foldr and is often the most efficient fold to implement.',
    tags: ['haskell', 'type-classes', 'foldable'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-advanced-traversable-006',
    front: 'How does Traversable extend Foldable and what does traverse do?',
    back: 'Traversable adds traverse :: (Applicative f, Traversable t) => (a -> f b) -> t a -> f (t b) and sequence. traverse applies an effectful function to each element and collects results in the Applicative context. It preserves the structure while effects are sequenced. Traversable implies both Functor and Foldable.',
    tags: ['haskell', 'type-classes', 'traversable'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-advanced-lens-007',
    front: 'What is a lens and what is its type signature?',
    back: 'A lens is a first-class accessor for nested data structures. Type: Lens s t a b = forall f. Functor f => (a -> f b) -> s -> f t. Lenses allow composition with (.), getter access via view, setter access via set, and modification via over. The lens library provides makeLenses for automatic generation.',
    tags: ['haskell', 'lens'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-advanced-stm-008',
    front: 'What is STM and how does it differ from IORef?',
    back: 'STM (Software Transactional Memory) provides composable, atomic transactions using TVar. Transactions are retried automatically on conflict, preventing deadlocks. IORef offers simple mutable references but requires manual synchronization (MVar, IORef with atomicModifyIORef). STM is composable; IORef operations are not.',
    tags: ['haskell', 'concurrency', 'stm'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-advanced-ioref-009',
    front: 'When would you use IORef over STM and what are the tradeoffs?',
    back: 'IORef is simpler and faster for single-threaded mutable state within IO. Use it when no concurrent access is needed. STM is necessary for concurrent reads/writes because IORef has no transactional guarantees. atomicModifyIORef provides atomic updates but is not composable across multiple IORefs.',
    tags: ['haskell', 'concurrency', 'iorefs'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-advanced-phantom-010',
    front: 'What is a phantom type and how is it used for type-level safety?',
    back: 'A phantom type parameter appears on the left side of = but not in any constructor field. Example: newtype Meter a = Meter Double where a is phantom. This prevents mixing units: Meter Meter and Meter Foot are distinct types. The type is erased at runtime, so there is zero cost.',
    tags: ['haskell', 'types', 'phantom'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-advanced-kind-poly-011',
    front: 'What is kind polymorphism and when is it needed?',
    back: 'Kind polymorphism allows type parameters to be polymorphic over kinds (the type of types). Enable with PolyKinds. Example: class Functor f where type Domain f :: Type; type Codomain f :: Type. This allows defining abstractions over type constructors of different arities, essential for libraries like kind-generics.',
    tags: ['haskell', 'types', 'kinds'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-advanced-type-families-012',
    front: 'What are type families and how do they differ from type classes?',
    back: 'Type families are type-level functions associated with type classes. They can be open (extensible) or closed (fixed). Example: class Collection c where type Element c; insert :: Element c -> c -> c. Unlike multi-parameter type classes, type families compute types from other types, enabling more expressive type-level programming.',
    tags: ['haskell', 'types', 'type-families'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-advanced-gadts-013',
    front: 'What are GADTs and what advantage do they provide over regular ADTs?',
    back: 'GADTs (Generalized Algebraic Data Types) give each constructor an explicit type signature. This lets the type checker enforce invariants: data Expr a where Lit :: Int -> Expr Int; Add :: Expr Int -> Expr Int -> Expr Int ensures Add only works on Int expressions. The type parameter narrows in each constructor.',
    tags: ['haskell', 'types', 'gadts'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-advanced-coyoneda-014',
    front: 'What is the Coyoneda functor and why is it useful?',
    back: 'Coyoneda f a = forall b. (a -> b) -> f b. It is the free functor: any function a -> b can be lifted into Coyoneda f without requiring a Functor instance for f. This is useful for composing functors in a deferred manner and is the basis for the Coyoneda-based free monad, which avoids the cost of repeated fmap.',
    tags: ['haskell', 'functors', 'coyoneda'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-advanced-free-monad-015',
    front: 'What is a free monad and when would you use one?',
    back: 'FreeMonad f a = Pure a | Free (f (FreeMonad f a)). It creates a monad from any Functor f, representing a tree of computations. Use it for: DSLs (interpret the structure differently at runtime), testing (record operations instead of executing), and interpreters (defer side effects). Tradeoff: O(n) bind vs O(1) for built-in monads.',
    tags: ['haskell', 'monads', 'free-monad'],
    difficulty: 'hard',
  },
]
