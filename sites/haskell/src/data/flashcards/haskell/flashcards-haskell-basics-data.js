export const flashcardData = [
  {
    id: 'haskell-paradigm-001',
    front: 'What does "purely functional" mean in Haskell?',
    back: 'Functions have no side effects and are referentially transparent -- the same inputs always produce the same outputs. Side effects (I/O, state) are isolated in the IO monad and tracked in the type system.',
    tags: ['haskell', 'paradigm'],
    difficulty: 'easy',
  },
  {
    id: 'haskell-lazy-eval-002',
    front: 'What is lazy evaluation (call-by-need)?',
    back: 'Expressions are not evaluated until their results are needed. This allows working with infinite data structures, modular composition, and avoids unnecessary computation. Thunks represent unevaluated expressions in memory.',
    tags: ['haskell', 'evaluation'],
    difficulty: 'easy',
  },
  {
    id: 'haskell-type-inference-003',
    front: 'What is Hindley-Milner type inference?',
    back: 'The compiler deduces the types of expressions automatically. Type variables (like "a") represent polymorphic types. Most types do not need explicit annotations, but top-level signatures are recommended for documentation and error localization.',
    tags: ['haskell', 'types'],
    difficulty: 'easy',
  },
  {
    id: 'haskell-currying-004',
    front: 'What is currying in Haskell?',
    back: 'Every function takes exactly one argument. A function with signature a -> b -> c is really a -> (b -> c). Calling it with one argument returns a partially applied function. This enables partial application and point-free style.',
    tags: ['haskell', 'functions'],
    difficulty: 'easy',
  },
  {
    id: 'haskell-pattern-match-005',
    front: 'How does pattern matching work in Haskell?',
    back: 'Pattern matching destructures values by structure. The wildcard _ matches anything without binding. Guards (`| condition`) add boolean tests. Patterns are tried top to bottom. Irrefutable patterns (~) always match. As-patterns (@) bind the whole value while destructuring.',
    tags: ['haskell', 'patterns'],
    difficulty: 'easy',
  },
  {
    id: 'haskell-type-class-006',
    front: 'What is a type class in Haskell?',
    back: 'A type class defines a set of functions that types must implement. It is a interfaces on steroids -- ad-hoc polymorphism. Example: `class Eq a where (==) :: a -> a -> Bool`. Types declare instances: `instance Eq Color where Red == Red = True`. Type classes support multiple dispatch.',
    tags: ['haskell', 'type-classes'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-maybe-007',
    front: 'How does the Maybe type handle absence?',
    back: 'Maybe a = Just a | Nothing. Just wraps a present value; Nothing represents absence. The bind operator (>>=) short-circuits on Nothing: if any step returns Nothing, the entire chain returns Nothing. This eliminates null checks and nested conditionals.',
    tags: ['haskell', 'types', 'maybe'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-either-008',
    front: 'How does Either differ from Maybe for error handling?',
    back: 'Either e a = Left e | Right a. Left carries error information; Right carries success. Unlike Maybe which only signals presence/absence, Either provides context about what went wrong. The bind operator short-circuits on Left, propagating the error value.',
    tags: ['haskell', 'types', 'either'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-io-009',
    front: 'What is the IO monad and why is it special?',
    back: 'IO a represents a computation that performs side effects and returns a value of type a. It is special because Haskell is pure -- IO is the only way to interact with the outside world. The type system tracks which computations have effects. `return` lifts a pure value into IO.',
    tags: ['haskell', 'monads', 'io'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-do-notation-010',
    front: 'How does do notation desugar?',
    back: 'x <- action becomes action >>= (\\x -> ...). Lines without <- use >> for sequencing. The last line is the result. "let x = e" does not use bind. do notation is syntactic sugar for nested >>= calls, making monadic code read like imperative code.',
    tags: ['haskell', 'monads', 'do-notation'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-foldr-011',
    front: 'When should you use foldr vs foldl?',
    back: "foldr builds lists and processes infinite lists (foldr (:) [] xs). foldl computes accumulated values like sums. foldl can cause space leaks due to thunk buildup; foldl' (strict variant) forces the accumulator. foldr is natural for algebraic data types; foldl' for numeric accumulations.",
    tags: ['haskell', 'functions', 'folds'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-monad-transformers-012',
    front: 'What problem do monad transformers solve?',
    back: 'Different monads handle different effects (Maybe for failure, State for mutable state, IO for side effects). Real applications need multiple effects simultaneously. Transformers like MaybeT, StateT, and ExceptT stack monads to combine their capabilities in a single transformer stack.',
    tags: ['haskell', 'monads', 'transformers'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-stm-013',
    front: 'How does Software Transactional Memory prevent deadlocks?',
    back: 'STM transactions are atomic: all reads/writes happen or none do. Transactions auto-retry on TVar conflicts. Since there are no locks, there is no deadlock from lock ordering. `retry` blocks until a read TVar changes. `atomically` executes a transaction. TVars are shared mutable state.',
    tags: ['haskell', 'concurrency', 'stm'],
    difficulty: 'medium',
  },
  {
    id: 'haskell-gadts-014',
    front: 'What are GADTs in Haskell?',
    back: 'GADTs (Generalized Algebraic Data Types) give each constructor an explicit return type. This lets the type checker enforce invariants: `data Expr a where Lit :: Int -> Expr Int; Add :: Expr Int -> Expr Int -> Expr Int` ensures Add only works on Int expressions. The type parameter narrows per constructor.',
    tags: ['haskell', 'types', 'gadts'],
    difficulty: 'hard',
  },
  {
    id: 'haskell-state-monad-015',
    front: 'How does the State monad work internally?',
    back: 'State s a is a newtype over s -> (a, s) -- a function from initial state to (result, new state). get reads the state, put replaces it, modify transforms it. runState applies the function, evalState returns only the result, execState returns only the final state.',
    tags: ['haskell', 'monads', 'state'],
    difficulty: 'hard',
  },
]
