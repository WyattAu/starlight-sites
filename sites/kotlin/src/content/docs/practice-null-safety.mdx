---
title: Kotlin Null Safety Practice (Interactive)
tags:
  - kotlin
description: "8 auto-graded practice problems covering Kotlin null safety, scope functions, sealed classes, and coroutines. Select an answer, submit, and review the"
---

import PracticeProblem from "@components/PracticeProblem";


## Kotlin — Null Safety Practice

8 auto-graded practice problems covering Kotlin null safety, scope functions, sealed classes, and
coroutines. Select an answer, submit, and review the explanation.

---

### Null Safety

<PracticeProblem client:only="solid-js" question={"What does `val length: Int = name?.length` compile to when `name` is of type `String?`?&quot; options={[ 'Compilation error: Type mismatch (Int? vs Int)', '0', 'null', 'It compiles successfully', ]} correctAnswer={0} explanation='The safe call operator `?.` returns `null` if the receiver is null, so `name?.length` has type `Int?`. Assigning `Int?` to `Int` causes a compilation error: &quot;Type mismatch: inferred type is Int? but Int was expected.&quot; You need `val length: Int? = name?.length` or use a default.' difficulty=&quot;easy"}  />

<PracticeProblem client:only="solid-js" question={"What does `val len = name?.length ?: 0` do when `name` is null?&quot; options={['Returns 0', 'Throws NullPointerException', 'Returns -1', 'Compilation error']} correctAnswer={0} explanation=&quot;The elvis operator `?:` provides a default value when the left side is null. If `name` is null, `name?.length` is null, so the elvis operator returns 0. This is a common Kotlin idiom for handling nullable types."} difficulty="easy"  />

---

### Scope Functions

<PracticeProblem client:only="solid-js" question='What does `val x = "hello".let { it.uppercase() }` return?' options={['\"HELLO\"', '\"hello\"', 'Unit', 'Compilation error']} correctAnswer={0} explanation='`let` executes the lambda with the receiver as `it`, and returns the lambda&apos;s result. `"hello".let { it.uppercase() }` calls `uppercase()` on "hello", returning "HELLO".' difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"What is the difference between `apply` and `also`?&quot; options={[ 'apply uses `this` as context; also uses `it` as argument — both return the receiver object', 'apply returns the lambda result; also returns the receiver', 'There is no difference', 'apply is for nullable types; also is for non-null', ]} correctAnswer={0} explanation=&quot;Both `apply` and `also` return the receiver object (not the lambda result). The difference is: `apply` uses `this` as the context object inside the block (useful for configuring objects), while `also` uses `it` as the lambda argument (useful for side effects like logging)."} difficulty="medium"  />

---

### Sealed Classes

<PracticeProblem client:only="solid-js" question={"What advantage does a `sealed class` provide over an `open class` for modelling a restricted hierarchy?&quot; options={[ 'The compiler ensures all subclasses are known and handles exhaustiveness in `when` expressions', 'Sealed classes are faster at runtime', 'Sealed classes cannot have properties', 'Sealed classes can only have one subclass', ]} correctAnswer={0} explanation=&quot;Sealed classes restrict inheritance to a known set of subclasses defined in the same file/package. This enables the compiler to verify that all cases are handled in `when` expressions (exhaustive checking), making the code safer. In Kotlin 1.5+, subclasses can be in the same package."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Given `sealed class Result { data class Success(val data: String) : Result(); data class Error(val message: String) : Result() }`, which `when` expression is exhaustive?&quot; options={[ 'when (r) { is Result.Success -> ...; is Result.Error -> ... }', 'when (r) { is Result.Success -> ... }', 'when (r) { }', 'when (r) { else -> ... }', ]} correctAnswer={0} explanation=&quot;Since `Result` is a sealed class with exactly two subclasses (`Success` and `Error`), a `when` expression covering both `is Result.Success` and `is Result.Error` branches is exhaustive — no `else` branch needed. The compiler verifies all sealed subclasses are handled."} difficulty="easy"  />

---

### Coroutines

<PracticeProblem client:only="solid-js" question={"What is the key difference between `launch` and `async` in Kotlin coroutines?&quot; options={[ 'launch returns a Job (fire-and-forget); async returns a Deferred<T> that produces a result', 'launch is blocking; async is non-blocking', 'launch runs on IO dispatcher; async runs on Default', 'There is no difference', ]} correctAnswer={0} explanation=&quot;`launch` starts a coroutine that returns a `Job` — it\'s fire-and-forget and doesn\'t return a value. `async` starts a coroutine that returns a `Deferred<T>`, which you can await with `.await()` to get the computed result. Use `async` when you need a result."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"What does the `suspend` keyword on a function signify?&quot; options={[ 'The function can be paused and resumed, and can only be called from a coroutine or another suspend function', 'The function runs on a separate thread', 'The function blocks the calling thread', 'The function is marked for garbage collection', ]} correctAnswer={0} explanation=&quot;A `suspend` function can suspend execution at suspension points without blocking a thread. It can only be called from within a coroutine or another suspend function. The compiler transforms suspend functions into state machines that handle suspension and resumption efficiently."} difficulty="easy"  />

## Common Mistakes

**Using `!!` (not-null assertion) as a shortcut:** `!!` throws a NullPointerException if the value is null. It defeats the purpose of null safety. Use safe calls (`?.`), Elvis operator (`?:`), or explicit null checks instead.

**Forgetting that smart casts dont work with mutable properties:** If a variable is declared `var` and could be modified between the null check and usage, Kotlin cannot smart-cast it. Use a local val or explicit cast to work around this.

**Confusing `let` with `apply` for null handling:** `?.let { ... }` executes the block only if the receiver is non-null and provides the non-null value as `it`. `?.apply { ... }` executes only if non-null but provides the receiver as `this`.
