export const flashcardData = [
  {
    id: 'kotlin-val-001',
    front: 'What is the difference between `val` and `var` in Kotlin?',
    back: '`val` declares an immutable read-only reference (like Java final). The reference cannot be reassigned, but the object itself may be mutable. `var` declares a mutable reference that can be reassigned. Prefer `val` unless reassignment is required.',
    tags: ['kotlin', 'variables'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-var-002',
    front: 'When should you use `var` instead of `val` in Kotlin?',
    back: 'Use `var` only when the variable must be reassigned, such as loop counters, accumulators, or state that changes over time. In most cases `val` is preferred for immutability and thread safety. Local variables should default to `val`.',
    tags: ['kotlin', 'variables'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-data-class-003',
    front: 'What does a Kotlin `data class` provide automatically?',
    back: 'A `data class` generates `equals()`, `hashCode()`, `toString()`, `componentN()` functions (for destructuring), and a `copy()` method. The primary constructor must have at least one parameter. Data classes cannot be abstract, open, sealed, or inner.',
    tags: ['kotlin', 'data-classes'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-sealed-class-004',
    front: 'What are sealed classes and sealed interfaces in Kotlin?',
    back: 'Sealed classes and sealed interfaces restrict inheritance to a fixed set of subclasses defined in the same package (Kotlin 1.5+). This allows exhaustive `when` expressions without an `else` branch. Ideal for modeling result types, state machines, and domain events.',
    tags: ['kotlin', 'sealed-classes'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-extension-005',
    front: 'What are extension functions and how do they work?',
    back: 'Extension functions add methods to existing classes without modifying their source. They are resolved statically at compile time. Under the hood, `fun String.addExclamation() = this + "!"` compiles to a static function with the receiver as the first parameter. They cannot access private members.',
    tags: ['kotlin', 'extensions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-null-safety-006',
    front: 'How does Kotlin null safety work?',
    back: 'Kotlin distinguishes nullable (`T?`) and non-nullable (`T`) types at compile time. Safe calls (`?.`), elvis operator (`?:`), and non-null assertions (`!!`) handle nullable values. The compiler prevents null pointer exceptions by enforcing null checks before use.',
    tags: ['kotlin', 'null-safety'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-scope-007',
    front: 'What are the five Kotlin scope functions and their differences?',
    back: '`let` (context as `it`, returns lambda result), `run` (context as `this`, returns lambda result), `apply` (context as `this`, returns receiver), `also` (context as `it`, returns receiver), `with` (context as `this`, returns lambda result, not an extension). Use `apply` for configuration, `also` for side effects.',
    tags: ['kotlin', 'scope-functions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-companion-008',
    front: 'What is a companion object in Kotlin?',
    back: 'A companion object is a singleton associated with a class, declared with `companion object`. Its members are accessed via the class name (e.g., `MyClass.create()`), similar to Java static methods. Unlike Java static, a companion object can implement interfaces and be passed as a parameter. One per class.',
    tags: ['kotlin', 'companion-objects'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-inline-009',
    front: 'What are `inline` functions and what are their benefits?',
    back: 'The `inline` keyword tells the compiler to substitute the function body at each call site, avoiding lambda object creation and function call overhead. Useful for higher-order functions. Limitations: cannot access private members of the enclosing class or recurse. `noinline` prevents inlining for specific parameters.',
    tags: ['kotlin', 'inline-functions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-reified-010',
    front: 'What are reified type parameters and why are they needed?',
    back: 'Reified type parameters (`inline` + `reified`) preserve type information at runtime, which is normally erased by the JVM. Example: `inline fun <reified T> isType(value: Any) = value is T`. Only `inline` functions can have reified parameters because the compiler substitutes the actual type at each call site.',
    tags: ['kotlin', 'generics', 'reified'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-coroutines-011',
    front: 'What does the `suspend` keyword mean on a function?',
    back: 'A `suspend` function can suspend execution at suspension points without blocking the thread. It can only be called from coroutines or other suspend functions. The compiler transforms suspend functions into callback-based state machines, enabling structured concurrency with efficient thread utilization.',
    tags: ['kotlin', 'coroutines', 'suspend'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-flow-012',
    front: 'What is a Flow and how does it differ from a Sequence?',
    back: '`Flow` is a cold asynchronous stream of values supporting coroutines and structured concurrency. Unlike `Sequence` (synchronous, single-threaded), `Flow` supports operators like `flowOn` (changing dispatcher), `catch` (error handling), and integrates with coroutine cancellation. Use Flow for asynchronous data streams.',
    tags: ['kotlin', 'coroutines', 'flow'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-channel-013',
    front: 'What is a Channel in Kotlin coroutines?',
    back: 'A `Channel` is a coroutine-safe pipe for communicating between coroutines. It supports buffered, rendezvous (default), and conflated modes. `send` suspends until the item is received; `receive` suspends until an item is available. Channels enable producer-consumer patterns with backpressure.',
    tags: ['kotlin', 'coroutines', 'channels'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-delegate-014',
    front: 'How does property delegation with `by` work in Kotlin?',
    back: 'The `by` keyword delegates property access to a delegate object. Built-in delegates: `by lazy` (lazy initialization, thread-safe by default), `by observable` (notifies on change), `by vetoable` (validates changes). Custom delegates implement `ReadOnlyProperty` or `ReadWriteProperty`.',
    tags: ['kotlin', 'delegation'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-delegate-015',
    front: 'How does class delegation with `by` differ from property delegation?',
    back: 'Class delegation forwards interface implementation to another object: `class Derived(b: Base) : Base by b`. All interface methods are forwarded to `b` unless overridden. Property delegation delegates individual property access. Class delegation replaces inheritance-based delegation patterns and supports the Composition over Inheritance principle.',
    tags: ['kotlin', 'delegation'],
    difficulty: 'medium',
  },
]
