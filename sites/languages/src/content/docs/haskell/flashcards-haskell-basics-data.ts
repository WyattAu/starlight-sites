export const flashcardData = [ { id: 'haskell-paradigm-001', front: 'What does "purely functional" mean in Haskell?', back: 'Functions have no side effects and are referentially transparent -- the same inputs always produce the same outputs. Side effects (I/O, state) are isolated in the IO monad and tracked in the type system.', tags: ['haskell', 'paradigm'], difficulty: 'easy' }, { id: 'haskell-lazy-eval-002', front: 'What is lazy evaluation (call-by-need)?', back: 'Expressions are not evaluated until their results are needed. This allows working with infinite data structures, modular composition, and avoids unnecessary computation. Thunks represent unevaluated expressions in memory.', tags: ['haskell', 'evaluation'], difficulty: 'easy' }, { id: 'haskell-types-003', front: 'What is the difference between Int and Integer in Haskell?', back: 'Int is a fixed-size integer (machine word, typically 64-bit) that can overflow. Integer has arbitrary precision and never overflows. Use Integer when overflow is a concern; Int is faster for bounded values.', tags: ['haskell', 'types'], difficulty: 'easy' }, { id: 'haskell-type-inference-004', front: 'What is Hindley-Milner type inference?', back: 'The compiler deduces the types of expressions automatically. Type variables (like "a") represent polymorphic types. Most types do not need explicit annotations, but top-level signatures are recommended for documentation and error localization.', tags: ['haskell', 'types'], difficulty: 'easy' }, { id: 'haskell-currying-005', front: 'What is currying in Haskell?', back: 'Every function takes exactly one argument. A function with signature a -> b -> c is really a -> (b -> c). Calling it with one argument returns a partially applied function. This enables partial application and point-free style.', tags: ['haskell', 'functions'], difficulty: 'easy' }, { id: 'haskell-guards-006', front: 'What is the difference between "where" and "let" in Haskell?', back: 'where binds local definitions visible across all guards of a function. let is an expression (produces a value) scoped to the expression it precedes. In do notation, let does not require "in"; where is at the end of the do block.', tags: ['haskell', 'syntax'], difficulty: 'easy' }, { id: 'haskell-pattern-match-007', front: 'What does the wildcard pattern (_) do in Haskell?', back: 'The wildcard _ matches any value without binding it. It signals the matched value is not needed, avoids unused variable warnings, and serves as a catch-all in pattern matching. Unlike a variable, it cannot be referenced.', tags: ['haskell', 'patterns'], difficulty: 'easy' }, { id: 'haskell-type-class-008', front: 'What is the difference between "data" and "newtype" in Haskell?', back: 'data introduces a new type with runtime overhead (heap allocation for the wrapper). newtype creates a type isomorphic to the wrapped type with zero runtime overhead -- it is erased during compilation. newtype requires exactly one constructor and one field.', tags: ['haskell', 'types'], difficulty: 'medium' }, { id: 'haskell-maybe-009', front: 'How does the Maybe monad handle failure propagation?', back: 'Just a represents success with value a. Nothing represents failure. The bind operator (>>=) short-circuits on Nothing: if any step in a do-block returns Nothing, the entire computation returns Nothing. This eliminates nested null checks.', tags: ['haskell', 'monads'], difficulty: 'medium' }, { id: 'haskell-foldr-foldl-010', front: 'When should you use foldr vs foldl in Haskell?', back: 'Use foldr for building lists (foldr (:) [] xs) and processing infinite lists. Use foldl (strict variant foldl) for computing single accumulated values like sums. foldl builds thunks and can cause space leaks; foldl forces the accumulator.', tags: ['haskell', 'functions'], difficulty: 'medium' }, { id: 'haskell-do-notation-011', front: 'How does do notation desugar in Haskell?', back: 'x <- action becomes action >>= (\\x -> ...). Lines without <- use >> for sequencing. The last line in a do block is the result. "let x = e" does not use bind at all. Each do-block is just syntactic sugar for nested >>= calls.', tags: ['haskell', 'monads'], difficulty: 'medium' }, { id: 'haskell-functor-012', front: 'What is the Functor type class and its laws?', back: 'Functor has one method: fmap :: (a -> b) -> f a -> f b. It applies a function to a wrapped value. Laws: identity (fmap id = id) and composition (fmap (f . g) = fmap f . fmap g). Maybe, [], IO, and (->) r are all Functors.', tags: ['haskell', 'type-classes'], difficulty: 'medium' }, { id: 'haskell-stm-013', front: 'How does Software Transactional Memory (STM) prevent deadlocks?', back: 'STM transactions are atomic: either all reads and writes happen or none do. Transactions are automatically retried when TVars change. Since there are no locks, there is no possibility of deadlock from lock ordering. retry blocks until a read TVar changes.', tags: ['haskell', 'concurrency'], difficulty: 'medium' }, { id: 'haskell-applicative-014', front: 'How does Applicative differ from Monad in Haskell?', back: 'Applicative applies a wrapped function to a wrapped value using <\*> but cannot make the second argument depend on the first. Monad uses >>= which allows the second computation to depend on the result of the first. Every Monad is an Applicative but not vice versa.', tags: ['haskell', 'type-classes'], difficulty: 'medium' }, { id: 'haskell-higher-order-015', front: 'What are higher-order functions in Haskell?', back: 'Functions that take functions as arguments, return functions, or both. Examples: map applies a function to each element; filter keeps elements satisfying a predicate; foldr/foldl reduce a list to a single value. They are the backbone of functional programming.', tags: ['haskell', 'functions'], difficulty: 'medium' }, { id: 'haskell-monad-transformers-016', front: 'What problem do monad transformers solve?', back: 'Different monads handle different effects (Maybe for failure, State for mutable state, IO for side effects). Real applications need multiple effects simultaneously. Transformers like MaybeT, StateT, and ExceptT stack monads to combine their capabilities.', tags: ['haskell', 'monads'], difficulty: 'medium' }, { id: 'haskell-either-monad-017', front: 'How does the Either monad differ from Maybe for error handling?', back: 'Either e a represents Left e (error with detail) or Right a (success). Unlike Maybe which only has Nothing for failure, Either carries information about what went wrong. The bind operator short-circuits on Left, propagating the error.', tags: ['haskell', 'monads'], difficulty: 'medium' }, { id: 'haskell-as-pattern-018', front: 'What is an as-pattern (@) in Haskell?', back: 'The as-pattern (x@pattern) matches the value and binds the whole matched value to x while also deconstructing it. For example: s@(c:cs) binds s to the entire string and c/cs to head/tail. Useful when you need both the whole value and its parts.', tags: ['haskell', 'patterns'], difficulty: 'medium' }, { id: 'haskell-gadts-019', front: 'What are GADTs and how do they extend regular algebraic data types?', back: 'GADTs (Generalized Algebraic Data Types) allow constructors to have explicit type signatures, so each constructor can return a different type. This lets the type checker enforce invariants that regular ADTs cannot, such as ensuring If-conditions are Bool.', tags: ['haskell', 'types'], difficulty: 'hard' }, { id: 'haskell-st-monad-020', front: 'How does the State monad work internally?', back: 'State s a is a newtype over s -> (a, s) -- a function from initial state to (result, new state). get reads the state, put replaces it, modify transforms it. runState applies the function, evalState returns only the result, execState returns only the final state.', tags: ['haskell', 'monads'], difficulty: 'hard' }, ];
