export const flashcardData = [{id:"java-basics-001", front:"What is the difference between the JVM heap and the stack?", back:"The heap stores objects (allocated via `new`), is shared across all threads, and is managed by the garbage collector. The stack stores primitive local variables, object references, and method frames, is thread-private, and memory is reclaimed when a method returns. StackOverflowError occurs when the stack is exhausted; OutOfMemoryError occurs when the heap is full.", tags:["jvm","memory"], difficulty:"easy"},{id:"java-basics-002", front:"What is JIT compilation and why does the JVM use it?", back:"JIT (Just-In-Time) compilation translates frequently executed bytecode into native machine code at runtime. The JVM starts by interpreting bytecode, profiles which methods are hot, then compiles them natively for faster execution. This hybrid approach gives fast startup (immediate interpretation) and peak performance (compiled hot methods). HotSpot uses C1 (client, fast compile) and C2 (server, aggressive optimization) compilers in a tiered compilation strategy.", tags:["jvm","jit"], difficulty:"medium"},{id:"java-basics-003", front:"What is autoboxing and widening in Java? Which takes precedence?", back:"Autoboxing converts a primitive to its wrapper (e.g., `int` to `Integer`). Widening converts a narrower primitive to a wider one (e.g., `int` to `long`). Widening has higher priority than autoboxing, so `void f(long)` is preferred over `void f(Integer)` when passing an `int`. You cannot widen and then box (e.g., `int` cannot become `Long`) or box and then widen in a single step.", tags:["types","primitives"], difficulty:"medium"},{id:"java-basics-004", front:"What is String interning and how does the String pool work?", back:"String literals are interned: identical literals share a single String object in the String pool (part of the heap). `String.intern()` adds a String to the pool if absent and returns the canonical reference. Two interned Strings with the same content are `==` identical. This saves memory and speeds up equality checks but the pool is unbounded in older JVMs; use it judiciously for dynamic strings.", tags:["strings"], difficulty:"easy"},{id:"java-basics-005", front:"When should you use StringBuilder instead of String concatenation?", back:"Use StringBuilder (or StringBuffer for thread-safe contexts) when building strings in a loop or across multiple statements. String concatenation with `+` in a single expression is optimized by the compiler into StringBuilder, but inside a loop each iteration creates a new StringBuilder, wasting objects. For example, building a CSV line in a loop should use a single StringBuilder with `append` calls.", tags:["strings"], difficulty:"easy"},{id:"java-basics-006", front:"What is the difference between an interface and an abstract class?", back:"An interface defines a contract (methods) but no state (prior to Java 8). An abstract class can have concrete methods, instance fields, and constructors. A class can implement many interfaces but extend only one abstract class. Since Java 8, interfaces can have `default` and `static` methods with bodies. Since Java 9, interfaces can have `private` methods. Use interfaces for pure contracts; use abstract classes when subclasses need shared state or template-method logic.", tags:["oop","design"], difficulty:"easy"},{id:"java-basics-007", front:"What does the `final` keyword mean when applied to a class, method, and variable?", back:"`final class`: cannot be subclassed (e.g., `String`, `Integer`). `final method`: cannot be overridden by subclasses (but can be overloaded). `final variable`: can only be assigned once; for primitives the value is immutable, for references the reference is immutable but the object it points to can still be mutated. `final` on method parameters and local variables is a compile-time check to prevent reassignment.", tags:["oop","keywords"], difficulty:"easy"},{id:"java-basics-008", front:"How does `instanceof` pattern matching (Java 16+) simplify type checks?", back:"The pattern match `obj instanceof String s` combines a type check and a cast in one expression. If `obj` is a `String`, `s` is bound to the cast value and is in scope for subsequent code. This eliminates the common `if (obj instanceof String) { String s = (String) obj; ... }` boilerplate. The pattern variable `s` is only in scope where the pattern match succeeds, enforced at compile time.", tags:["modern-java","oop"], difficulty:"medium"},{id:"java-basics-009", front:"What is the difference between ArrayList and LinkedList?", back:"ArrayList is backed by a resizable array: O(1) random access by index, O(n) insertion/deletion in the middle (shifts elements). LinkedList is a doubly-linked list: O(n) random access, O(1) insertion/deletion at known positions. ArrayList has better cache locality and is faster for most workloads. LinkedList has more memory overhead (node pointers per element). Prefer ArrayList unless you do many insertions/removals at the head or within an iterator.", tags:["collections"], difficulty:"easy"},{id:"java-basics-010", front:"How does a HashMap work internally?", back:"HashMap stores key-value pairs in buckets using hash codes. `key.hashCode()` determines the bucket index (via `hash % array.length`). Each bucket is a linked list (or a balanced tree since Java 8 when a bucket exceeds 8 entries). `equals()` resolves collisions within a bucket. Initial capacity is 16 with a default load factor of 0.75; when `size / capacity > loadFactor`, the map resizes and rehashes all entries. `null` keys and values are allowed.", tags:["collections","maps"], difficulty:"medium"},{id:"java-basics-011", front:"What is the difference between intermediate and terminal Stream operations?", back:"Intermediate operations (e.g., `filter`, `map`, `flatMap`, `sorted`, `distinct`) are lazy: they return a new Stream and do not execute until a terminal operation is invoked. Terminal operations (e.g., `collect`, `forEach`, `reduce`, `count`, `toArray`) trigger the pipeline and produce a result. A Stream can only be consumed once; calling a terminal operation closes it. Attempting to reuse a Stream throws `IllegalStateException`.", tags:["streams"], difficulty:"easy"},{id:"java-basics-012", front:"Are Java Streams lazy? What does that mean in practice?", back:"Yes, Streams are lazy. Intermediate operations build a description of the pipeline but do not process elements. Elements are processed one at a time only when a terminal operation is invoked. For example, `stream.filter(x -> expensive(x)).findFirst()` may only evaluate the predicate on one element. However, operations like `sorted` that require seeing all elements before producing output need to buffer internally. Short-circuiting operations (`findFirst`, `anyMatch`) can terminate early.", tags:["streams"], difficulty:"medium"},{id:"java-basics-013", front:"What is the difference between `synchronized` and `ReentrantLock`?", back:"`synchronized` is a built-in JVM keyword: it acquires a monitor lock on an object, is implicit (block scope), and automatically releases on method exit (even on exceptions). `ReentrantLock` is a class from `java.util.concurrent.locks`: it supports tryLock with timeout, fair locking, interruptible lock acquisition, and multiple condition variables. `ReentrantLock` is more flexible but requires manual unlock in a finally block. Prefer `synchronized` for simplicity; use `ReentrantLock` when you need its advanced features.", tags:["concurrency","locks"], difficulty:"medium"},{id:"java-basics-014", front:"What are virtual threads (Project Loom, Java 21) and how do they differ from platform threads?", back:"Virtual threads are lightweight threads managed by the JVM rather than the OS. They are created via `Thread.ofVirtual().start()` or `Executors.newVirtualThreadPerTaskExecutor()`. Unlike platform threads (1:1 mapping to OS threads), virtual threads are multiplexed onto a small pool of carrier threads. They are cheap to create (millions vs. thousands), and blocking I/O does not waste a carrier thread. They are suited for I/O-bound concurrency (web servers, database calls) not CPU-bound parallelism.", tags:["concurrency","modern-java"], difficulty:"medium"},{id:"java-basics-015", front:"What is type erasure and how does it affect Java generics at runtime?", back:"Type erasure removes generic type parameters at compile time, replacing them with their bounds (or `Object` if unbounded). `List<String>` and `List<Integer>` both become `List` at runtime. This means you cannot create a generic array (`new T[]`), use `instanceof` with type parameters, or have different overloads that differ only in type parameters. The compiler inserts casts at bridge methods to maintain type safety at compile time.", tags:["generics"], difficulty:"medium"},{id:"java-basics-016", front:"What are bounded wildcards in generics (`? extends` vs `? super`)?", back:"`? extends T` (upper bounded wildcard) accepts `T` or any subtype; used for reading (producer). You can `get` a `T` from it but cannot `add` anything except `null`. `? super T` (lower bounded wildcard) accepts `T` or any supertype; used for writing (consumer). You can `add` a `T` to it but reads return `Object`. This is the PECS principle: Producer Extends, Consumer Super. Use `? extends` when you only read from a collection and `? super` when you only write to it.", tags:["generics"], difficulty:"hard"},{id:"java-basics-017", front:"How does generational garbage collection work in the JVM?", back:"The heap is split into young and old generations based on the generational hypothesis (most objects die young). New objects are allocated in Eden (young gen). When Eden fills, a minor GC traces reachable objects, copying survivors to a survivor space. Objects that survive enough minor GCs are promoted to the old generation (tenured). Major/Full GCs collect the old generation and are much slower. This design allows frequent fast minor GCs that reclaim most garbage quickly.", tags:["jvm","gc"], difficulty:"medium"},{id:"java-basics-018", front:"What is the G1 garbage collector and how does it differ from Parallel GC?", back:"G1 (Garbage-First) divides the heap into equal-sized regions and tracks which regions have the most garbage. It evacuates the region with the most garbage first (hence the name), aiming to keep pause times predictable (target set by `-XX:MaxGCPauseMillis`). G1 uses concurrent marking phases to reduce stop-the-world pauses. Parallel GC (the default before Java 9) uses a stop-the-world approach with a single young and old generation. G1 is the default since Java 9 and is better for large heaps with low-latency requirements.", tags:["jvm","gc"], difficulty:"medium"},{id:"java-basics-019", front:"What are Java records (Java 16+) and what do they replace?", back:"A record (`public record Point(double x, double y) {}`) is a transparent carrier for immutable data. The compiler generates the constructor, field accessors (`x()`, not `getX()`), `equals`, `hashCode`, and `toString` automatically. Records replace verbose data classes, Lombok `@Value`, and Kotlin data classes for simple Java use cases. Fields are implicitly final. You can add custom validation via compact constructors, additional methods, and implement interfaces, but you cannot extend another class (records implicitly extend `java.lang.Record`).", tags:["modern-java","records"], difficulty:"easy"},{id:"java-basics-020", front:"What are sealed classes (Java 17+) and when would you use them?", back:"A sealed class restricts which classes can extend it using the `permits` clause: `sealed class Shape permits Circle, Square, Triangle`. All permitted subclasses must be in the same module/package and declared `final`, `non-sealed`, or `sealed`. This enables exhaustive pattern matching in switch expressions: the compiler knows all possible subtypes and can warn if a case is missing. Sealed classes model closed type hierarchies where the full set of variants is known at compile time, like algebraic data types in functional languages.", tags:["modern-java","sealed-classes"], difficulty:"medium"}];
