---
title: Java OOP Practice (Interactive)
tags:
  - java
description: "8 auto-graded practice problems covering Java OOP concepts. Select an answer, su Comprehensive educational content coverage with definitions and practice proble"
---

import PracticeProblem from "@components/PracticeProblem";


## Java — OOP Practice

8 auto-graded practice problems covering Java OOP concepts. Select an answer, submit, and review the explanation.

---

### Classes

<PracticeProblem client:only="solid-js" question='Given `class Animal { void speak() { System.out.println("..."); } }` and `class Dog extends Animal { void speak() { System.out.println("Woof"); } }`, what does `((Animal) new Dog()).speak()` print?' options={['\"Woof\"', '\"...\"', 'Compilation error', 'ClassCastException']} correctAnswer={0} explanation='Java uses dynamic dispatch for instance methods. Even though the reference type is `Animal`, the actual object is a `Dog`. At runtime, Java calls `Dog`&apos;s overridden `speak()` method, printing "Woof". This is polymorphism in action.' difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"What is the output of `System.out.println(new int[]{1,2,3}.length)`?&quot; options={['3', 'Compilation error', 'length() not defined', 'undefined']} correctAnswer={0} explanation=&quot;Arrays in Java have a `length` property (not a method — no parentheses). For `int[]{1,2,3}`, the length is 3. Note: this is different from `String.length()` which is a method."} difficulty="easy"  />

---

### Interfaces

<PracticeProblem client:only="solid-js" question={"In Java 8+, can an interface have a method with a body?&quot; options={[ 'Yes, using the `default` keyword', 'No, interface methods must always be abstract', 'Yes, but only static methods', 'Only in abstract classes', ]} correctAnswer={0} explanation=&quot;Since Java 8, interfaces can have `default` methods with implementations. This allows adding new methods to existing interfaces without breaking implementing classes. Interfaces can also have `static` methods with bodies."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"Which of the following is a valid functional interface?&quot; options={[ 'interface Printer { void print(String s); }', 'interface Printer { void print(String s); void close(); }', 'interface Printer { String toString(); }', 'abstract class Printer { abstract void print(); }', ]} correctAnswer={0} explanation=&quot;A functional interface has exactly one abstract method. `interface Printer { void print(String s); }` has one abstract method and qualifies. The second option has two abstract methods. The third has zero (it inherits `toString` from Object). The fourth is an abstract class, not an interface."} difficulty="medium"  />

---

### Generics

<PracticeProblem client:only="solid-js" question={"What does `<T extends Number>` mean in a generic class declaration?&quot; options={[ 'T must be Number or a subclass of Number', 'T must be exactly Number', 'T can be any type that Number extends', 'T must implement the Number interface', ]} correctAnswer={0} explanation=&quot;The `extends` keyword in generics specifies an upper bound. `<T extends Number>` means T can be `Number` or any subclass (`Integer`, `Double`, etc.). This allows the generic code to call methods defined in `Number` like `intValue()` or `doubleValue()`."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"What is the difference between `List<?>` and `List<Object>`?&quot; options={[ 'List<?> can hold any type; List<Object> can only hold Object references', 'They are identical', 'List<?> cannot hold any elements', 'List<Object> is more permissive than List<?>', ]} correctAnswer={0} explanation=&quot;`List<?>` is a wildcard type that can reference a list of any specific type but you cannot add elements (except null). `List<Object>` specifically means a list that can hold any Object. You can add any Object to `List<Object>` but not to `List<?>`. They are not the same."} difficulty="hard"  />

---

### Collections

<PracticeProblem client:only="solid-js" question={"What does `List<Integer> nums = Arrays.asList(1, 2, 3, 4, 5); nums.stream().filter(n -> n % 2 == 0).map(n -> n * n).collect(Collectors.toList())` return?&quot; options={['[4, 16]', '[2, 4]', '[1, 9, 25]', '[4]']} correctAnswer={0} explanation=&quot;The stream filters for even numbers (2, 4), then squares them (4, 16), then collects to a list: `[4, 16]`. `filter` keeps elements matching the predicate, `map` transforms each element, and `collect` gathers results into a list."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question='What does `Optional.ofNullable(null).orElse("default")` return?' options={['\"default\"', 'null', 'Optional[default]', 'NoSuchElementException']} correctAnswer={0} explanation='`Optional.ofNullable(null)` creates an empty Optional. `orElse("default")` returns the provided default value when the Optional is empty. So the result is the string "default".' difficulty="easy"  />

---

### Interfaces and Abstract Classes

<PracticeProblem client:only="solid-js" question={"Given `interface Describable { default String describe() { return \"default\"; } }` and `class Widget implements Describable { }`, what does `new Widget().describe()` return?&quot; options={['\"default\"', 'Compilation error', 'null', 'Empty string']} correctAnswer={0} explanation=&quot;Since Widget does not override describe(), it inherits the default method from the Describable interface. The default method returns \"default\"."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"Can an abstract class have a constructor?&quot; options={[ 'Yes, to initialize fields or enforce setup in subclasses', 'No, abstract classes cannot have constructors', 'Only if it has no abstract methods', 'Only if declared public', ]} correctAnswer={0} explanation=&quot;Abstract classes can have constructors. They are called via super() from subclass constructors to initialize inherited fields or perform common setup. Abstract classes cannot be instantiated directly, but their constructors are essential for subclass initialization."} difficulty="medium"  />

---

### Enums

<PracticeProblem client:only="solid-js" question={"What is the key difference between a basic enum and an enum with fields and methods in Java?&quot; options={[ 'Basic enums are simple constants; enums with fields can carry data and override methods per constant', 'There is no difference', 'Enums with fields cannot be used in switch statements', 'Basic enums support generics', ]} correctAnswer={0} explanation=&quot;Basic enums like `enum Color { RED, GREEN, BLUE }` are simple constants. Enums with fields like `enum Planet { MERCURY(3.303e+23, 2.4397e6) }` can store per-constant data and provide methods. Both can override methods, but enums with fields carry additional state."} difficulty="medium"  />

---

### Records and Sealed Classes

<PracticeProblem client:only="solid-js" question={"What does a Java record automatically generate?&quot; options={[ 'Constructor, accessor methods, equals, hashCode, and toString', 'Only a constructor and getters', 'A mutable data class with setters', 'Nothing -- records are empty by default', ]} correctAnswer={0} explanation=&quot;A Java record like `record Point(int x, int y)` automatically generates: a canonical constructor, accessor methods x() and y(), equals(), hashCode(), and toString(). Fields are implicitly final."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"Given `sealed interface Shape permits Circle, Square, Triangle {}`, what must Circle, Square, and Triangle declare?&quot; options={[ 'final, non-sealed, or sealed', 'Only final', 'Only abstract', 'No special declaration needed', ]} correctAnswer={0} explanation=&quot;Permitted subclasses of a sealed type must be declared final (cannot be extended), non-sealed (can be extended by anyone), or sealed (further restricts to its own permits). This ensures the compiler knows the complete type hierarchy for exhaustive checking."} difficulty="medium"  />

---

### Enums and Records

<PracticeProblem client:only="solid-js" question={"What does `enum Planet { MERCURY(3.303e+23, 2.4397e6), VENUS(4.869e+24, 6.0518e6); final double mass; final double radius; Planet(double mass, double radius) { this.mass = mass; this.radius = radius; } }` allow you to do?&quot; options={[ 'Access Planet.MERCURY.mass directly without getters', 'Iterate planets with Planet.values()', 'Both A and B', 'Neither A nor B', ]} correctAnswer={2} explanation=&quot;Enums with fields let you access per-constant data directly (Planet.MERCURY.mass). Planet.values() returns an array of all enum constants. Enhanced enums can also have methods and implement interfaces."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"What is the key difference between a record and a regular class with final fields?&quot; options={[ 'Records auto-generate equals/hashCode/toString; regular classes do not', 'Records can have instance methods; regular classes cannot', 'Records are mutable; regular classes are immutable', 'There is no difference', ]} correctAnswer={0} explanation=&quot;Records generate equals(), hashCode(), toString(), and accessor methods automatically. A regular class with final fields requires manually implementing these methods. Records also provide a canonical constructor and can only have data fields (no additional instance fields)."} difficulty="easy"  />

---

### Inheritance and Polymorphism

<PracticeProblem client:only="solid-js" question={"What does `@Override` annotation do in Java?&quot; options={[ 'Signals the compiler to verify the method actually overrides a superclass method', 'Changes the method signature', 'Makes the method private', 'Enables runtime polymorphism', ]} correctAnswer={0} explanation=&quot;@Override tells the compiler to verify that the method actually overrides a method from a superclass or interface. If there is no matching method (e.g., typo in name or parameters), compilation fails. It is not required but catches errors early."} difficulty="easy"  />
