---
title: Java OOP Practice (Interactive)
tags:
  - java
description: "8 auto-graded practice problems covering Java OOP concepts. Select an answer, su Comprehensive educational content coverage with definitions and practice proble''
---

import PracticeProblem from "@components/PracticeProblem";


## Java — OOP Practice

8 auto-graded practice problems covering Java OOP concepts. Select an answer, submit, and review the
explanation.

---

### Classes

<PracticeProblem question='Given `class Animal { void speak() { System.out.println("..."); } }` and `class Dog extends Animal { void speak() { System.out.println("Woof"); } }`, what does `((Animal) new Dog()).speak()` print?' options={['\"Woof\"', '\"...\"', 'Compilation error', 'ClassCastException']} correctAnswer={0} explanation='Java uses dynamic dispatch for instance methods. Even though the reference type is `Animal`, the actual object is a `Dog`. At runtime, Java calls `Dog`&apos;s overridden `speak()` method, printing "Woof". This is polymorphism in action.' difficulty="easy" />

<PracticeProblem question="What is the output of `System.out.println(new int[]{1,2,3}.length)`?" options={['3', 'Compilation error', 'length() not defined', 'undefined']} correctAnswer={0} explanation="Arrays in Java have a `length` property (not a method — no parentheses). For `int[]{1,2,3}`, the length is 3. Note: this is different from `String.length()` which is a method." difficulty="easy" />

---

### Interfaces

<PracticeProblem question="In Java 8+, can an interface have a method with a body?" options={[ 'Yes, using the `default` keyword', 'No, interface methods must always be abstract', 'Yes, but only static methods', 'Only in abstract classes', ]} correctAnswer={0} explanation="Since Java 8, interfaces can have `default` methods with implementations. This allows adding new methods to existing interfaces without breaking implementing classes. Interfaces can also have `static` methods with bodies." difficulty="easy" />

<PracticeProblem question="Which of the following is a valid functional interface?" options={[ 'interface Printer { void print(String s); }', 'interface Printer { void print(String s); void close(); }', 'interface Printer { String toString(); }', 'abstract class Printer { abstract void print(); }', ]} correctAnswer={0} explanation="A functional interface has exactly one abstract method. `interface Printer { void print(String s); }` has one abstract method and qualifies. The second option has two abstract methods. The third has zero (it inherits `toString` from Object). The fourth is an abstract class, not an interface." difficulty="medium" />

---

### Generics

<PracticeProblem question="What does `<T extends Number>` mean in a generic class declaration?" options={[ 'T must be Number or a subclass of Number', 'T must be exactly Number', 'T can be any type that Number extends', 'T must implement the Number interface', ]} correctAnswer={0} explanation="The `extends` keyword in generics specifies an upper bound. `<T extends Number>` means T can be `Number` or any subclass (`Integer`, `Double`, etc.). This allows the generic code to call methods defined in `Number` like `intValue()` or `doubleValue()`." difficulty="medium" />

<PracticeProblem question="What is the difference between `List<?>` and `List<Object>`?" options={[ 'List<?> can hold any type; List<Object> can only hold Object references', 'They are identical', 'List<?> cannot hold any elements', 'List<Object> is more permissive than List<?>', ]} correctAnswer={0} explanation="`List<?>` is a wildcard type that can reference a list of any specific type but you cannot add elements (except null). `List<Object>` specifically means a list that can hold any Object. You can add any Object to `List<Object>` but not to `List<?>`. They are not the same." difficulty="hard" />

---

### Collections

<PracticeProblem question="What does `List<Integer> nums = Arrays.asList(1, 2, 3, 4, 5); nums.stream().filter(n -> n % 2 == 0).map(n -> n * n).collect(Collectors.toList())` return?" options={['[4, 16]', '[2, 4]', '[1, 9, 25]', '[4]']} correctAnswer={0} explanation="The stream filters for even numbers (2, 4), then squares them (4, 16), then collects to a list: `[4, 16]`. `filter` keeps elements matching the predicate, `map` transforms each element, and `collect` gathers results into a list." difficulty="medium" />

<PracticeProblem question='What does `Optional.ofNullable(null).orElse("default")` return?' options={['\"default\"', 'null', 'Optional[default]', 'NoSuchElementException']} correctAnswer={0} explanation='`Optional.ofNullable(null)` creates an empty Optional. `orElse("default")` returns the provided default value when the Optional is empty. So the result is the string "default".' difficulty="easy" />
