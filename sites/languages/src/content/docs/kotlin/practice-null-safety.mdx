---

date: 2026-07-23T22:34:31+01:00
title: Kotlin Null Safety Practice (Interactive)
tags:
  - kotlin
description: "8 auto-graded practice problems covering Kotlin null safety, scope functions, sealed classes, and coroutines. Select an answer, submit, and review the"
---


import PracticeProblem from "@components/PracticeProblem";

## Intuition

**Learning through practice:** Practice problems are like training drills — they help you apply knowledge and identify areas that need more study.

**Why it matters:** Regular practice builds confidence and reveals patterns in how concepts are tested. Each problem reinforces key programming concepts.

**The key insight:** Making mistakes during practice is valuable — each error points to a concept that needs clarification.

## Kotlin — Null Safety Practice

13 auto-graded practice problems covering Kotlin null safety, scope functions, sealed classes, and coroutines. Select an answer, submit, and review the explanation.

---

### Null Safety

<PracticeProblem client:only="solid-js" question={"What does `val length: Int = name?.length` compile to when `name` is of type `String?`?&quot; options={[ 'Compilation error: Type mismatch (Int? vs Int)', '0', 'null', 'It compiles successfully', ]} correctAnswer={0} explanation='The safe call operator `?.` returns `null` if the receiver is null, so `name?.length` has type `Int?`. Assigning `Int?` to `Int` causes a compilation error: &quot;Type mismatch: inferred type is Int? but Int was expected.&quot; You need `val length: Int? = name?.length` or use a default.' difficulty=&quot;easy"}  />

<PracticeProblem client:only="solid-js" question={"What does `val len = name?.length ?: 0` do when `name` is null?&quot; options={['Throws NullPointerException','Returns -1','Compilation error','Returns 0']} correctAnswer={3} explanation=&quot;The elvis operator `?:` provides a default value when the left side is null. If `name` is null, `name?.length` is null, so the elvis operator returns 0. This is a common Kotlin idiom for handling nullable types."} difficulty="easy"  />

---

### Scope Functions

<PracticeProblem client:only="solid-js" question='What does `val x = "hello".let { it.uppercase() }` return?' options={['\'hello\'','Unit','Compilation error','\'HELLO\'']} correctAnswer={3} explanation='`let` executes the lambda with the receiver as `it`, and returns the lambda&apos;s result. `"hello".let { it.uppercase() }` calls `uppercase()` on "hello", returning "HELLO".' difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"What is the difference between `apply` and `also`?&quot; options={[ 'apply uses `this` as context; also uses `it` as argument — both return the receiver object', 'apply returns the lambda result; also returns the receiver', 'There is no difference', 'apply is for nullable types; also is for non-null', ]} correctAnswer={0} explanation=&quot;Both `apply` and `also` return the receiver object (not the lambda result). The difference is: `apply` uses `this` as the context object inside the block (useful for configuring objects), while `also` uses `it` as the lambda argument (useful for side effects like logging)."} difficulty="medium"  />

---

### Sealed Classes

<PracticeProblem client:only="solid-js" question={"What advantage does a `sealed class` provide over an `open class` for modelling a restricted hierarchy?&quot; options={[ 'The compiler ensures all subclasses are known and handles exhaustiveness in `when` expressions', 'Sealed classes are faster at runtime', 'Sealed classes cannot have properties', 'Sealed classes can only have one subclass', ]} correctAnswer={0} explanation=&quot;Sealed classes restrict inheritance to a known set of subclasses defined in the same file/package. This enables the compiler to verify that all cases are handled in `when` expressions (exhaustive checking), making the code safer. In Kotlin 1.5+, subclasses can be in the same package."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Given `sealed class Result { data class Success(val data: String) : Result(); data class Error(val message: String) : Result() }`, which `when` expression is exhaustive?&quot; options={[ 'when (r) { is Result.Success -> ...; is Result.Error -> ... }', 'when (r) { is Result.Success -> ... }', 'when (r) { }', 'when (r) { else -> ... }', ]} correctAnswer={0} explanation=&quot;Since `Result` is a sealed class with exactly two subclasses (`Success` and `Error`), a `when` expression covering both `is Result.Success` and `is Result.Error` branches is exhaustive — no `else` branch needed. The compiler verifies all sealed subclasses are handled."} difficulty="easy"  />

---

### Coroutines

<PracticeProblem client:only="solid-js" question={"What is the key difference between `launch` and `async` in Kotlin coroutines?&quot; options={[ 'launch returns a Job (fire-and-forget); async returns a Deferred<T> that produces a result', 'launch is blocking; async is non-blocking', 'launch runs on IO dispatcher; async runs on Default', 'There is no difference', ]} correctAnswer={0} explanation=&quot;`launch` starts a coroutine that returns a `Job` — it\'s fire-and-forget and doesn\'t return a value. `async` starts a coroutine that returns a `Deferred<T>`, which you can await with `.await()` to get the computed result. Use `async` when you need a result."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"What does the `suspend` keyword on a function signify?&quot; options={[ 'The function can be paused and resumed, and can only be called from a coroutine or another suspend function', 'The function runs on a separate thread', 'The function blocks the calling thread', 'The function is marked for garbage collection', ]} correctAnswer={0} explanation=&quot;A `suspend` function can suspend execution at suspension points without blocking a thread. It can only be called from within a coroutine or another suspend function. The compiler transforms suspend functions into state machines that handle suspension and resumption efficiently."} difficulty="easy"  />

---

### Safe Calls and Let

<PracticeProblem client:only="solid-js" question={"What does `user?.address?.let { it.city }` return when `user` is non-null but `address` is null?&quot; options={[ 'null', 'Compilation error', 'Unit', 'Throws NullPointerException', ]} correctAnswer={0} explanation=&quot;The safe call `?.` short-circuits on null. Since `user` is non-null but `user.address` is null, the safe call on `address` returns null. The `let` block is never executed, and the entire expression evaluates to null."} difficulty="medium"  />

---

### Also and Require

<PracticeProblem client:only="solid-js" question={"What is the difference between `also` and `apply` in Kotlin scope functions?&quot; options={[ 'both return the receiver; also uses `it` as argument, apply uses `this` as context', 'apply returns the lambda result; also returns the receiver', 'also is for nullable types; apply is for non-null', 'There is no difference', ]} correctAnswer={0} explanation=&quot;Both `also` and `apply` return the receiver object. `also` provides the receiver as `it` (explicit argument), making it ideal for side effects like logging. `apply` provides the receiver as `this` (implicit context), making it ideal for configuration."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"What does `require(age >= 0) { \"Age must be non-negative\" }` do when age is -5?&quot; options={[ 'Throws IllegalArgumentException with the message', 'Returns false', 'Does nothing', 'Throws IllegalStateException', ]} correctAnswer={0} explanation=&quot;`require` checks a precondition and throws IllegalArgumentException if the condition is false. The lambda provides the error message. `require` is for validating function arguments; `check` is for validating internal state (throws IllegalStateException)."} difficulty="easy"  />

---

### Check and Assertion

<PracticeProblem client:only="solid-js" question={"When should you use `check` vs `require` in Kotlin?&quot; options={[ 'check for internal state invariants; require for function argument validation', 'check for function arguments; require for internal state', 'They are interchangeable', 'check is for null checks; require is for type checks', ]} correctAnswer={0} explanation=&quot;`require` validates conditions on function parameters and throws IllegalArgumentException. `check` validates internal invariants and throws IllegalStateException. This distinction helps callers understand whether a failure is due to bad input (require) or a bug (check)."} difficulty="medium"  />

---

### Scope Functions

<PracticeProblem client:only="solid-js" question={"What does `val result = listOf(1,2,3).let { \"Size: ${it.size}\" }` return?&quot; options={[ '\"Size: 3\"', '[1, 2, 3]', '3', 'Compilation error', ]} correctAnswer={0} explanation=&quot;`let` executes the lambda with the list as `it` and returns the lambda result. The lambda constructs a string using the list's size property. The result is the string \"Size: 3\"."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"What is the difference between `also` and `apply` in Kotlin?&quot; options={[ 'both return the receiver; also uses `it` as argument, apply uses `this` as context', 'apply returns the lambda result; also returns the receiver', 'also is for nullable types; apply is for non-null', 'There is no difference', ]} correctAnswer={0} explanation=&quot;Both `also` and `apply` return the receiver object. `also` provides the receiver as `it` (explicit argument), making it ideal for side effects like logging. `apply` provides the receiver as `this` (implicit context), making it ideal for configuration."} difficulty="medium"  />

---

### Coroutines and Flows

<PracticeProblem client:only="solid-js" question={"What does `flow { emit(1); emit(2); emit(3) }.collect { println(it) }` do?&quot; options={[ 'Prints 1, 2, 3 each on a new line', 'Prints [1, 2, 3]', 'Only prints 3', 'Compilation error', ]} correctAnswer={0} explanation=&quot;A flow is a cold asynchronous stream. The block only executes when collected. emit sends each value to the collector. collect is the terminal operator that processes each emitted value. The values are printed sequentially."} difficulty="medium"  />

## Cross-References

- **[Null Safety Deep Dive](../../../../../kotlin/src/content/docs/advanced/null-safety-deep-dive):** Safe-call operators and nullable flow handling.
- **[Types and Variables](../../../../../kotlin/src/content/docs/basics/types-and-variables):** Nullable type fundamentals behind null safety patterns.
- **[Kotlin Practice](../../../../../kotlin/src/content/docs/practice-kotlin-basics):** Core Kotlin concepts including coroutines and collections.

## Common Mistakes

**Confusing `require` with `check`:** `require` validates function arguments (throws `IllegalArgumentException`); `check` validates internal state (throws `IllegalStateException`). Using the wrong one misleads callers about whether a failure is bad input or a bug.

**Using `!!` (not-null assertion) unnecessarily:** `!!` converts any nullable type to non-nullable and throws `NullPointerException` if null. Prefer safe calls `?.`, elvis `?:`, or `let` for safe handling.

**Forgetting that `lazy` is thread-safe by default:** `by lazy` uses synchronized double-checked locking. For single-threaded contexts, pass `LazyThreadSafetyMode.NONE` to avoid unnecessary synchronization overhead.
