---
title: Ruby Practice (Basics)
description: ""@components/PracticeProblem";


## Ruby -- Interactive Practice

10 auto-graded practice problems covering core Ruby concepts. Select an answer, submit, and review
the explanation.

---

### Types and Variables

<PracticeProblem question="What is the type of the expression `42.class`?" options={['Integer', 'Fixnum', 'Numeric', 'Object']} correctAnswer={0} explanation="42.class returns Integer. Since Ruby 2.4, Fixnum and Bignum were unified into Integer with arbitrary precision. There is no Fixnum or Bignum class in modern Ruby." difficulty="easy" />

<PracticeProblem question="Which of the following values is falsy in Ruby?" options={['nil', '0', '"" (empty string)', '[] (empty array)']} correctAnswer={0} explanation="Only nil and false are falsy in Ruby. The integer 0, empty strings, empty arrays, and empty hashes are all truthy. This differs from languages like Python where 0 and empty collections are falsy." difficulty="easy" />

<PracticeProblem question='What does `"hello".equal?("hello")` return?' options={['false', 'true', 'nil', 'TypeError']} correctAnswer={0} explanation="equal? checks object identity (same object in memory). Two string literals with the same content are different objects, so equal? returns false. Use == for content comparison, eql? for content and type comparison." difficulty="medium" />

---

### Blocks and Iterators

<PracticeProblem question="What does `[1, 2, 3, 4, 5].select { |n| n.odd? }.map { |n| n ** 2 }` return?" options={['[1, 9, 25]', '[1, 4, 9, 16, 25]', '[9, 25]', '[1, 3, 5]']} correctAnswer={0} explanation="select filters to keep only odd numbers: [1, 3, 5]. Then map squares each: [1, 9, 25]. Higher-order methods chain left-to-right." difficulty="easy" />

<PracticeProblem question="What is the output of `5.times { |i| puts i }` and what range of values does `i` take?" options={['Prints 0 through 4', 'Prints 1 through 5', 'Prints 0 through 5', 'Prints 1 through 4']} correctAnswer={0} explanation="Integer#times iterates from 0 to n-1. So 5.times yields 0, 1, 2, 3, 4. The upper bound is exclusive, not inclusive. Use 1.upto(5) for 1 through 5." difficulty="easy" />

---

### OOP and Modules

<PracticeProblem question="What does `super` do when called without arguments in an overridden method?" options={[ 'Passes the same arguments that were passed to the current method', 'Calls the parent method with no arguments', 'Raises a NoMethodError', 'Returns nil', ]} correctAnswer={0} explanation="super without arguments passes all arguments from the current method to the parent method, including block arguments. To call the parent with no arguments, use super(). This is a common Ruby gotcha." difficulty="medium" />

<PracticeProblem question="What is the method lookup order for a class Dog that includes modules M1 then M2, and inherits from Animal?" options={[ 'Dog, M2, M1, Animal, Object, Kernel, BasicObject', 'Dog, M1, M2, Animal, Object, Kernel, BasicObject', 'Dog, Animal, M2, M1, Object, Kernel, BasicObject', 'Dog, M1, M2, Animal, Kernel, Object, BasicObject', ]} correctAnswer={0} explanation="Module lookup follows reverse inclusion order. Since M2 was included after M1, it appears before M1 in the ancestors chain. prepend would place the module before the class itself." difficulty="medium" />

---

### Control Flow and Exceptions

<PracticeProblem question="In `begin; risky; rescue => e; raise; end`, what does `raise` without arguments in the rescue block do?" options={[ 'Re-raises the caught exception preserving the original backtrace', 'Raises a new RuntimeError', 'Raises ArgumentError', 'Does nothing (no-op)', ]} correctAnswer={0} explanation="raise without arguments inside a rescue block re-raises the current exception. The original exception object and its backtrace are preserved. This is the idiomatic way to re-raise after logging or partial handling." difficulty="medium" />

<PracticeProblem question="What does `[1, 2, 3].each { |n| return n if n == 2 }` return from the enclosing method?" options={[ '2 (returns from the method, not just the block)', '1 (returns nil, since each returns the array)', 'NoMethodError', 'break is required instead of return', ]} correctAnswer={0} explanation="return inside a block returns from the enclosing method, not just from the block. So if this block is inside a method, that method returns 2 immediately. Use next to skip iterations or break to exit the block only." difficulty="hard" />

---

### Metaprogramming

<PracticeProblem question="Which method should you always define alongside method_missing to maintain respond_to? correctness?" options={['respond_to_missing?', 'respond_to?', 'method_defined?', 'has_method?']} correctAnswer={0} explanation="respond_to_missing? must be defined alongside method_missing. Without it, respond_to? returns false for dynamically handled methods, breaking polymorphism. The second parameter include_private controls whether private methods are included." difficulty="hard" />
