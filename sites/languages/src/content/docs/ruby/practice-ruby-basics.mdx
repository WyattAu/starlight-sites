---

title: Ruby Practice (Basics)
description: "10 auto-graded practice problems covering core Ruby concepts. Select an answer,  Comprehensive educational content coverage with definitions and practice proble"
date: 2026-06-04
tags:
  - Ruby
categories:
  - Ruby
---


import PracticeProblem from "@components/PracticeProblem";


## Intuition

**Learning through practice:** Practice problems are like training drills — they help you apply knowledge and identify areas that need more study.

**Why it matters:** Regular practice builds confidence and reveals patterns in how concepts are tested. Each problem reinforces key programming concepts.

**The key insight:** Making mistakes during practice is valuable — each error points to a concept that needs clarification.

## Ruby -- Interactive Practice

10 auto-graded practice problems covering core Ruby concepts. Select an answer, submit, and review
the explanation.

---


### Types and Variables

<PracticeProblem client:only="solid-js" question={"What is the type of the expression `42.class`?&quot; options={['Integer', 'Fixnum', 'Numeric', 'Object']} correctAnswer={0} explanation=&quot;42.class returns Integer. Since Ruby 2.4, Fixnum and Bignum were unified into Integer with arbitrary precision. There is no Fixnum or Bignum class in modern Ruby."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"Which of the following values is falsy in Ruby?&quot; options={['nil', '0', '&quot;&quot; (empty string)', '[] (empty array)']} correctAnswer={0} explanation=&quot;Only nil and false are falsy in Ruby. The integer 0, empty strings, empty arrays, and empty hashes are all truthy. This differs from languages like Python where 0 and empty collections are falsy."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question='What does `"hello".equal?("hello")` return?' options={['false', 'true', 'nil', 'TypeError']} correctAnswer={0} explanation="equal? checks object identity (same object in memory). Two string literals with the same content are different objects, so equal? returns false. Use == for content comparison, eql? for content and type comparison." difficulty="medium"  />

---


### Blocks and Iterators

<PracticeProblem client:only="solid-js" question={"What does `[1, 2, 3, 4, 5].select { |n| n.odd? }.map { |n| n ** 2 }` return?&quot; options={['[1, 9, 25]', '[1, 4, 9, 16, 25]', '[9, 25]', '[1, 3, 5]']} correctAnswer={0} explanation=&quot;select filters to keep only odd numbers: [1, 3, 5]. Then map squares each: [1, 9, 25]. Higher-order methods chain left-to-right."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"What is the output of `5.times { |i| puts i }` and what range of values does `i` take?&quot; options={['Prints 0 through 4', 'Prints 1 through 5', 'Prints 0 through 5', 'Prints 1 through 4']} correctAnswer={0} explanation=&quot;Integer#times iterates from 0 to n-1. So 5.times yields 0, 1, 2, 3, 4. The upper bound is exclusive, not inclusive. Use 1.upto(5) for 1 through 5."} difficulty="easy"  />

---


### OOP and Modules

<PracticeProblem client:only="solid-js" question={"What does `super` do when called without arguments in an overridden method?&quot; options={[ 'Passes the same arguments that were passed to the current method', 'Calls the parent method with no arguments', 'Raises a NoMethodError', 'Returns nil', ]} correctAnswer={0} explanation=&quot;super without arguments passes all arguments from the current method to the parent method, including block arguments. To call the parent with no arguments, use super(). This is a common Ruby gotcha."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"What is the method lookup order for a class Dog that includes modules M1 then M2, and inherits from Animal?&quot; options={[ 'Dog, M2, M1, Animal, Object, Kernel, BasicObject', 'Dog, M1, M2, Animal, Object, Kernel, BasicObject', 'Dog, Animal, M2, M1, Object, Kernel, BasicObject', 'Dog, M1, M2, Animal, Kernel, Object, BasicObject', ]} correctAnswer={0} explanation=&quot;Module lookup follows reverse inclusion order. Since M2 was included after M1, it appears before M1 in the ancestors chain. prepend would place the module before the class itself."} difficulty="medium"  />

---


### Control Flow and Exceptions

<PracticeProblem client:only="solid-js" question={"In `begin; risky; rescue => e; raise; end`, what does `raise` without arguments in the rescue block do?&quot; options={[ 'Re-raises the caught exception preserving the original backtrace', 'Raises a new RuntimeError', 'Raises ArgumentError', 'Does nothing (no-op)', ]} correctAnswer={0} explanation=&quot;raise without arguments inside a rescue block re-raises the current exception. The original exception object and its backtrace are preserved. This is the idiomatic way to re-raise after logging or partial handling."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"What does `[1, 2, 3].each { |n| return n if n == 2 }` return from the enclosing method?&quot; options={[ '2 (returns from the method, not just the block)', '1 (returns nil, since each returns the array)', 'NoMethodError', 'break is required instead of return', ]} correctAnswer={0} explanation=&quot;return inside a block returns from the enclosing method, not just from the block. So if this block is inside a method, that method returns 2 immediately. Use next to skip iterations or break to exit the block only."} difficulty="hard"  />

---


### Metaprogramming

<PracticeProblem client:only="solid-js" question={"Which method should you always define alongside method_missing to maintain respond_to? correctness?&quot; options={['respond_to_missing?', 'respond_to?', 'method_defined?', 'has_method?']} correctAnswer={0} explanation=&quot;respond_to_missing? must be defined alongside method_missing. Without it, respond_to? returns false for dynamically handled methods, breaking polymorphism. The second parameter include_private controls whether private methods are included."} difficulty="hard"  />

## Cross-References

- **[Ruby Flashcards](flashcards-ruby-basics):** Interactive flashcards covering the same core Ruby concepts.
- **[Elixir Basics](../../../../../elixir/src/content/docs/01-basics/1_basics-and-pattern-matching):** Functional programming patterns shared between Ruby and Elixir.

## Common Mistakes

**Using `equal?` when you mean `==`:** `equal?` checks object identity, not content. Two strings with identical content are different objects. Use `==` for value comparison.

**Confusing `super` with `super()`:** `super` passes all arguments to the parent method; `super()` passes none. Using the wrong one causes unexpected argument forwarding or missing argument errors.

**Assuming `return` exits the block:** `return` inside a block exits the enclosing method, not just the block. Use `next` to skip an iteration or `break` to exit the block early.
