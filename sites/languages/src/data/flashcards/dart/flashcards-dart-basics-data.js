export const flashcardData = [
  {
    id: 'dart-null-safety-001',
    front: 'What is Sound Null Safety in Dart and what guarantee does it provide?',
    back: 'Sound null safety is a compile-time guarantee that null values cannot appear where a non-nullable type is expected. The Dart compiler performs static analysis and flow analysis to ensure that nullable types (T?) are explicitly handled before use. If the compiler cannot prove a variable is non-null, it rejects the code. This eliminates null-reference exceptions at runtime without requiring runtime checks.',
    tags: ['dart', 'null-safety', 'types'],
    difficulty: 'medium',
  },
  {
    id: 'dart-late-keyword-002',
    front: 'What does the `late` keyword do and when should you use it?',
    back: '`late` defers initialization of a non-nullable variable to a later point. Use it when: 1) The variable is initialized in the constructor body (not initializer list), 2) It is set by a method before being read, 3) It holds a lazily computed value. The compiler trusts you that it will be non-null when accessed -- if it is not, a runtime LateInitializationError occurs.',
    tags: ['dart', 'null-safety', 'variables'],
    difficulty: 'easy',
  },
  {
    id: 'dart-required-named-params-003',
    front: 'What is the `required` keyword used for in Dart?',
    back: '`required` marks named parameters as mandatory when calling a function or constructor. Example: `void configure({required String host, int port = 8080}) {}`. Callers must provide `host` but `port` has a default. It was introduced with null safety to replace `@required` from the meta package.',
    tags: ['dart', 'functions', 'syntax'],
    difficulty: 'easy',
  },
  {
    id: 'dart-mixins-004',
    front: 'What is a mixin in Dart and how does it differ from inheritance?',
    back: 'A mixin is a class-like construct that provides reusable method implementations to other classes through the `with` clause. Unlike inheritance (single `extends`), a class can use multiple mixins. Mixins cannot be instantiated directly and do not have their own constructors. They enable a form of multiple inheritance for method reuse without the diamond problem. Example: `class Bird extends Animal with Flyer, Walker {}`.',
    tags: ['dart', 'oop', 'mixins'],
    difficulty: 'medium',
  },
  {
    id: 'dart-extension-methods-005',
    front: 'What are extension methods in Dart?',
    back: 'Extensions add new methods to existing types (including classes you do not own) without subclassing. Example: `extension StringX on String { int get asInt => int.parse(this); }`. They are resolved at compile time and do not modify the original class. Extensions can also add getters, operators, and static members. They are ideal for utility methods.',
    tags: ['dart', 'oop', 'extensions'],
    difficulty: 'medium',
  },
  {
    id: 'dart-factory-constructors-006',
    front:
      'What is a factory constructor in Dart and how does it differ from a generative constructor?',
    back: 'A factory constructor (keyword `factory`) does not always create a new instance. It can return: 1) An existing cached instance (singleton pattern), 2) A subtype instance, 3) An instance from a different source. Unlike generative constructors, factory constructors cannot use initializer lists and must explicitly return an instance with `return`. Example: `factory Logger(String name) => _cache.putIfAbsent(name, () => Logger._internal(name));`.',
    tags: ['dart', 'oop', 'constructors'],
    difficulty: 'medium',
  },
  {
    id: 'dart-future-async-await-007',
    front: 'How do `Future`, `async`, and `await` work together in Dart?',
    back: 'A `Future<T>` represents a value or error that will be available later. Marking a function `async` makes it return a Future automatically. `await` pauses the function until the Future completes, yielding the result or throwing the error. `async/await` is syntactic sugar over the Future API (`.then()`, `.catchError()`, `.whenComplete()`). They enable writing asynchronous code that reads like synchronous code.',
    tags: ['dart', 'async', 'futures'],
    difficulty: 'easy',
  },
  {
    id: 'dart-stream-008',
    front: 'What is a Stream in Dart and how does it differ from a Future?',
    back: 'A Stream delivers a sequence of values or errors over time (like an async Iterable), while a Future delivers a single value or error. Streams can be single-subscription (one listener, events consumed once) or broadcast (multiple listeners, each gets events from subscription point). Listen with `stream.listen()`, transform with `stream.map()`, and cancel with `subscription.cancel()`.',
    tags: ['dart', 'async', 'streams'],
    difficulty: 'medium',
  },
  {
    id: 'dart-completer-009',
    front: 'What is a `Completer<T>` and when would you use one?',
    back: 'A Completer wraps a Future and gives you manual control over its completion. Call `completer.complete(value)` to resolve it or `completer.completeError(error)` to reject it. Use it when you need to bridge callback-based APIs with Future-based code, or when the Future must be completed from a different scope. Access the underlying Future via `completer.future`.',
    tags: ['dart', 'async', 'completer'],
    difficulty: 'medium',
  },
  {
    id: 'dart-sealed-classes-010',
    front: 'What are sealed classes in Dart 3 and why are they useful?',
    back: 'Sealed classes restrict which types can extend or implement them to a known, finite set defined in the same library. The compiler enforces exhaustiveness checking in switch expressions -- if you handle all subtypes, no `default` case is needed. Example: `sealed class Shape { } class Circle extends Shape { } class Square extends Shape { }`. Switching on `Shape` without a default is exhaustive and safe.',
    tags: ['dart', 'dart3', 'sealed-classes'],
    difficulty: 'medium',
  },
  {
    id: 'dart-pattern-matching-011',
    front: 'What pattern matching features did Dart 3 introduce?',
    back: 'Dart 3 added: 1) Object and type test patterns (`if (obj is String s)`), 2) Logical patterns (`||`, `&&`, `!`), 3) Relational patterns (`case > 0`), 4) Cast patterns (`case num as int i`), 5) List and map patterns (`case [a, b, ...rest]`), 6) Variable binding in `if-case` and `switch-case`, 7) Null-check and null-assert patterns. These make data extraction from complex structures concise and safe.',
    tags: ['dart', 'dart3', 'pattern-matching'],
    difficulty: 'medium',
  },
  {
    id: 'dart-records-012',
    front: 'What are records in Dart 3 and how do they work?',
    back: "Records are anonymous, immutable, aggregate data types. Syntax: `var record = ('Alice', age: 30)` creates a positional record with a named field. Access with `record.$1` (positional) or `record.age` (named). Records have structural equality -- two records with the same fields are equal. They support destructuring: `var (name, age: a) = record;`. Records are value types, perfect for returning multiple values from functions.",
    tags: ['dart', 'dart3', 'records'],
    difficulty: 'easy',
  },
  {
    id: 'dart-class-modifiers-013',
    front: 'What class modifiers did Dart 3 introduce?',
    back: 'Dart 3 added: `sealed` (restricts subtyping to same library), `final` (prevents being extended or implemented outside own library), `interface` (class can only be implemented, not extended), `base` (ensures all subtypes also use `base`, enabling private field access guarantees), and `mixin class` (usable as both a class and a mixin). These replace informal conventions with compiler-enforced restrictions.',
    tags: ['dart', 'dart3', 'class-modifiers'],
    difficulty: 'hard',
  },
  {
    id: 'dart-collections-014',
    front: 'What are Dart’s core collection types and their key characteristics?',
    back: "List (ordered, indexed,\ngrowable or fixed-length): `[1, 2, 3]`. Set (unordered, unique elements, hash-based): `{1, 2, 3}`.\nMap (key-value pairs, keys are unique): `{'a': 1}`. All implement Iterable, supporting `map`,\n`where`, `reduce`, `fold`, `any`, `every`, etc. Dart also provides Queue, LinkedList, and\nspecialized types through the collection and dart:collection\nlibrary.",
    tags: ['dart', 'collections'],
    difficulty: 'easy',
  },
  {
    id: 'dart-iterable-015',
    front: 'What is the\n`Iterable` interface in Dart?',
    back: 'Iterable is the lazy base class for all collections. It\nprovides a read-only view via an Iterator without materializing the full collection. Key methods:\n`map()`, `where()`, `expand()`, `reduce()`, `fold()`, `take()`, `skip()`, `any()`, `every()`,\n`firstWhere()`, `singleWhere()`. Iterables are chainable -- each operation returns a new lazy\nIterable. Call `.toList()` or `.toSet()` to\nmaterialize.',
    tags: ['dart', 'collections', 'iterable'],
    difficulty: 'medium',
  },
  {
    id: 'dart-exception-vs-error-016',
    front: 'What\nis the difference between `Exception` and `Error` in Dart?',
    back: 'Exception represents conditions\nthat a program should anticipate and handle (IOException, FormatException). Error represents\nprogramming bugs that should not be caught (StateError, TypeError, ArgumentError). Catching Error is\ndiscouraged -- it hides bugs. Use `on Exception catch (e)` to catch only exceptions, or `catch (e)`\nto catch everything. Dart does not have checked exceptions like\nJava.',
    tags: ['dart', 'error-handling'],
    difficulty: 'easy',
  },
  {
    id: 'dart-try-catch-finally-017',
    front: 'How\ndoes `try-catch-finally` work in Dart?',
    back: '`try` wraps code that may throw. `on Type catch (e)`\ncatches a specific type. `catch (e, stackTrace)` catches everything and provides the stack trace.\n`finally` always executes (cleanup code) regardless of whether an exception was thrown or caught.\nYou can combine:\n`try { ... } on FormatException catch (e) { ... } catch (e) { ... } finally { ... }`. Re-throw with\n`rethrow` to\npropagate.',
    tags: ['dart', 'error-handling'],
    difficulty: 'easy',
  },
  {
    id: 'dart-statelesswidget-vs-statefulwidget-018',
    front: 'What\nis the difference between StatelessWidget and StatefulWidget in Flutter?',
    back: 'StatelessWidget\ndescribes a part of the UI that depends only on its configuration (props). Its `build()` method is\ncalled with an `ImmutableWidget` context and returns a widget tree. It cannot change after being\nbuilt. StatefulWidget holds mutable state via a `State` object. When state changes, call\n`setState()` to trigger a rebuild. The `createState()` method returns a State instance that persists\nacross rebuilds and has access to `context` and lifecycle\nmethods.',
    tags: ['dart', 'flutter', 'widgets'],
    difficulty: 'medium',
  },
  {
    id: 'dart-state-management-019',
    front: 'What\nare the common state management patterns in Dart/Flutter?',
    back: '1) setState: Built-in, simple,\nscoped to one widget. 2) InheritedWidget / Provider: Propagate state down the tree via context. 3)\nRiverpod: Compile-safe, testable, no BuildContext dependency. 4) BLoC/Cubit: Events/states with\nstreams, separates business logic from UI. 5) GetX: Lightweight, uses observables. 6) Redux: Single\nstore, pure reducers, predictable. Choice depends on app complexity, team size, and testability\nrequirements.',
    tags: ['dart', 'flutter', 'state-management'],
    difficulty: 'medium',
  },
  {
    id: 'dart-testing-020',
    front: 'What\nare the three levels of testing in Dart/Flutter and what tools are used?',
    back: '1) Unit tests: Test\nindividual functions and classes in isolation. Use `test` package and `test()` function. Mock\ndependencies with `mockito` or `mocktail`. 2) Widget tests: Test individual UI widgets in a test\nenvironment. Use `flutter_test` with `testWidgets()` and `WidgetTester` to pump widgets, tap, enter\ntext, and verify output. 3) Integration tests: Test the full app end-to-end on a real\ndevice/emulator. Use `integration_test` package with `testWidgets()` driving the actual app. Run\nwith `flutter test integration_test/`.',
    tags: ['dart', 'testing', 'flutter'],
    difficulty: 'easy',
  },
  {
    id: 'dart-isolates-021',
    front: 'What are isolates in Dart and why does Dart use them instead of threads?',
    back: 'Isolates are independent execution units with their own memory and event loop. They communicate via message passing (send/receive) rather than shared state. Dart uses isolates because the Dart VM does not have a GIL -- isolates avoid shared-memory concurrency issues entirely. Flutter uses a single main isolate for UI; use Isolate.spawn for heavy computation.',
    tags: ['dart', 'concurrency'],
    difficulty: 'medium',
  },
  {
    id: 'dart-typed-lists-022',
    front: 'What is the difference between List<dynamic> and List<Object>?',
    back: 'List<dynamic> opts out of type checking entirely -- any type can be added and elements are not checked at runtime. List<Object> enforces that elements are Object instances, but since almost everything is an Object, it provides little practical type safety. List<String> provides compile-time type safety. Always use the most specific type possible.',
    tags: ['dart', 'types'],
    difficulty: 'easy',
  },
  {
    id: 'dart-cascade-023',
    front: 'What does the cascade operator (..) do in Dart?',
    back: 'The cascade operator lets you perform a sequence of operations on the same object without repeating the reference. Example: button..label = "Click"..color = Colors.blue; returns the original button, not the result of the last operation. The single-dot cascade (?.) short-circuits on null. Cascades are especially useful for method chaining and builder patterns.',
    tags: ['dart', 'operators'],
    difficulty: 'easy',
  },
  {
    id: 'dart-assert-024',
    front: 'How does the assert statement work in Dart and when is it stripped out?',
    back: 'assert(condition, "message") checks a boolean condition at development time. If false, throws an AssertionError. In production mode (--release or dart compile), all asserts are stripped and have zero cost. Asserts are for debugging invariants, not for runtime validation. Use exceptions or if-checks for production error handling.',
    tags: ['dart', 'debugging'],
    difficulty: 'easy',
  },
  {
    id: 'dart-mixin-class-025',
    front: 'What is a mixin class in Dart 3 and how is it different from a regular mixin?',
    back: 'A mixin class (declared with `mixin class`) can be used both as a mixin (with `with`) and as a regular class (with `extends` or `implements`). Regular mixins cannot have constructors and cannot be extended. A mixin class can have a constructor and can be extended, making it more flexible for class hierarchies.',
    tags: ['dart', 'dart3', 'mixins'],
    difficulty: 'medium',
  },
  {
    id: 'dart-enum-026',
    front: 'What are enhanced enums in Dart 2.17+ and what capabilities do they add?',
    back: "Enhanced enums support fields, constructors, methods, and can implement interfaces. Each value is an instance of the enum type. Example: `enum Color { red('FF0000'); final String hex; const Color(this.hex); }`. This replaces the old pattern of using separate classes with static const values for rich enum-like behavior.",
    tags: ['dart', 'enums'],
    difficulty: 'medium',
  },
  {
    id: 'dart-futures-error-027',
    front: 'What happens if an async function throws an exception without a try-catch?',
    back: "The exception is caught by the zone error handler or the zone's unhandled error callback. If no zone handler is set, the error is printed to stderr and the program continues. Future errors that are never listened to (unawaited futures) trigger the Zone.error handler. Always catch errors with try-catch in async code or handle them with catchError on Futures.",
    tags: ['dart', 'async', 'errors'],
    difficulty: 'medium',
  },
  {
    id: 'dart-keyword-const-028',
    front: 'What is the difference between `final`, `const`, and `static` in Dart?',
    back: 'final can be set only once (runtime constant). const is a compile-time constant that must be known at compile time. static is a class-level member, not tied to any instance. A final instance variable can be set in the constructor; a const variable cannot. Static final combines class-level scope with single assignment.',
    tags: ['dart', 'variables'],
    difficulty: 'easy',
  },
  {
    id: 'dart-completer-029',
    front: 'When would you use a StreamController in Dart?',
    back: 'StreamController creates and manages a Stream. Use it when you need to add events manually via controller.add() and controller.close(). It is useful for bridging callback-based APIs with Stream consumers, creating event buses, or implementing custom stream sources. Use StreamController.broadcast() for multiple listeners.',
    tags: ['dart', 'async', 'streams'],
    difficulty: 'medium',
  },
  {
    id: 'dart-iterable-030',
    front: 'What does the `expand` method do on an Iterable?',
    back: 'expand takes a function that returns an iterable for each element and flattens the results into a single iterable. Example: [[1,2],[3,4]].expand((e) => e) yields 1,2,3,4. It is the flatMap of Dart collections. Use it when each element maps to multiple output elements.',
    tags: ['dart', 'collections', 'iterable'],
    difficulty: 'medium',
  },
]
