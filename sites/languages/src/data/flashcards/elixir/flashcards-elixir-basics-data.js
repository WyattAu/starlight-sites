export const flashcardData = [ { id: 'elixir-creator-001', front: 'Who created Elixir and in what year?', back: 'Jose Valim created Elixir in 2011. He was a core Ruby on Rails contributor who wanted to combine the Erlang VM\'s proven concurrency and fault tolerance with a modern, Ruby-like syntax.',tags: ['elixir', 'history'], difficulty: 'easy' }, { id: 'elixir-beam-002', front: 'What is the BEAM and why is it important for Elixir?', back: 'BEAM (Bogdan\'s Erlang Abstract Machine) is the Erlang virtual machine that executes Elixir code. It provides lightweight preemptive scheduling, per-process garbage collection, transparent distribution, and hot code swapping -- enabling millions of concurrent processes.', tags: ['elixir', 'runtime'], difficulty: 'easy' }, { id: 'elixir-immutability-003', front: 'What does immutability mean in Elixir?', back: 'All data in Elixir is immutable. When you "modify" a variable, you create a new binding pointing to a new value. The old value is unchanged. This eliminates race conditions, enables safe concurrency without locks, and allows structural sharing for efficiency.', tags: ['elixir', 'paradigm'], difficulty: 'easy' }, { id: 'elixir-pattern-match-004', front: 'What is the difference between = (match) and ^ (pin) in Elixir?', back: 'The = operator is a match operator that binds unbound variables to matched values. The ^ pin operator prevents rebinding -- it forces comparison against the current value of the variable. Use ^x = 2 to assert x is already 2; use x = 2 to bind or rebind x.', tags: ['elixir', 'pattern-matching'], difficulty: 'easy' }, { id: 'elixir-atoms-005', front: 'What are atoms in Elixir and what are they used for?', back: 'Atoms are constants whose name is their value, written with a colon prefix (:ok, :error, :hello). They are globally unique, never garbage-collected, and commonly used for tagging tuples ({:ok, value}, {:error, reason}), status codes, and map keys.', tags: ['elixir', 'types'], difficulty: 'easy' }, { id: 'elixir-lists-006', front: 'Why is list prepend O(1) but append O(n) in Elixir?', back: 'Lists are singly-linked in Elixir. Prepending [head | tail] creates a new head node pointing to the existing tail in constant time. Appending list ++ [x] must traverse and copy the entire list, making it O(n). Prefer building lists by prepending and reversing.', tags: ['elixir', 'data-structures'], difficulty: 'easy' }, { id: 'elixir-pipe-007', front: 'What does the pipe operator (|>) do in Elixir?', back: 'The pipe operator passes the result of one expression as the first argument to the next function. expr |> fun(args) is equivalent to fun(expr, args). It enables readable data transformation pipelines like [1,2,3] |> Enum.map(&(&1*2)) |> Enum.sum().', tags: ['elixir', 'operators'], difficulty: 'easy' }, { id: 'elixir-anonymous-fn-008', front: 'What is the difference between fn...end and & shorthand for anonymous functions?', back: 'fn...end creates a full anonymous function with explicit parameters. & is a capture shorthand: &(&1 * 2) captures a one-argument function. &Mod.fun/arity captures an existing named function. Both produce the same function type, but & is more concise for simple cases.', tags: ['elixir', 'functions'], difficulty: 'medium' }, { id: 'elixir-structs-009', front: 'What is the difference between a struct and a map in Elixir?', back: 'Structs are maps with a fixed set of fields, default values, and compile-time type checking. They require defstruct, support @enforce_keys, and pattern match by type (%User{} only matches User structs). Maps have dynamic keys and no type constraint.', tags: ['elixir', 'data-structures'], difficulty: 'medium' }, { id: 'elixir-guards-010', front: 'What expressions are allowed in guard clauses (when) and why?', back: 'Guards allow only a limited set of safe expressions: comparison operators, boolean operators (and/or/not), arithmetic, type-check functions (is_atom, is_list, etc.), and a few BIFs (abs, div, elem, length, map_size). This ensures guards are free of side effects and guaranteed to terminate.', tags: ['elixir', 'pattern-matching'], difficulty: 'medium' }, { id: 'elixir-with-expr-011', front: 'How does the with expression work in Elixir?', back: 'with chains pattern matches sequentially. Each <- line matches a pattern; if it fails, execution jumps to the else clause. Bare = raises MatchError on failure. It eliminates deeply nested case statements when processing {:ok, value} / {:error, reason} chains.', tags: ['elixir', 'control-flow'], difficulty: 'medium' }, { id: 'elixir-genserver-012', front: 'What is GenServer and what are its main callbacks?', back: 'GenServer (Generic Server) is the primary OTP abstraction for stateful server processes. Main callbacks: init/1 initializes state, handle_call/3 for synchronous calls (reply), handle_cast/2 for async messages (no reply), handle_info/2 for unexpected messages, terminate/2 for cleanup.', tags: ['elixir', 'otp'], difficulty: 'medium' }, { id: 'elixir-supervisor-013', front: 'What are the three Supervisor restart strategies?', back: ':one_for_one restarts only the crashed child (independent workers). :one_for_all restarts ALL children when any one crashes (tightly coupled processes). :rest_for_one restarts the crashed child and all children defined after it in the list (sequential dependencies).', tags: ['elixir', 'otp'], difficulty: 'medium' }, { id: 'elixir-call-cast-014', front: 'What is the difference between GenServer.call and GenServer.cast?', back: 'call is synchronous -- the caller blocks until it receives {:reply, reply, state} from handle_call/3. cast is asynchronous -- it returns :ok immediately and the server processes via handle_cast/2 with {:noreply, state}. Use call when you need a result, cast for fire-and-forget.', tags: ['elixir', 'otp'], difficulty: 'medium' }, { id: 'elixir-link-monitor-015', front: 'What is the difference between spawn_link and spawn_monitor?', back: 'spawn_link creates a bidirectional link: if one process crashes, the exit signal propagates to the other (both can crash). spawn_monitor creates a one-way monitor: the monitor receives {:DOWN, ref, :process, pid, reason} but does not crash. Use links for paired processes, monitors for observation.', tags: ['elixir', 'concurrency'], difficulty: 'medium' }, { id: 'elixir-protocols-016', front: 'How do Protocols differ from Behaviours in Elixir?', back: 'Protocols provide polymorphic dispatch based on the type of the first argument (like interfaces). They use defprotocol/defimpl. Behaviours define a set of callbacks a module must implement (like abstract classes). They use @callback/@impl. Protocols dispatch by type; behaviours enforce a contract.', tags: ['elixir', 'abstractions'], difficulty: 'medium' }, { id: 'elixir-let-crash-017', front: 'What is the "let it crash" philosophy in Elixir/Erlang?', back: 'Instead of defensive error handling with try/rescue everywhere, let processes crash and rely on supervisors to restart them with clean state. This works because processes are cheap, isolated, and quickly restarted. Supervision trees ensure system-level fault tolerance while individual components can fail safely.', tags: ['elixir', 'concurrency', 'philosophy'], difficulty: 'medium' }, { id: 'elixir-macros-018', front: 'What is the difference between defmacro and def in Elixir?', back: 'def defines a regular function -- arguments are evaluated at the call site. defmacro defines a macro -- arguments are unevaluated AST that gets expanded at compile time. Macros can inspect and transform code structure. Use macros for DSLs and compile-time code generation; prefer functions otherwise.', tags: ['elixir', 'metaprogramming'], difficulty: 'hard' }, { id: 'elixir-ast-019', front: 'What is the structure of Elixir\'s AST representation?', back: 'The AST is a three-element tuple: {atom, metadata, arguments}. The first element is the operation (:def, :+, :if). The second is metadata (line numbers, context). The third is a list of arguments (themselves AST tuples or literals). Literals like integers and strings represent themselves.', tags: ['elixir', 'metaprogramming'],difficulty: 'hard' }, { id: 'elixir-hygiene-020', front: 'What is macro hygiene in Elixir and how can it be bypassed?', back: 'Hygiene prevents macros from accidentally capturing or overwriting the caller\'s variables. Variables defined in a macro have their own context. To intentionally access or set the caller\'s variables, use var!(variable_name) to break hygiene, or bind_quoted to safely inject values while preserving hygiene.', tags: ['elixir', 'metaprogramming'], difficulty: 'hard' }, ];
