export const flashcards1 = [
  {
    id: 'kotlin-null-safety-001',
    front:
      'What is the difference between the safe call operator `?.` and the non-null assertion `!!`?',
    back: 'The safe call operator `?.` returns null if the receiver is null without throwing an exception. The non-null assertion `!!` throws a `NullPointerException` if the receiver is null. Prefer `?.` or the elvis operator `?:` over `!!` -- use `!!` only when you are certain the value is non-null and want an explicit crash on programmer error.',
    tags: ['kotlin', 'null-safety'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-null-safety-002',
    front: 'How does the elvis operator `?:` work and what is its return type?',
    back: 'The elvis operator `a ?: b` returns `a` if it is non-null, otherwise returns `b`. If `a` has type `T?`, then `b` must have type `T` and the expression has type `T`. On the right side, `return` and `throw` are also allowed since they have type `Nothing`, which is a subtype of every type. This makes patterns like `val name = user.name ?: throw IllegalArgumentException(...)` possible.',
    tags: ['kotlin', 'null-safety'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-null-safety-003',
    front: 'What does `let` do and how is it used for null safety?',
    back: '`let` is a scope function that executes a lambda with the receiver as `it` and returns the lambda result. With null safety, it chains with `?.`: `nullableValue?.let { useNonNull(it) }` executes the block only if `nullableValue` is non-null, converting the nullable type to a non-null context inside the lambda. This avoids nested `if (x != null)` checks.',
    tags: ['kotlin', 'null-safety', 'scope-functions'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-coroutines-004',
    front: 'What does the `suspend` keyword mean on a function?',
    back: 'A `suspend` function can suspend its execution at suspension points without blocking the underlying thread. It can only be called from a coroutine or another suspend function. The Kotlin compiler transforms suspend functions into callback-based state machines at compile time, enabling structured concurrency with efficient thread utilization.',
    tags: ['kotlin', 'coroutines'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-coroutines-005',
    front: 'What is the difference between `launch` and `async`?',
    back: '`launch` starts a fire-and-forget coroutine that returns a `Job` with no result value. `async` starts a coroutine that returns a `Deferred<T>`, which you can call `.await()` on to obtain the result. Use `launch` for side effects (logging, updating UI) and `async` when you need to compute and return a value. Multiple `async` coroutines can run concurrently and their results awaited together.',
    tags: ['kotlin', 'coroutines'],
    difficulty: 'easy',
  },
]

export const flashcards2 = [
  {
    id: 'kotlin-coroutines-006',
    front: 'What are the main Coroutine Dispatchers and when should you use each?',
    back: '`Dispatchers.Main` runs coroutines on the main/UI thread (Android UI updates). `Dispatchers.IO` is optimized for I/O operations like network calls and disk reads, using a thread pool sized for blocking I/O. `Dispatchers.Default` is optimized for CPU-bound work with a pool sized equal to CPU cores. `Dispatchers.Unconfined` starts in the caller’s thread but resumes in whichever thread the first suspension point resumes on -- rarely recommended.',
    tags: ['kotlin', 'coroutines'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-coroutines-007',
    front: 'What is a `Flow` and how does it differ from a `Sequence`?',
    back: '`Flow` is a cold asynchronous stream of values that can emit multiple values over time, supporting coroutines and structured concurrency. Unlike `Flow`, a `Sequence` is synchronous and lazy -- it computes values one at a time on demand within a single coroutine. `Flow` supports operators like `flowOn` (changing dispatcher), `catch` (error handling), and `collect` (terminal operator), and integrates naturally with coroutine cancellation.',
    tags: ['kotlin', 'coroutines'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-scope-functions-008',
    front: 'What are the five Kotlin scope functions and how do they differ?',
    back: '`let` (context as `it`, returns lambda result), `run` (context as `this`, returns lambda result), `apply` (context as `this`, returns receiver), `also` (context as `it`, returns receiver), `with` (context as `this`, returns lambda result, called as `with(obj) {}` not an extension). Group by return: `let`/`run`/`with` return the lambda result; `apply`/`also` return the receiver.',
    tags: ['kotlin', 'scope-functions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-scope-functions-009',
    front: 'When should you use `apply` vs `also`?',
    back: 'Use `apply` when you want to configure an object’s properties (context is `this`, so you can refer to members directly). Use `also` when you need a side effect like logging or debugging (context is `it`, so you avoid shadowing class members). Both return the receiver object, making them chainable. Common pattern: `return user.also { log(it) }`.',
    tags: ['kotlin', 'scope-functions'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-data-classes-010',
    front: 'What does a Kotlin `data class` provide automatically?',
    back: 'A `data class` automatically generates `equals()`, `hashCode()`, `toString()`, `componentN()` functions (for destructuring), and a `copy()` method. The primary constructor must have at least one parameter, and those parameters are used for `equals`/`hashCode`. Data classes cannot be abstract, open, sealed, or inner. For immutability, declare properties as `val`.',
    tags: ['kotlin', 'data-classes'],
    difficulty: 'easy',
  },
]

export const flashcards3 = [
  {
    id: 'kotlin-sealed-classes-011',
    front: 'What are sealed classes and sealed interfaces, and why are they useful?',
    back: 'Sealed classes and sealed interfaces restrict inheritance to a fixed set of subclasses defined in the same package. This allows the compiler to perform exhaustive checks in `when` expressions without an `else` branch. Sealed interfaces (Kotlin 1.5+) allow multiple inheritors without the single-class constraint. They are ideal for modeling result types, state machines, and domain events where all cases must be handled.',
    tags: ['kotlin', 'sealed-classes'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-extension-functions-012',
    front: 'What are extension functions and how do they work under the hood?',
    back: 'Extension functions let you add methods to existing classes without modifying their source code. They are resolved statically at compile time -- they do not modify the original class or add members to its type. Under the hood, an extension function `fun String.addExclamation() = this + "!"` compiles to a static function that takes the receiver as its first parameter. They cannot access private members of the receiver class.',
    tags: ['kotlin', 'extensions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-delegation-013',
    front: 'How does the `by` keyword work for class delegation?',
    back: 'The `by` keyword delegates the implementation of an interface to another object. For example, `class Derived(b: Base) : Base by b` forwards all `Base` interface method calls to `b`. You can override specific methods while delegating the rest. Kotlin also supports property delegation with `by lazy`, `by observable`, `by vetoable`, and custom delegates via `ReadWriteProperty`.',
    tags: ['kotlin', 'delegation'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-companion-objects-014',
    front: 'What is a companion object and how does it relate to static members?',
    back: 'A companion object is a singleton object associated with a class, declared with `companion object`. Members of the companion object can be accessed using the class name directly (e.g., `MyClass.create()`), similar to Java static methods. Unlike Java static members, a companion object is an actual object and can implement interfaces, have an extension function, and be passed as a parameter. Only one companion object is allowed per class.',
    tags: ['kotlin', 'companion-objects'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-type-system-015',
    front: 'What are `Nothing`, `Unit`, and `Any` in the Kotlin type hierarchy?',
    back: '`Any` is the root of the non-nullable type hierarchy (equivalent to Java `Object`). `Unit` is the type with exactly one value, used for functions that return nothing meaningful (like Java `void`). `Nothing` has no instances and is a subtype of every type -- it represents unreachable code (e.g., a function that always throws or never returns). The nullable type `Nothing?` is the only type that can be assigned to any nullable reference.',
    tags: ['kotlin', 'type-system'],
    difficulty: 'medium',
  },
]

export const flashcards4 = [
  {
    id: 'kotlin-collections-016',
    front: 'What is the difference between a `List` and a `Sequence` in Kotlin?',
    back: 'A `List` is eager -- all elements are computed and stored in memory at creation time, supporting indexed access and multiple traversals. A `Sequence` is lazy -- elements are computed one at a time on demand, supporting only a single traversal. Chaining multiple operations on a `Sequence` avoids intermediate collections. Use `Sequence` for large datasets or expensive transformations where you iterate only once.',
    tags: ['kotlin', 'collections'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-inline-functions-017',
    front: 'What are `inline` functions and what are their benefits and limitations?',
    back: 'The `inline` keyword tells the compiler to substitute the function body at each call site, avoiding the overhead of creating a lambda object and a function call. This is useful for higher-order functions that accept lambdas. However, `inline` functions cannot access private members of the enclosing class or recurse. `noinline` on individual lambda parameters prevents inlining for that specific parameter.',
    tags: ['kotlin', 'inline-functions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-reified-types-018',
    front: 'What are reified type parameters and why are they needed?',
    back: 'Reified type parameters (using the `inline` and `reified` keywords) preserve type information at runtime, which is normally erased by the JVM. For example, `inline fun <reified T> isType(value: Any) = value is T` would fail to compile without `reified` because `T` is erased at runtime. Only `inline` functions can have `reified` type parameters since the compiler substitutes the actual type at each call site.',
    tags: ['kotlin', 'generics', 'reified'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-null-safety-019',
    front: 'What are smart casts in Kotlin and how do they work?',
    back: 'Smart casts allow the compiler to automatically cast a variable to a more specific type after a null check or type check. For example, after `if (x is String)`, the compiler knows `x` is a `String` in the true branch and allows calling String methods directly. Smart casts work with immutable local variables (`val`) and cannot apply to mutable properties that could change between the check and usage.',
    tags: ['kotlin', 'null-safety', 'type-system'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-scope-functions-020',
    front: 'How do `run` and `with` differ, and when would you use each?',
    back: '`run` is an extension function: `obj.run { this.toUpperCase() }` -- it calls the block with the receiver as `this`. `with` is not an extension: `with(obj) { toUpperCase() }` -- it takes the receiver as a parameter. Both return the lambda result. Use `run` for chaining on nullable types (`nullable?.run { ... }`) and `with` when you have a clear grouping of operations on a non-null object.',
    tags: ['kotlin', 'scope-functions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-nullable-types-021',
    front: 'How do you convert a nullable type to a non-null type safely in Kotlin?',
    back: 'Use ?.let { } to execute code only if non-null. Use ?: (elvis) to provide a default. Use assert (requireNotNull(value)) to crash if null. Use !! only when you are certain the value is non-null. Smart casts after if (x != null) automatically narrow the type.',
    tags: ['kotlin', 'null-safety'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-scope-functions-022',
    front: 'When should you use `with` instead of `run`?',
    back: 'with(obj) { } is not an extension function -- it takes the object as an explicit parameter. obj.run { } is an extension function with the object as receiver. Use with when you have an existing non-null object and want to group operations. Use run when chaining on nullable types or when you prefer the extension function syntax.',
    tags: ['kotlin', 'scope-functions'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-collection-ops-023',
    front: 'What is the difference between map and flatMap in Kotlin?',
    back: 'map transforms each element: listOf(1,2,3).map { it * 2 } yields [2,4,6]. flatMap transforms each element into a collection and flattens the results: listOf(1,2,3).flatMap { listOf(it, it*10) } yields [1,10,2,20,3,30].',
    tags: ['kotlin', 'collections'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-destructuring-024',
    front: 'What are destructuring declarations in Kotlin?',
    back: 'val (name, age) = Person("Alice", 30) decomposes an object into variables using componentN() functions. Data classes auto-generate these. Works with maps: for ((key, value) in map). Destructuring in lambdas: map.map { (key, value) -> "$key: $value" }.',
    tags: ['kotlin', 'syntax'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-typealias-025',
    front: 'What is a typealias in Kotlin and when would you use it?',
    back: 'typealias creates an alternative name for an existing type: typealias UserMap = Map<String, User>. It does not create a new type (unlike a value class). Use it to reduce verbosity in complex generic types and improve readability in code that references the same type repeatedly.',
    tags: ['kotlin', 'types'],
    difficulty: 'easy',
  },
  {
    id: 'kotlin-enum-class-026',
    front: 'What features do Kotlin enums support beyond simple constants?',
    back: 'Kotlin enums can have properties, methods, and implement interfaces. Each constant is an instance: enum class Color(val hex: String) { RED("FF0000"), BLUE("0000FF") }. They support valuesOf(name) to get a constant by string name and entries for iterating all constants.',
    tags: ['kotlin', 'enums'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-value-class-027',
    front: 'What is a value class in Kotlin and how does it differ from a data class?',
    back: 'A value class wraps a single value with zero runtime overhead at the JVM level: value class UserId(val id: String). The wrapper is inlined by the compiler. Unlike data classes, value classes have exactly one property, cannot have other properties, and their equals/hashCode are based on the wrapped value.',
    tags: ['kotlin', 'classes'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-sequence-028',
    front: 'When should you use asSequence() instead of direct collection operations?',
    back: 'Use asSequence() for large datasets where chaining many operations (map, filter, etc.) would create unnecessary intermediate collections. Sequences process elements one at a time through the entire pipeline. For small collections, direct operations are faster due to less overhead.',
    tags: ['kotlin', 'collections'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-result-029',
    front: 'What is the Result type in Kotlin and how do you use it?',
    back: 'Result<T> is a sealed class wrapping either a success value or an exception. Use runCatching { code } to get a Result. Access with .getOrNull(), .getOrElse { }, .exceptionOrNull(), or fold(). It provides a clean way to handle operations that may throw without try-catch.',
    tags: ['kotlin', 'error-handling'],
    difficulty: 'medium',
  },
  {
    id: 'kotlin-contracts-030',
    front: 'What are contracts in Kotlin and why are they needed?',
    back: 'Contracts declare guarantees about function behavior to the compiler: contract { returns() implies (value != null) } tells the compiler that if the function returns, the value is non-null. This enables smart casts and other compiler optimizations that would otherwise be impossible with black-box function calls.',
    tags: ['kotlin', 'advanced'],
    difficulty: 'hard',
  },
]
