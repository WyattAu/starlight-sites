export const flashcardData = [
  {
    id: 'typescript-type-system-001',
    front: 'What is the difference between `any` and `unknown` in TypeScript?',
    back: '`any` disables all type checking -- you can assign any value, perform any operation, and pass it anywhere without compiler errors. `unknown` is a type-safe alternative: you can assign any value to it, but you must narrow or assert the type before performing operations. `unknown` forces you to prove the type is correct before use, making it the preferred choice over `any` when the type is truly not known at design time.',
    tags: ['typescript', 'type-system'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-type-system-002',
    front: 'What is the `never` type and when is it inferred?',
    back: '`never` represents values that never occur. It is inferred in unreachable code branches (e.g., after a `throw` statement or in a function with no return) and in exhaustive type checks. For example, in a discriminated union `when` expression, assigning `never` to the default case proves exhaustiveness -- if a new variant is added later, the compiler will error because the `never` branch would be reachable.',
    tags: ['typescript', 'type-system'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-type-system-003',
    front: 'What is the difference between `void` and `never` in TypeScript?',
    back: '`void` means a function returns without a value (or returns `undefined`). `never` means a function never returns at all (e.g., it always throws, enters an infinite loop, or has an unreachable endpoint). A `void` function is callable and completes normally; a `never` function is a type-level signal that control never reaches the calling code.',
    tags: ['typescript', 'type-system'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-type-system-004',
    front: 'What are union and intersection types?',
    back: 'A union type (`A | B`) means a value can be of type A or type B -- the value is one of the members. An intersection type (`A & B`) means a value has all properties of both A and B simultaneously -- it combines multiple types into one. Unions require narrowing to access type-specific members; intersections grant access to all combined members immediately.',
    tags: ['typescript', 'type-system'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-generics-005',
    front: 'What are generic constraints and how do you define them?',
    back: 'Generic constraints restrict the types a generic parameter can accept using the `extends` keyword. For example, `function getLength<T extends { length: number }>(item: T): number` ensures `T` must have a `length` property. This allows you to safely access members of the constraint within the function body. Multiple constraints use intersection: `<T extends A & B>`.',
    tags: ['typescript', 'generics'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-generics-006',
    front: 'What are conditional types in TypeScript?',
    back: 'Conditional types select one of two types based on a condition: `T extends U ? X : Y`. They enable type-level programming. For example, `type IsString<T> = T extends string ? true : false`. When used with `infer`, they can extract and transform types: `type ReturnType<T> = T extends (...args: any[]) => infer R ? R : never`. Distributive conditional types apply the check to each member of a union automatically.',
    tags: ['typescript', 'generics'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-generics-007',
    front: 'What are mapped types and how are they used?',
    back: 'Mapped types create new types by transforming each property of an existing type. The syntax is `type NewType<T> = { [K in keyof T]: Transform<T[K]> }`. TypeScript has built-in mapped type aliases like `Partial<T>`, `Required<T>`, `Readonly<T>`, and `Pick<T, K>`. You can add or remove modifiers with `+` and `-`: `{ -readonly [K in keyof T]: T[K] }` removes readonly.',
    tags: ['typescript', 'generics'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-generics-008',
    front: 'What does the `infer` keyword do in TypeScript?',
    back: '`infer` declares a type variable to be inferred from within a conditional type. For example, `type ElementType<T> = T extends (infer U)[] ? U : never` extracts the element type from an array. `infer` can also extract return types (`T extends (...args: any) => infer R`), promise resolve types, and more. The inferred type exists only within the conditional branch and cannot be referenced outside it.',
    tags: ['typescript', 'generics'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-utility-types-009',
    front: 'What does `Partial<T>` do and how is it implemented?',
    back: '`Partial<T>` makes all properties of `T` optional by adding `?` to each property. It is implemented as `type Partial<T> = { [P in keyof T]?: T[P] }`. This is useful when you want to update only some fields of an object, such as in `Object.assign` or update functions that accept partial data.',
    tags: ['typescript', 'utility-types'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-utility-types-010',
    front: 'What is the difference between `Pick<T, K>` and `Omit<T, K>`?',
    back: "`Pick<T, K>` selects a subset of properties from `T` -- it keeps only the keys listed in `K`. `Omit<T, K>` removes a subset of properties from `T` -- it keeps everything except the keys listed in `K`. They are complementary operations: `Pick<User, 'name' | 'email'>` keeps name and email; `Omit<User, 'id' | 'password'>` removes id and password.",
    tags: ['typescript', 'utility-types'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-utility-types-011',
    front: 'What does `Record<K, V>` create?',
    back: "`Record<K, V>` creates an object type where each key in `K` maps to a value of type `V`. For example, `Record<string, number>` is `{ [key: string]: number }`. It is commonly used for dictionaries, lookup tables, and configuration maps. If `K` is a union of string literals (e.g., `'a' | 'b'`), it creates an exact object type with only those keys.",
    tags: ['typescript', 'utility-types'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-advanced-patterns-012',
    front: 'What are discriminated unions and how do they enable exhaustive checking?',
    back: "Discriminated unions (also called tagged unions) share a common discriminant property with literal types. For example, `{ type: 'loading' } | { type: 'success'; data: T } | { type: 'error'; error: Error }`. TypeScript narrows the union based on the discriminant, giving you type-safe access to variant-specific properties. With a `switch` statement covering all cases, assigning `never` to the default case catches missing variants at compile time.",
    tags: ['typescript', 'advanced-patterns'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-advanced-patterns-013',
    front: 'What are template literal types?',
    // biome-ignore lint/suspicious/noTemplateCurlyInString: backtick examples in educational content
    back: "Template literal types allow you to create string types by combining literal strings using template syntax: `type EventName = `on${Capitalize<string>}``. They support interpolation with unions for combinatorial string types: `type Direction = 'top' | 'bottom'; type Padding = `${Direction}-padding``produces`'top-padding' | 'bottom-padding'`. They are powerful for type-safe string APIs like CSS units and event names.",
    tags: ['typescript', 'advanced-patterns'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-advanced-patterns-014',
    front: 'What are branded types and why would you use them?',
    back: "Branded types (nominal typing via type branding) create distinct types that are structurally identical but semantically different. For example, `type UserId = string & { **brand: 'UserId' }`and`type OrderId = string & { **brand: 'OrderId' }`prevent accidentally passing a UserId where an OrderId is expected, even though both are strings at runtime. This is achieved using intersection with a unique symbol or branded property.",
    tags: ['typescript', 'advanced-patterns'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-enums-015',
    front: 'When should you use`const enum` vs a union of string literal types?',
    back: "`const enum` members are inlined at compile time, producing no runtime JavaScript code and improving performance. However, they cannot be iterated and are incompatible with isolatedModules. A union type (`type Status = 'active' | 'inactive'`) provides the same exhaustiveness checking in `switch`statements, works with isolatedModules, and is the approach recommended by the TypeScript team for most use cases.",
    tags: ['typescript', 'enums'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-classes-016',
    front: 'What is the difference between`implements`and`extends` for classes?',
    back: '`extends`creates inheritance (a subclass relationship) -- the child class inherits all members and can override them.`implements`declares that a class fulfills an interface contract -- TypeScript checks that the class has all required properties and methods, but no implementation is inherited. A class can`implement`multiple interfaces but can only`extend`one class.',
    tags: ['typescript', 'classes'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-classes-017',
    front: 'What are abstract classes in TypeScript?',
    back: 'Abstract classes are base classes that cannot be instantiated directly. They can define abstract methods (without implementation) that subclasses must implement, and concrete methods that subclasses inherit. Use`abstract`on the class and on methods that require implementation. Abstract classes provide a way to share common code while enforcing a contract -- they bridge the gap between interfaces and concrete classes.',
    tags: ['typescript', 'classes'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-tooling-018',
    front: 'What does`strict: true` enable in tsconfig.json?',
    back: '`strict: true`is a shorthand that enables all strict type-checking options:`strictNullChecks`, `strictFunctionTypes`, `strictBindCallApply`, `strictPropertyInitialization`, `noImplicitAny`, `noImplicitThis`, `alwaysStrict`, and `useUnknownInCatchVariables`. It is strongly recommended for all new TypeScript projects as it catches the most common categories of bugs at compile time, including null pointer errors and implicit any usage.',
    tags: ['typescript', 'tooling'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-react-019',
    front: 'What is the `FC` type and why has its usage declined?',
    back: '`FC<Props>`(or`FunctionComponent<Props>`) types a React function component. Historically it implicitly included `children`in Props. Since React 18 removed implicit children from FC, many developers prefer typing components as plain functions with explicit props:`function MyComponent({ title }: Props) { return ... }`. This is more explicit, avoids the unused `children`prop, and works better with generics.',
    tags: ['typescript', 'react'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-react-020',
    front: 'How do you type event handlers in React with TypeScript?',
    back: 'React provides generic event types from the`React`namespace:`React.MouseEvent<HTMLButtonElement>`for click events,`React.ChangeEvent<HTMLInputElement>`for input changes,`React.FormEvent<HTMLFormElement>`for form submissions. Event handlers are typed as`(event: React.EventType<HTMLElement>) => void`. Use `React.KeyboardEvent`for keyboard events and`React.FocusEvent` for focus events, always parameterizing with the specific HTML element type.',
    tags: ['typescript', 'react'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-utility-types-021',
    front: 'What does Readonly<T> do?',
    back: 'Readonly<T> makes all properties of T readonly: type ReadonlyUser = Readonly<User>. You cannot reassign any property after creation. It is a mapped type: type Readonly<T> = { readonly [P in keyof T]: T[P] }. Useful for enforcing immutability in functions that should not modify their arguments.',
    tags: ['typescript', 'utility-types'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-utility-types-022',
    front: 'What does Exclude<T, U> do?',
    back: 'Exclude<T, U> removes from union type T all members that are assignable to U. Example: Exclude<"a" | "b" | "c", "a"> yields "b" | "c". The opposite is Extract<T, U> which keeps only members assignable to U. Both operate on union types.',
    tags: ['typescript', 'utility-types'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-assert-023',
    front: 'What is the difference between a type assertion and a type guard?',
    back: 'A type assertion (value as Type or <Type>value) tells the compiler to treat a value as a specific type without runtime checking. A type guard (typeof, instanceof, in, custom function returning boolean) narrows the type at runtime. Type guards are safer; assertions bypass the type checker.',
    tags: ['typescript', 'type-system'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-pick-024',
    front: 'What does Pick<User, "name" | "email"> create?',
    back: 'Pick selects a subset of properties from a type. Pick<User, "name" | "email"> creates a type with only the name and email properties from User. It is a mapped type: type Pick<T, K extends keyof T> = { [P in K]: T[P] }. The complementary Omit<T, K> removes properties.',
    tags: ['typescript', 'utility-types'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-indexed-types-025',
    front: 'What are indexed access types in TypeScript?',
    back: 'T[K] extracts the type of a property from type T. Example: type NameType = User["name"] yields string. You can use unions: User["name" | "email"] yields string | string. It is the type-level equivalent of bracket notation at runtime.',
    tags: ['typescript', 'type-system'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-decorators-026',
    front: 'What are decorators in TypeScript?',
    back: 'Decorators are functions that modify classes, methods, properties, or parameters at declaration time (Stage 3 proposal). Apply with @decorator syntax. Class decorators receive the constructor; method decorators receive the prototype, method name, and descriptor. Enable logging, validation, and AOP patterns.',
    tags: ['typescript', 'advanced-patterns'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-readonly-027',
    front: 'What is the `readonly` modifier in TypeScript?',
    back: 'readonly on a property prevents reassignment after initialization: class User { readonly id: number }. It does not make nested objects immutable (only the reference is readonly). For deep readonly, use Readonly<T> utility type or the Immutable.js library.',
    tags: ['typescript', 'types'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-type-assertion-028',
    front: 'What is the `satisfies` operator in TypeScript 4.9?',
    back: 'satisfies validates that a value matches a type without widening it: const config = { port: 8080 } satisfies Record<string, number>. The inferred type is { port: number } not Record<string, number>. It catches errors while preserving the most specific type information.',
    tags: ['typescript', 'advanced-patterns'],
    difficulty: 'medium',
  },
  {
    id: 'typescript-no-emit-029',
    front: 'What does "noEmit" do in tsconfig.json?',
    back: '"noEmit": true prevents TypeScript from emitting JavaScript output files. It is used when another tool (like esbuild, swc, or Babel) handles transpilation, and you only want TypeScript for type checking. This speeds up the build process since TypeScript only validates types.',
    tags: ['typescript', 'tooling'],
    difficulty: 'easy',
  },
  {
    id: 'typescript-declaration-030',
    front: 'What are .d.ts files and when do you need them?',
    back: 'Declaration files (.d.ts) describe the shape of JavaScript modules for TypeScript. They contain type information without implementation. You need them when using untyped JavaScript libraries. DefinitelyTyped (@types/*) provides community-maintained declarations for thousands of packages.',
    tags: ['typescript', 'tooling'],
    difficulty: 'easy',
  },
]
