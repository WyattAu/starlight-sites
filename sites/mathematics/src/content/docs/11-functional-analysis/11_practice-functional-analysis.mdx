---

date: 2026-07-24T02:22:58+01:00
title: Functional Analysis Practice (Interactive)
tags:
  - mathematics
  - university
description: "UNIVERSITY Mathematics practice problems: Functional Analysis Practice (Interactive). Comprehensive study material with definitions, examples, and assessment"
---


import PracticeProblem from "@components/PracticeProblem";

## Intuition

**Infinite-dimensional linear algebra:** Functional analysis extends linear algebra to infinite-dimensional spaces (function spaces). It studies operators on these spaces, providing the mathematical framework for quantum mechanics, signal processing, and partial differential equations.

**Why it matters:** Quantum mechanics is formulated in Hilbert spaces (complete inner product spaces), signal processing uses L2 spaces, and PDE theory uses Sobolev spaces. Functional analysis provides the language for all of these.

**The key insight:** Compactness in infinite dimensions replaces finiteness — a compact operator behaves like a finite-rank operator, which is why spectral theory works for compact operators on Hilbert spaces.

## University Mathematics -- Functional Analysis Practice

10 auto-graded practice problems at medium to hard difficulty. Select an answer, submit, and review
the explanation.

---

### Banach and Hilbert Spaces

<PracticeProblem client:only="solid-js" question={"Which of the following norms on $C[0,1]$ makes it a Banach space?&quot; options={[ '$\\|f\\|_\\infty = \\sup_{x \\in [0,1]} |f(x)|$', '$\\|f\\|_1 = \\int_0^1 |f(x)|\\, dx$', '$\\|f\\|_p = \\left(\\int_0^1 |f(x)|^p\\, dx\\right)^{1/p}$ for $1 \\leq p < \\infty$', '$\\|f\\| = |f(0)|$', ]} correctAnswer={0} explanation=&quot;$(C[0,1], \|\cdot\|_\infty)$ is complete: uniform limits of continuous functions are continuous. The $L^p$ norms ($1 \leq p < \infty$) do not yield completeness because $L^p$-limits of continuous functions may not be continuous (the completion of $C[0,1]$ in $L^p$-norm is $L^p[0,1]$). The norm $\|f\| = |f(0)|$ fails positive definiteness: any $f$ with $f(0) = 0$ has zero norm."} difficulty="easy"  />

<PracticeProblem client:only="solid-js" question={"In a Hilbert space $H$, which of the following characterizes when a norm comes from an inner product?&quot; options={[ 'The parallelogram law: $\\|x+y\\|^2 + \\|x-y\\|^2 = 2\\|x\\|^2 + 2\\|y\\|^2$', 'The triangle inequality', 'Homogeneity: $\\|\\alpha x\\| = |\\alpha| \\|x\\|$', 'Positive definiteness: $\\|x\\| = 0$ iff $x = 0$', ]} correctAnswer={0} explanation=&quot;The Jordan-von Neumann theorem states that a norm $\|\cdot\|$ on a vector space comes from an inner product if and only if it satisfies the parallelogram law. The triangle inequality, homogeneity, and positive definiteness hold for all norms. The parallelogram law is specific to inner product norms and fails, for example, in $\ell^1$ or $\ell^\infty$."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Let $e_n$ be the standard basis in $\\ell^2$. Which statement is true?&quot; options={[ '$e_n \\to 0$ strongly (in norm) as $n \\to \\infty$', '$e_n \\rightharpoonup 0$ weakly but $\\|e_n\\| = 1$ for all $n$', '$e_n$ has no weakly convergent subsequence', '$e_n$ converges weakly to some nonzero element', ]} correctAnswer={0} explanation=&quot;For any $y \in \ell^2$, $\langle e_n, y\rangle = y_n \to 0$ as $n \to \infty$ (since $\sum |y_n|^2 < \infty$ implies $y_n \to 0$). So $e_n \rightharpoonup 0$ weakly. But $\|e_n\| = 1$ for all $n$, so $e_n$ does not converge in norm to $0$. This is a standard example showing weak convergence is strictly weaker than strong convergence."} difficulty="medium"  />


---

### Fundamental Theorems

<PracticeProblem client:only="solid-js" question={"Let $T : X \\to Y$ be a bijective bounded linear operator. Under what condition is $T^{-1}$ guaranteed to be bounded?&quot; options={[ '$X$ and $Y$ must both be Banach spaces', '$X$ must be finite-dimensional', '$Y$ must be separable', '$T$ must have closed range', ]} correctAnswer={0} explanation=&quot;By the Bounded Inverse Theorem (a corollary of the Open Mapping Theorem), if $T : X \to Y$ is a bijective bounded operator between Banach spaces, then $T^{-1} : Y \to X$ is bounded. Completeness of both spaces is essential. Without it, $T^{-1}$ may be unbounded even if $T$ is bounded and bijective."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Which theorem guarantees that if $\{T_n\}$ is a sequence of bounded operators with $\\sup_n \\|T_n x\\| < \\infty$ for each $x$, then $\\sup_n \\|T_n\\| < \\infty$?&quot; options={[ 'Hahn-Banach Theorem', 'Uniform Boundedness Principle', 'Closed Graph Theorem', 'Riesz Representation Theorem', ]} correctAnswer={0} explanation=&quot;The Uniform Boundedness Principle (Banach-Steinhaus) states: if $X$ is a Banach space and ${T_\alpha} \subseteq \mathcal{B}(X, Y)$ is pointwise bounded, then $\sup_\alpha \|T_\alpha\| < \infty$. The proof relies on the Baire Category Theorem. The Hahn-Banach theorem is about extending functionals; the Closed Graph Theorem characterizes boundedness via closed graphs."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Why does the Closed Graph Theorem fail for the differentiation operator $Tf = f'$ on $C[0,1]$ with the supremum norm?&quot; options={[ 'Because $T$ is not linear', 'Because $C^1[0,1]$ is not complete under the $\\|\\cdot\\|_\\infty$ norm of $C[0,1]$', 'Because $T$ is not defined on all of $C[0,1]$', 'Both (b) and (c) are reasons', ]} correctAnswer={0} explanation=&quot;The differentiation operator $Tf = f'$ is defined on $C^1[0,1]$, which is not complete under the $\|\cdot\|_\infty$ norm of $C[0,1]$ (the completion is $C[0,1]$). The Closed Graph Theorem requires both domain and codomain to be Banach spaces. Since $C^1[0,1]$ with this norm is not complete, the theorem does not apply, and indeed $T$ is unbounded (consider $f_n(x) = x^n$)."} difficulty="hard"  />

## Common Mistakes

**Confusing weak and strong convergence:** In $\ell^2$, the standard basis $e_n$ converges weakly to 0 ($\langle e_n, y\rangle \to 0$ for all $y$) but does not converge in norm ($\|e_n\| = 1$ for all $n$). Weak convergence does not imply strong convergence. Do not conclude $\|x_n - x\| \to 0$ from $\langle x_n, y\rangle \to \langle x, y\rangle$ for all $y$.

**Assuming all bounded operators are compact:** On infinite-dimensional spaces, bounded operators need not be compact. The identity operator $I: \ell^2 \to \ell^2$ is bounded ($\|I\| = 1$) but not compact — it maps the unit ball to itself, which is not compact in infinite dimensions. Compact operators are a strict subset of bounded operators.

**Forgetting the completeness requirement in the Bounded Inverse Theorem:** A bijective bounded linear operator between Banach spaces has a bounded inverse, but if either space is not complete, this can fail. The differentiation operator on $C^1[0,1] \to C[0,1]$ is bijective and bounded but its inverse (antidifferentiation) is unbounded because $C^1[0,1]$ is not complete under the supremum norm.

---

### Compact Operators and Spectral Theory

<PracticeProblem client:only="solid-js" question={"Which of the following is true about a compact operator $T$ on an infinite-dimensional Banach space?&quot; options={[ '$T$ maps the closed unit ball to a compact set', '$T$ is always injective', '$T$ has a bounded inverse', '$T$ preserves the dimension of the unit ball', ]} correctAnswer={0} explanation=&quot;A compact operator $T$ maps the closed unit ball to a relatively compact set (its closure is compact). In infinite dimensions, the closed unit ball is never compact (Riesz\'s Lemma), so a compact operator must 'compress' the ball. Compact operators are never invertible on infinite-dimensional spaces: if $T$ were invertible, then $T^{-1}(TB_X) = B_X$ would be compact, contradicting that $B_X$ is not compact."} difficulty="medium"  />

<PracticeProblem client:only="solid-js" question={"Let $T$ be a compact self-adjoint operator on a separable Hilbert space $H$. Which statement about its eigenvalues is false?&quot; options={[ 'All nonzero eigenvalues have finite multiplicity', 'The nonzero eigenvalues can accumulate only at $0$', 'The eigenvectors form an orthonormal basis for $H$', '$T$ has exactly one eigenvalue', ]} correctAnswer={0} explanation=&quot;$T$ can have any number of nonzero eigenvalues (countably many, accumulating only at $0$). For example, the diagonal operator $T(x_1, x_2, \\ldots) = (\\lambda_1 x_1, \\lambda_2 x_2, \\ldots)$ with $\\lambda_n = 1/n$ has infinitely many eigenvalues. The other statements are true: nonzero eigenvalues have finite multiplicity (compactness), eigenvectors form an orthonormal basis, and nonzero eigenvalues accumulate only at $0$."} difficulty="hard"  />

## Common Mistakes

**Confusing weak and strong convergence:** In $\ell^2$, the standard basis $e_n$ converges weakly to 0 ($\langle e_n, y\rangle \to 0$ for all $y$) but does not converge in norm ($\|e_n\| = 1$ for all $n$). Weak convergence does not imply strong convergence. Do not conclude $\|x_n - x\| \to 0$ from $\langle x_n, y\rangle \to \langle x, y\rangle$ for all $y$.

**Assuming all bounded operators are compact:** On infinite-dimensional spaces, bounded operators need not be compact. The identity operator $I: \ell^2 \to \ell^2$ is bounded ($\|I\| = 1$) but not compact — it maps the unit ball to itself, which is not compact in infinite dimensions. Compact operators are a strict subset of bounded operators.

**Forgetting the completeness requirement in the Bounded Inverse Theorem:** A bijective bounded linear operator between Banach spaces has a bounded inverse, but if either space is not complete, this can fail. The differentiation operator on $C^1[0,1] \to C[0,1]$ is bijective and bounded but its inverse (antidifferentiation) is unbounded because $C^1[0,1]$ is not complete under the supremum norm.

## Cross-References

- **[Site Home](../../):** Main landing page for Mathematics notes.
- **[Linear Algebra](../linear-algebra):** Vector spaces, matrices, and linear transformations.
- **[Real Analysis](../real-analysis):** Rigorous treatment of real numbers and calculus.
- **[Practice](../../practice-*.mdx):** Practice problems for revision.
