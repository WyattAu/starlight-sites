---
title: 'C++ Flashcards: Memory Management'
description:
  'Interactive flashcards on RAII, smart pointers, move semantics, Rule of Five, memory leaks,
  dangling pointers, stack vs heap, and exception safety.'
tags:
  - C++
  - Flashcards
---

import FlashcardDeck from '@components/FlashcardDeck';
import { flashcards1, flashcards2, flashcards3, flashcards4 } from './flashcards-memory-management-data';


## C++ -- Memory Management Flashcards

20 interactive flashcards. Press Space to flip, rate 1-4 to schedule next review.

---

  {
    id: 'cpp-memory-management-001',
    front: 'What does RAII stand for and what is its core principle?',
    back: 'Resource Acquisition Is Initialization. A resource (memory, file, lock) is acquired in a constructor and released in a destructor, ensuring deterministic cleanup even when exceptions are thrown.',
    tags: ['raii'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-memory-management-002',
    front: 'What is the difference between stack and heap allocation?',
    back: 'Stack allocation is automatic: variables live within a scope and are freed on scope exit. Heap allocation (`new`) is manual: memory persists until explicitly `delete`d. Stack is faster but size-limited; heap is larger but slower and prone to leaks.',
    tags: ['stack', 'heap'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-memory-management-003',
    front: 'What is `std::unique_ptr` and how does it differ from a raw pointer?',
    back: '`std::unique_ptr` owns a heap-allocated object exclusively -- it cannot be copied, only moved. When destroyed, it automatically calls `delete`. Unlike a raw pointer, it cannot alias or be accidentally double-deleted.',
    tags: ['smart-pointers', 'unique_ptr'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-memory-management-004',
    front: 'When should you prefer `std::make_unique` over `new`?',
    back: '`std::make_unique<T>(args...)` is preferred over `new T(args...)` because it avoids a separate allocation for the control block (unlike `make_shared`), is exception-safe when used in expressions, and makes ownership explicit at the point of construction.',
    tags: ['smart-pointers', 'make_unique'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-memory-management-005',
    front: 'What is a dangling pointer and how does it commonly occur?',
    back: 'A dangling pointer points to memory that has been freed or gone out of scope. Common causes: returning a reference/pointer to a local variable, using a pointer after `delete`, or accessing an element in a invalidated container iterator.',
    tags: ['pointers', 'bugs'],
    difficulty: 'easy',
  },
];

  {
    id: 'cpp-memory-management-006',
    front: 'What is `std::shared_ptr` and how does its reference counting work?',
    back: '`std::shared_ptr` allows shared ownership via an internal reference count. Each copy increments the count; each destruction decrements it. When the count reaches zero, the managed object is deleted. Uses `std::make_shared` for single allocation.',
    tags: ['smart-pointers', 'shared_ptr'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-007',
    front: 'What problem does `std::weak_ptr` solve?',
    back: '`std::weak_ptr` provides non-owning observation of a `shared_ptr`-managed object. It breaks circular reference cycles (e.g., parent-child mutual ownership) that would otherwise prevent reference counts from reaching zero, causing memory leaks.',
    tags: ['smart-pointers', 'weak_ptr', 'memory-leaks'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-008',
    front: 'What is the Rule of Five in C++?',
    back: 'If a class defines (or deletes) any of the following, it should probably define all five: destructor, copy constructor, copy assignment operator, move constructor (`T(T&&)`), and move assignment operator (`T& operator=(T&&)`). C++11 onward.',
    tags: ['rule-of-five'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-009',
    front: 'What is the Rule of Zero?',
    back: 'If a class manages no resources (no raw pointers, handles, or `new`/`delete`), declare none of the special member functions -- let the compiler generate defaults. Use smart pointers and standard containers so you do not need to write any of them.',
    tags: ['rule-of-zero'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-010',
    front: 'What are move semantics and what problem do they solve?',
    back: 'Move semantics transfer ownership of resources from a source object (rvalue) to a destination, leaving the source in a valid but unspecified state. This avoids expensive deep copies of temporaries and enables efficient return-by-value and container operations.',
    tags: ['move-semantics'],
    difficulty: 'medium',
  },
];

  {
    id: 'cpp-memory-management-011',
    front: 'What is the difference between `std::make_shared` and `new` with `shared_ptr`?',
    back: '`std::make_shared<T>(args)` performs a single heap allocation for both the control block and the object, improving cache locality and exception safety. `shared_ptr<T>(new T(args))` performs two allocations and is not exception-safe in function call arguments.',
    tags: ['smart-pointers', 'make_shared'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-012',
    front: 'How can a circular reference with `shared_ptr` cause a memory leak?',
    back: 'If object A holds a `shared_ptr<B>` and B holds a `shared_ptr<A>`, their reference counts never reach zero -- neither is destroyed. Fix by replacing one side with `std::weak_ptr`, which does not increment the reference count.',
    tags: ['memory-leaks', 'shared_ptr', 'weak_ptr'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-013',
    front: 'What are custom deleters and how do you specify one?',
    back: 'A custom deleter is a callable invoked instead of `delete` when a smart pointer destroys its object. Specified as a template argument: `std::unique_ptr<FILE, decltype(&fclose)> f(fopen("a.txt", "r"), fclose);`. Enables managing non-memory resources.',
    tags: ['smart-pointers', 'deleters'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-014',
    front: 'What are the three levels of exception safety?',
    back: '**Basic**: operations leave objects in a valid state (no resource leaks, no undefined behavior). **Strong**: operations are atomic -- either fully succeed or have no effect (rollback on failure). **No-throw**: operations never throw exceptions (`noexcept`).',
    tags: ['exception-safety'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-015',
    front: 'What guarantees do C++ destructors provide?',
    back: 'Destructors are called in reverse order of construction for local objects when a scope exits (normally or via exception). They are never called for objects allocated with `new` that are not `delete`d. Making destructors `noexcept` (default in C++11+) is best practice.',
    tags: ['destructors', 'raii'],
    difficulty: 'medium',
  },
];

  {
    id: 'cpp-memory-management-016',
    front: 'What is the difference between `delete` and `delete[]`?',
    back: '`delete` frees memory for a single object and calls its destructor once. `delete[]` frees memory for an array and calls destructors for every element. Mixing them (e.g., `delete` on an array) is undefined behavior.',
    tags: ['new-delete', 'memory'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-memory-management-017',
    front: 'How does RAII interact with stack unwinding during exception propagation?',
    back: 'When an exception propagates, the stack is unwound: local objects in each exited scope have their destructors called. This is why RAII works for exception safety -- resources are released automatically even when exceptions interrupt control flow.',
    tags: ['raii', 'exceptions', 'stack-unwinding'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-memory-management-018',
    front: 'What is a memory allocator and how does `std::allocator` work?',
    back: 'An allocator abstracts memory allocation strategy. `std::allocator<T>` uses `::operator new` to allocate raw memory (`allocate(n)`) and placement new to construct objects (`construct(p, args)`). Separation allows containers to customize allocation without coupling to construction logic.',
    tags: ['allocators'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-memory-management-019',
    front: 'What is the resource acquisition is initialization (RAII) pattern for file handling?',
    back: 'Wrap a file handle in a class: open in constructor (`std::fstream`), close in destructor (automatic). Guarantees the file is closed even if an exception or early return occurs. `std::fstream`, `std::ifstream`, and `std::ofstream` follow this pattern natively.',
    tags: ['raii', 'file-handling'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-memory-management-020',
    front:
      'How do you implement the strong exception safety guarantee with the copy-and-swap idiom?',
    back: '1) Make a copy of the data. 2) Modify the copy (if it throws, the original is untouched). 3) Swap the copy into the object using `noexcept` swap. 4) The old data is destroyed when the temporary copy goes out of scope. Used for assignment operators.',
    tags: ['exception-safety', 'copy-and-swap', 'idioms'],
    difficulty: 'hard',
  },
];

<FlashcardDeck
  deckId="cpp-memory-management"
  title="C++ Memory Management Flashcards"
  description="Interactive flashcards on RAII, smart pointers, move semantics, Rule of Five, memory leaks, dangling pointers, stack vs heap, and exception safety."
  cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]}
/>
