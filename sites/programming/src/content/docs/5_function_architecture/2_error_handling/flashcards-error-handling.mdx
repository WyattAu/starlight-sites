---
title: 'C++ Flashcards: Error Handling'
description:
  'Interactive flashcards on C++ exceptions, std::exception hierarchy, error codes vs exceptions,
  std::optional, std::expected, stack unwinding, and safety.'
tags:
  - C++
  - Flashcards
---

import FlashcardDeck from '@components/FlashcardDeck';
import { flashcards1, flashcards2, flashcards3, flashcards4 } from './flashcards-error-handling-data';


## C++ -- Error Handling Flashcards

20 interactive flashcards. Press Space to flip, rate 1-4 to schedule next review.

---

  {
    id: 'cpp-error-handling-001',
    front: 'What are the three keywords used in C++ exception handling?',
    back: '`throw` signals an error by creating an exception object. `try` begins a block where exceptions are monitored. `catch` handles a specific exception type thrown within the preceding `try` block. Example: `try { f(); } catch (const std::runtime_error& e) { log(e.what()); }`',
    tags: ['exceptions', 'try-catch'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-error-handling-002',
    front: 'What is `noexcept` and why is it useful?',
    back: '`noexcept` (or `noexcept(true)`) declares that a function is guaranteed not to throw exceptions. Enables compiler optimizations (no unwinding tables needed) and allows containers and algorithms to make stronger guarantees. Destructors are implicitly `noexcept` since C++11.',
    tags: ['noexcept'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-error-handling-003',
    front: 'What is the base class of all standard C++ exceptions?',
    back: '`std::exception` (defined in `<exception>`). It provides the virtual member function `what()` returning a `const char*` description. Derived classes include `std::runtime_error` and `std::logic_error` for categorizing errors.',
    tags: ['std-exception', 'hierarchy'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-error-handling-004',
    front: 'What is `std::optional` and when should you use it?',
    back: '`std::optional<T>` (C++17) wraps a value that may or may not be present. Use it when a function has a meaningful "no result" case that is not an error. Check with `.has_value()`, access with `*` or `.value()`. Safer than returning sentinel values like `-1` or `nullptr`.',
    tags: ['std-optional', 'monadic'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-error-handling-005',
    front: 'What is `assert` and when is it active?',
    back: '`assert(expr)` from `<cassert>` tests an expression and calls `std::abort()` if it is false. Active only when `NDEBUG` is **not** defined (debug builds). Use for checking internal invariants that should never fail. Dead code in release builds -- zero runtime cost.',
    tags: ['assertions', 'debugging'],
    difficulty: 'easy',
  },
];

  {
    id: 'cpp-error-handling-006',
    front: 'What is stack unwinding and how does it relate to exception handling?',
    back: 'When an exception is thrown and no matching `catch` is found in the current scope, the runtime "unwinds" the stack: destructors of all local objects in each exited frame are called automatically. This is the mechanism that makes RAII-based cleanup work during exception propagation.',
    tags: ['stack-unwinding', 'raii', 'exceptions'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-007',
    front: 'What is the difference between `std::logic_error` and `std::runtime_error`?',
    back: '`std::logic_error` represents programming bugs (violations of preconditions, invariants): `std::invalid_argument`, `std::out_of_range`, `std::domain_error`. `std::runtime_error` represents errors outside program control: `std::overflow_error`, `std::underflow_error`, `std::range_error`.',
    tags: ['std-exception', 'hierarchy'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-008',
    front: 'How do error codes compare to exceptions as an error handling strategy?',
    back: 'Error codes (`std::error_code`, return values) are lightweight, explicit, and compatible with C interfaces and systems without exception support. Exceptions separate error path from success path, propagate automatically through call stacks, and enforce handling via `catch`. Choose based on performance needs and domain.',
    tags: ['error-codes', 'exceptions', 'strategy'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-009',
    front: 'What is a catch-all handler and what is its syntax?',
    back: '`catch (...)` catches any exception type. Useful as a last-resort handler to prevent uncaught exceptions from calling `std::terminate()`. Cannot access the exception object directly. Commonly used to log, translate, or rethrow after cleanup.',
    tags: ['try-catch', 'catch-all'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-010',
    front: 'How do you create a custom exception class in C++?',
    back: 'Derive from `std::runtime_error` or `std::logic_error` (which inherit `std::exception`). Implement a constructor that passes a message to the base: `class NetworkError : public std::runtime_error { public: NetworkError(const string& msg) : std::runtime_error(msg) {} };`',
    tags: ['exceptions', 'custom'],
    difficulty: 'medium',
  },
];

  {
    id: 'cpp-error-handling-011',
    front: 'What is `std::expected<T, E>` (C++23) and how does it differ from `std::optional<T>`?',
    back: '`std::expected<T, E>` holds either a value `T` or an error `E`. Unlike `std::optional` (which only indicates absence), `std::expected` carries error information. Use `.value()` for the result or `.error()` for the error. Combines the benefits of error codes and exceptions without throwing.',
    tags: ['std-expected', 'c++23', 'monadic'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-012',
    front: 'What is `std::variant` and how does it handle errors?',
    back: '`std::variant<Ts...>` (C++17) is a type-safe union holding one of several types. Use it to represent a value-or-error pattern: `std::variant<Value, Error>`. Check which alternative is held with `std::holds_alternative<T>()` or `std::visit()`. More verbose than `std::expected` for error handling.',
    tags: ['std-variant', 'c++17'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-013',
    front: 'What is `static_assert` and how does it differ from `assert`?',
    back: '`static_assert(bool_constexpr, message)` is checked at compile time. If the condition is false, compilation fails with the message. Use for verifying type properties, static invariants, and configuration constraints. Has zero runtime cost and cannot be disabled.',
    tags: ['assertions', 'compile-time'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-014',
    front: 'How does RAII-based error handling differ from traditional try/catch?',
    back: 'RAII handles cleanup automatically via destructors without explicit try/catch. Resources (locks, files, memory) are released when scopes exit. Exception safety is achieved structurally rather than by writing catch blocks. Try/catch is still needed for decision logic on how to respond to errors.',
    tags: ['raii', 'strategy', 'exceptions'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-error-handling-015',
    front: 'How do you rethrow an exception in C++?',
    back: 'Use `throw;` (bare throw) inside a catch block to rethrow the currently handled exception. Preserves the original exception type and stack information. Avoid `throw e;` which copies the exception and may slice derived types to the base type.',
    tags: ['exceptions', 'rethrow'],
    difficulty: 'medium',
  },
];

  {
    id: 'cpp-error-handling-016',
    front: 'What are the three exception safety guarantees?',
    back: '**No-throw guarantee**: operation never throws (e.g., destructors, swap, move operations). **Strong guarantee**: operation either succeeds completely or has no side effects (atomic commit). **Basic guarantee**: operation leaves objects in a valid state with no resource leaks on failure.',
    tags: ['exception-safety', 'guarantees'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-error-handling-017',
    front: 'What happens when an exception is thrown during stack unwinding?',
    back: 'If a destructor called during stack unwinding throws another exception and that exception propagates, `std::terminate()` is called immediately. This is why destructors should always be `noexcept` -- throwing from a destructor during unwinding is undefined and fatal.',
    tags: ['stack-unwinding', 'destructors', 'exception-safety'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-error-handling-018',
    front: 'What is exception specification and what changed in C++11?',
    back: 'Pre-C++11: `throw()` dynamic exception spec (deprecated, removed in C++17). C++11: `noexcept` replaces it. `noexcept(expr)` conditionally marks based on a compile-time boolean. `noexcept` operator (`noexcept(f())`) checks at compile time whether an expression is declared noexcept.',
    tags: ['exception-specifications', 'noexcept', 'c++11'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-error-handling-019',
    front:
      'How do you use `std::error_code` and `std::error_condition` for portable error handling?',
    back: '`std::error_code` is a platform-specific error value paired with a `std::error_category` (e.g., `std::generic_category`, `std::system_category`). `std::error_condition` is a portable abstraction. Compare across categories using `std::errc` enum values. Defined in `<system_error>`.',
    tags: ['error-codes', 'system-error', 'portability'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-error-handling-020',
    front:
      'What is the error handling strategy for move operations in the context of exception safety?',
    back: "Move constructors and move assignment operators should be `noexcept`. This allows standard containers (e.g., `std::vector` reallocation) to use moves instead of copies, which can throw. Use `std::move_if_noexcept` to conditionally move or copy based on the target's `noexcept` guarantee.",
    tags: ['move-semantics', 'exception-safety', 'noexcept'],
    difficulty: 'hard',
  },
];

<FlashcardDeck
  deckId="cpp-error-handling"
  title="C++ Error Handling Flashcards"
  description="Interactive flashcards on C++ exceptions, std::exception hierarchy, error codes vs exceptions, std::optional, std::expected, stack unwinding, and safety."
  cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]}
/>
