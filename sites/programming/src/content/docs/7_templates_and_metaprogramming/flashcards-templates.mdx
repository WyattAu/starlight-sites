---
title: 'C++ Flashcards: Templates and Metaprogramming'
description:
  'Interactive flashcards on C++ templates, SFINAE, concepts, variadic templates, fold expressions,
  type traits, CRTP, and compile-time computation.'
tags:
  - C++
  - Flashcards
---

import FlashcardDeck from '@components/FlashcardDeck';
import { flashcards1, flashcards2, flashcards3, flashcards4 } from './flashcards-templates-data';


## C++ -- Templates and Metaprogramming Flashcards

20 interactive flashcards. Press Space to flip, rate 1-4 to schedule next review.

---

  {
    id: 'cpp-templates-001',
    front: 'What is a function template and how is it instantiated?',
    back: 'A function template defines a family of functions parameterized by type: `template<typename T> T max(T a, T b) { return a > b ? a : b; }`. Instantiation occurs when called with specific types -- the compiler generates a concrete function for each unique type used.',
    tags: ['function-templates'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-templates-002',
    front: 'What is a class template and how do you define member functions?',
    back: 'A class template defines a family of classes: `template<typename T> class Stack { ... };`. Member functions defined outside the class need their own `template<>` declaration: `template<typename T> void Stack<T>::push(const T& val) { ... }`. Instantiation is triggered by use.',
    tags: ['class-templates'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-templates-003',
    front: 'What are the three kinds of template parameters?',
    back: '**Type parameters**: `template<typename T>` -- types. **Non-type parameters**: `template<int N>` -- compile-time values (integers, enums, pointers). **Template template parameters**: `template<template<typename> class Container>` -- a template as a parameter, used to accept container types generically.',
    tags: ['template-parameters'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-templates-004',
    front: 'What is perfect forwarding and what function enables it?',
    back: 'Perfect forwarding preserves the value category (lvalue/rvalue) and cv-qualifiers of arguments when passing them to another function. `std::forward<T>(arg)` casts the argument to `T&&`, which becomes `T&` (lvalue) or `T&&` (rvalue) based on reference collapsing rules. Used with forwarding references.',
    tags: ['perfect-forwarding', 'std-forward'],
    difficulty: 'easy',
  },
  {
    id: 'cpp-templates-005',
    front: 'What is `if constexpr` (C++17) and when should you use it?',
    back: '`if constexpr(cond)` is a compile-time conditional that discards the false branch during template instantiation (not just at runtime). Prevents instantiation of code that would be invalid for certain types: `if constexpr (std::is_integral_v<T>) { ... } else { ... }`. Requires `cond` to be a constant expression.',
    tags: ['if-constexpr', 'c++17'],
    difficulty: 'easy',
  },
];

  {
    id: 'cpp-templates-006',
    front: 'What is SFINAE and how does it work?',
    back: 'Substitution Failure Is Not An Error. When the compiler substitutes template arguments during overload resolution, if substitution leads to an invalid type or expression, that overload is removed from the candidate set rather than causing a compilation error. Used to constrain templates.',
    tags: ['sfinae'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-007',
    front: 'How does `std::enable_if` work for SFINAE-based constraints?',
    back: '`std::enable_if<condition, T>::type` is `T` when `condition` is true and is not defined (SFINAE) when false. Used as a default template argument or function return type to enable/disable overloads: `template<typename T, typename = std::enable_if_t<std::is_integral_v<T>>> void f(T x);`',
    tags: ['enable-if', 'sfinae'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-008',
    front: 'What are concepts (C++20) and how do they improve on SFINAE?',
    back: 'Concepts are named constraints on template parameters: `template<std::integral T> void f(T x)`. They replace SFINAE with clearer syntax, provide better error messages, enable abbreviated function template syntax (`void f(std::integral auto x)`), and allow `requires` clauses for complex constraints.',
    tags: ['concepts', 'c++20', 'constraints'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-009',
    front: 'What is the `requires` clause and where can it appear?',
    back: 'The `requires` clause (C++20) specifies constraints on template parameters. Four forms: (1) `requires` after template params: `template<typename T> requires std::integral<T> void f();`. (2) Trailing: `void f() requires std::integral<T>`. (3) Requires expression: `requires { x + y; }`. (4) Abbreviated: `void f(std::integral auto x)`.',
    tags: ['requires', 'c++20', 'constraints'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-010',
    front: 'What are variadic templates and how do you access the parameter pack?',
    back: 'Variadic templates accept a variable number of template arguments via parameter packs: `template<typename... Ts> void f(Ts... args)`. Expand with `args...` (pattern-based expansion). In C++11/14, use recursive functions or initializer lists; in C++17+, use fold expressions.',
    tags: ['variadic-templates', 'parameter-packs'],
    difficulty: 'medium',
  },
];

  {
    id: 'cpp-templates-011',
    front: 'What are fold expressions (C++17) and what operators are supported?',
    back: 'Fold expressions reduce a parameter pack with a binary operator: `(args + ...)`, `(... + args)`, `(args + ... + init)`, `(init + ... + args)`. Supported operators: `+`, `-`, `*`, `/`, `%`, `&&`, `||`, `&`, `|`, `^`, `<<`, `>>`, `==`, `!=`, `<`, `>`, `<=`, `>=`, `,`, `.*`, `->*`.',
    tags: ['fold-expressions', 'c++17'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-012',
    front: 'What is the difference between full and partial template specialization?',
    back: '**Full specialization**: all template parameters are specified: `template<> class Vector<bool> { ... };`. **Partial specialization**: some parameters are specialized, others remain generic: `template<typename T> class Vector<T*> { ... };`. Partial specializations are still templates; full ones are concrete types.',
    tags: ['specialization'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-013',
    front: 'What are type traits and how do you use them?',
    back: 'Type traits (`<type_traits>`) provide compile-time type introspection. **Query**: `std::is_integral_v<T>`, `std::is_pointer_v<T>`. **Transformation**: `std::remove_reference_t<T>`, `std::add_const_t<T>`. Use with `if constexpr`, `std::enable_if`, or concepts to write type-aware generic code.',
    tags: ['type-traits'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-014',
    front: 'What is CTAD (Class Template Argument Deduction) in C++17?',
    back: 'CTAD allows the compiler to deduce class template arguments from constructor arguments: `std::pair p{1, 2.0};` deduces `std::pair<int, double>`. Works with deduction guides (implicit or explicit). Reduces verbosity and works with `std::vector`, `std::optional`, `std::tuple`, and custom types.',
    tags: ['ctad', 'c++17'],
    difficulty: 'medium',
  },
  {
    id: 'cpp-templates-015',
    front: 'What is `std::tuple` and how do you access its elements?',
    back: '`std::tuple<Ts...>` is a fixed-size heterogeneous container. Access via `std::get<N>(t)` (index-based, compile-time), `std::get<T>(t)` (type-based), or structured bindings (C++17): `auto [x, y, z] = t;`. Defined in `<tuple>`. Works with `std::make_tuple` for construction.',
    tags: ['std-tuple'],
    difficulty: 'medium',
  },
];

  {
    id: 'cpp-templates-016',
    front: 'What is CRTP (Curiously Recurring Template Pattern) and what is it used for?',
    back: 'CRTP is when a class derives from a base class template instantiated with itself: `class Derived : public Base<Derived>`. Uses: static polymorphism (no virtual dispatch overhead), mixins, interface injection, and the Barton-Nackman trick. Enables compile-time method dispatch.',
    tags: ['crtp', 'design-patterns', 'static-polymorphism'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-templates-017',
    front: 'How does compile-time computation with templates work?',
    back: 'Template metaprogramming uses template instantiation to perform computation at compile time. Values are encoded as types, and recursion (or `constexpr` since C++11/14) drives the logic. Example: `template<int N> struct Factorial { static constexpr int value = N * Factorial<N-1>::value; };`. `constexpr` functions are now preferred.',
    tags: ['metaprogramming', 'compile-time', 'constexpr'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-templates-018',
    front:
      'How do variadic templates interact with perfect forwarding in generic function wrappers?',
    back: 'A variadic forwarding reference captures all arguments preserving their categories: `template<typename... Args> auto wrapper(Args&&... args) { return target(std::forward<Args>(args)...); }`. The pack expansion `std::forward<Args>(args)...` applies forwarding to each argument individually.',
    tags: ['variadic-templates', 'perfect-forwarding'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-templates-019',
    front: 'What is a non-type template parameter (NTTP) and what types are allowed?',
    back: 'An NTTP is a compile-time value parameter: `template<int N, typename T> struct Array { T data[N]; };`. Allowed types: integral types, enumerations, pointers/references to objects/functions, `nullptr_t`, and (C++20) floating-point types, literal class types with `constexpr` constructors.',
    tags: ['non-type-parameters', 'c++20'],
    difficulty: 'hard',
  },
  {
    id: 'cpp-templates-020',
    front: 'What is template template parameter deduction and how has it changed in C++17?',
    back: 'A template template parameter takes a template as an argument: `template<template<typename, typename> class C> void f(C<int, std::allocator<int>>&)`. Before C++17, all template parameters had to match exactly. C++17 allows deduced template template parameter matching even when parameter packs differ, reducing boilerplate.',
    tags: ['template-template-parameters', 'c++17'],
    difficulty: 'hard',
  },
];

<FlashcardDeck
  deckId="cpp-templates"
  title="C++ Templates and Metaprogramming Flashcards"
  description="Interactive flashcards on C++ templates, SFINAE, concepts, variadic templates, fold expressions, type traits, CRTP, and compile-time computation."
  cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]}
/>
