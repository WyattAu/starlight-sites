export const flashcardData = [
  {
    id: 'rust-ownership-001',
    front: "What are Rust's three ownership\nrules?",
    back: '1) Each value has exactly one owner. 2) When the owner goes out of scope, the value is\ndropped. 3) Values can be moved or copied to create new owners. The compiler enforces these rules at\ncompile time, preventing use-after-free, double-free, and dangling pointer bugs without a garbage\ncollector.',
    tags: ['rust', 'ownership'],
    difficulty: 'easy',
  },
  {
    id: 'rust-ownership-002',
    front: 'What is the\ndifference between a move and a copy in Rust?',
    back: 'A move transfers ownership of the underlying\ndata to a new variable, invalidating the old one (applicable to types without Copy). A copy (types\nimplementing the Copy trait, like i32, f64, bool, and tuples of Copy types) duplicates the bits,\nleaving both variables valid. The compiler automatically calls Clone::clone for explicit deep copies\nof non-Copy\ntypes.',
    tags: ['rust', 'ownership'],
    difficulty: 'easy',
  },
  {
    id: 'rust-borrowing-003',
    front: 'What are the\ntwo borrowing rules in Rust?',
    back: '1) You can have any number of immutable references (&T) to the\nsame data simultaneously, but no mutable references. 2) You can have exactly one mutable reference\n(&mut T), but no other references (immutable or mutable) at the same time. References must always be\nvalid -- they cannot outlive the data they point to. These rules are enforced at compile\ntime.',
    tags: ['rust', 'borrowing'],
    difficulty: 'easy',
  },
  {
    id: 'rust-borrowing-004',
    front: 'What is\nreborrowing and why does it matter?',
    back: 'Reborrowing is creating a new reference from an existing\nreference without transferring the original borrow. For example, `&*x` where x is `&mut T`, or\npassing `&x` to a function when x is `&mut T`. The reborrow has a shorter lifetime than the\noriginal. The original borrow is temporarily suspended and becomes usable again after the reborrow\nends. This is critical for writing methods that take &self or &mut self while a mutable borrow\nexists.',
    tags: ['rust', 'borrowing'],
    difficulty: 'medium',
  },
  {
    id: 'rust-lifetimes-005',
    front: 'What is\nlifetime elision and what are the three elision rules?',
    back: 'Lifetime elision allows the compiler\nto infer lifetimes in function signatures. The three rules: 1) Each parameter that is a reference\ngets its own lifetime parameter. 2) If there is exactly one input lifetime, that lifetime is\nassigned to all output references. 3) If there are multiple input lifetimes but one of them is &self\nor &mut self, the lifetime of self is assigned to all output references. If the compiler cannot\ninfer lifetimes from these rules, you must annotate them\nexplicitly.',
    tags: ['rust', 'lifetimes'],
    difficulty: 'medium',
  },
  {
    id: 'rust-lifetimes-006',
    front: 'What is\nthe difference between covariance and contravariance in lifetimes?',
    back: 'If a type T is covariant\nwith respect to a lifetime (the default for most types including &T), a longer lifetime can be used\nwhere a shorter one is expected -- you can substitute a longer-lived reference for a shorter-lived\none. Contravariance (applies to function arguments / FnMut) means a shorter lifetime can substitute\nfor a longer one -- the function that needs to accept less can be used where more is required.\nInvariance (applies to &mut T and Cell/RefCell contents) means no subtyping relationship\nexists.',
    tags: ['rust', 'lifetimes'],
    difficulty: 'hard',
  },
  {
    id: 'rust-patterns-007',
    front: 'How does\npattern matching work with enums in Rust?',
    back: 'Enums in Rust use the `match` keyword to\ndestructure variants. Each arm can bind variables from the variant’s data, use guards with `if` conditions, and use `_` as a catch-all. The match must be exhaustive -- every possible variant must be covered. Rust supports irrefutable patterns (if let, let else) for single-variant matching. Enums can carry data (tuple variants, struct variants) and can be recursive.',
    tags: ['rust', 'patterns'],
    difficulty: 'easy',
  },
  {
    id: 'rust-patterns-008',
    front: 'What are struct patterns and how do you use destructuring with structs?',
    back: 'Struct patterns let you match against the fields of a struct. `let Point { x, y } = point;` binds x and y. You can use `..` to ignore remaining fields: `let Point { x, .. } = point;`. Named-field patterns use `field: new_name` for renaming. Struct patterns work in let bindings, match arms, if let, and function parameters. Tuple structs use positional patterns: `let Color(r, _, b) = color;`.',
    tags: ['rust', 'patterns'],
    difficulty: 'medium',
  },
  {
    id: 'rust-traits-009',
    front: 'What is the difference between `impl Trait` and `dyn Trait`?',
    back: '`impl Trait` (static dispatch / generics) monomorphizes the code -- the compiler generates a specialized version for each concrete type. It has zero runtime overhead but the exact type must be known at compile time. `dyn Trait` (dynamic dispatch / trait objects) uses a fat pointer (data pointer + vtable pointer) to dispatch method calls at runtime. It allows heterogeneous collections but has a small performance cost from indirection and prevents inlining.',
    tags: ['rust', 'traits'],
    difficulty: 'medium',
  },
  {
    id: 'rust-traits-010',
    front:
      'What are associated types in traits and how do they differ from generic type parameters?',
    back: "Associated types (`type Item;` inside a trait) define a single concrete type that the implementing type chooses. Unlike generic parameters (`trait Foo<T>`), there is only one associated type per impl -- you cannot implement the trait multiple times with different associated types. This makes the API cleaner when there is a natural one-to-one relationship (e.g., Iterator's Item type). Use generics\nwhen callers need to choose the type, associated types when implementors determine\nit.",
    tags: ['rust', 'traits'],
    difficulty: 'medium',
  },
  {
    id: 'rust-traits-011',
    front: 'What is a trait object\nand what are the object safety rules?',
    back: 'A trait object (`dyn Trait`) is a fat pointer used for\ndynamic dispatch. For a trait to be object-safe, it must: 1) Not return `Self` (except in methods\nwhere Self is the receiver type). 2) Not have generic type parameters. 3) Not have any\n`where Self: Sized` clauses. These restrictions ensure the compiler can build a vtable of uniform\nfunction signatures regardless of the concrete type behind the\npointer.',
    tags: ['rust', 'traits'],
    difficulty: 'hard',
  },
  {
    id: 'rust-generics-012',
    front: 'What is\nmonomorphization and how does it affect compilation?',
    back: 'Monomorphization is the compiler process\nof generating concrete copies of generic functions and types for each type they are used with. For\nexample, `fn identity<T>(x: T) -> T` called with i32 and String produces two separate compiled\nfunctions. This results in fast runtime performance (no vtable indirection) but can increase binary\nsize (code bloat). Rust’s generics are zero-cost abstractions -- the abstraction compiles away at build time.',
    tags: ['rust', 'generics'],
    difficulty: 'medium',
  },
  {
    id: 'rust-generics-013',
    front: 'What are trait bounds and how do you specify them?',
    back: 'Trait bounds constrain generic type parameters to types that implement specific traits. Syntax: `fn print<T: Display>(item: T)` or `fn print<T>(item: T) where T: Display`. Multiple bounds use `+`: `T: Display + Clone`. Bounds can be on functions, structs, enums, and impl blocks. The `?Sized` bound allows a type parameter to be dynamically sized. Bounds enable the generic code to call methods from the bounded traits.',
    tags: ['rust', 'generics'],
    difficulty: 'easy',
  },
  {
    id: 'rust-error-handling-014',
    front: 'How do Result and Option work in Rust error handling?',
    back: 'Option<T> represents a value that may be absent: `Some(T)` or `None`. Result<T, E> represents an operation that may fail: `Ok(T)` or `Err(E)`. Both are enums and must be explicitly handled -- you cannot use the inner value without unwrapping. The `?` operator propagates errors/None by returning early from the function. Pattern matching, unwrap(), expect(), and combinators like map(), and_then(), unwrap_or() are the primary ways to handle them.',
    tags: ['rust', 'error-handling'],
    difficulty: 'easy',
  },
  {
    id: 'rust-error-handling-015',
    front: 'What does the `?` operator do and where can it be used?',
    back: 'The `?` operator unwraps Ok/Some values and propagates Err/None by returning early from the enclosing function. It performs automatic conversion via From, so a function returning Result<T, E1> can use ? on a Result<T, E2> if E2: Into<E1>. It can only be used in functions whose return type is Option, Result, or a type that implements the Try trait (stabilized in Rust 1.82). It is the idiomatic way to chain fallible operations.',
    tags: ['rust', 'error-handling'],
    difficulty: 'medium',
  },
  {
    id: 'rust-concurrency-016',
    front: 'What are Send and Sync traits in Rust?',
    back: "Send means a type can be safely transferred to another thread (ownership can move across thread boundaries). Sync means a type can be safely shared between threads via shared references (&T). Most types are Send and Sync by default. Raw pointers are neither. Rc and RefCell are not Send/Sync (non-atomic). Arc<T> is Send + Sync when T: Send + Sync. Mutex<T> is Sync when T: Send. The compiler auto-implements these traits based on the type's\nfields.",
    tags: ['rust', 'concurrency'],
    difficulty: 'medium',
  },
  {
    id: 'rust-concurrency-017',
    front: 'What is\nthe difference between channels (mpsc) and Arc for sharing data?',
    back: 'mpsc (multi-producer,\nsingle-consumer) channels send cloned or moved values between threads through message passing -- no\nshared memory. Arc (Atomic Reference Counted) enables shared ownership of the same data across\nmultiple threads. Channels enforce a pipeline/conveyor pattern: data flows one direction. Arc\nenables concurrent reads (with Mutex/RwLock for writes). Channels avoid lock contention; Arc avoids\ndata copies. Use channels for communication, Arc for shared\nstate.',
    tags: ['rust', 'concurrency'],
    difficulty: 'medium',
  },
  {
    id: 'rust-macros-018',
    front:
      'What is the\ndifference between declarative macros (macro_rules!) and procedural macros?',
    back: 'Declarative\nmacros (macro_rules!) use pattern matching against token trees to generate code at expansion time.\nThey are simpler but limited -- they operate on raw syntax tokens, not AST nodes, making complex\ntransformations difficult. Procedural macros (derive, attribute, function-like) are Rust functions\nthat receive token streams and return token streams. They have full AST access via syn, can perform\narbitrary computation, but require a separate proc-macro crate compiled as a dynamic\nlibrary.',
    tags: ['rust', 'macros'],
    difficulty: 'hard',
  },
  {
    id: 'rust-unsafe-019',
    front: 'What operations\nrequire an unsafe block in Rust?',
    back: 'Five operations require unsafe: 1) Dereferencing raw\npointers (*const T, *mut T). 2) Calling unsafe functions or methods. 3) Accessing or modifying\nmutable static variables. 4) Implementing unsafe traits (e.g., Send, Sync). 5) Accessing union\nfields. Unsafe does not disable the borrow checker -- it only gives you access to these five\noperations. The goal is to minimize unsafe and encapsulate it behind safe\nabstractions.',
    tags: ['rust', 'unsafe'],
    difficulty: 'medium',
  },
  {
    id: 'rust-unsafe-020',
    front: 'What are raw\npointers and when should you use them?',
    back: 'Raw pointers are `*const T` (immutable) and `*mut T`\n(mutable). Unlike references, they have no ownership semantics, no lifetime constraints, can be\nnull, and can alias freely. Creating raw pointers is safe; dereferencing them requires unsafe. Use\nthem for FFI (interacting with C libraries), building data structures the borrow checker cannot\nverify (like graphs with parent-child pointers), or when interfacing with hardware. Always wrap\nunsafe pointer operations in safe abstractions.',
    tags: ['rust', 'unsafe'],
    difficulty: 'medium',
  },
]
