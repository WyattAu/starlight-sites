export const flashcardData = [
  {
    id: 'swift-let-001',
    front: 'What is the difference between `let` and `var` in Swift?',
    back: '`let` declares a constant (immutable) -- once assigned, the value cannot change. `var` declares a variable (mutable) that can be reassigned. Prefer `let` whenever possible for safety and compiler optimization. Constants can be assigned later but only once.',
    tags: ['swift', 'types'],
    difficulty: 'easy',
  },
  {
    id: 'swift-var-002',
    front: 'When should you use `var` instead of `let` in Swift?',
    back: 'Use `var` when the value must change after initialization: loop counters, accumulators, state properties, and UI bindings. Everything else should be `let`. Swift encourages immutability for predictable code and thread safety.',
    tags: ['swift', 'types'],
    difficulty: 'easy',
  },
  {
    id: 'swift-optionals-003',
    front: 'What is an optional in Swift and how do you unwrap it safely?',
    back: 'An optional (`T?`) holds either a value of type `T` or `nil`. Safe unwrapping methods: `if let` (optional binding), `guard let` (early exit), `??` (nil coalescing), and `?.` (optional chaining). Force unwrapping with `!` crashes if nil -- avoid it.',
    tags: ['swift', 'optionals'],
    difficulty: 'easy',
  },
  {
    id: 'swift-protocols-004',
    front: 'What is a protocol in Swift and how does it differ from a class?',
    back: 'A protocol defines a blueprint of requirements (methods, properties, requirements) that conforming types must implement. Unlike a class, it cannot store properties or provide implementations (except protocol extensions). Structs, enums, and classes can all conform to protocols. Multiple conformance is allowed.',
    tags: ['swift', 'protocols'],
    difficulty: 'medium',
  },
  {
    id: 'swift-extensions-005',
    front: 'What are extensions in Swift and what can they add?',
    back: 'Extensions add new functionality to existing types (including types you do not own). You can add computed properties, methods, initializers, subscripts, and nested types. Extensions cannot add stored properties or designated initializers. They are resolved at compile time.',
    tags: ['swift', 'extensions'],
    difficulty: 'medium',
  },
  {
    id: 'swift-generics-006',
    front: 'What are generics in Swift and why are they useful?',
    back: 'Generics write flexible, reusable code that works with any type. Example: `func swapTwo<T>(_ a: inout T, _ b: inout T)`. Type constraints restrict which types can be used: `<T: Comparable>`. Generic types like `Array<T>` and `Dictionary<K, V>` are core to the standard library.',
    tags: ['swift', 'generics'],
    difficulty: 'medium',
  },
  {
    id: 'swift-enums-007',
    front: 'What are enums in Swift and what makes them powerful?',
    back: 'Enums define a group of related values. Swift enums support: associated values (`case temperature(Double)`), raw values (`: String`), methods, computed properties, and protocol conformance. They are exhaustive -- the compiler ensures all cases are handled in switch statements.',
    tags: ['swift', 'enums'],
    difficulty: 'medium',
  },
  {
    id: 'swift-structs-008',
    front: 'What is a struct in Swift and how does it differ from a class?',
    back: 'Structs are value types -- copied on assignment, no inheritance, get automatic memberwise initializer. They are the default choice in Swift. Use structs for data models, views, and most types. Choose classes only for inheritance, identity semantics, or Objective-C interoperability.',
    tags: ['swift', 'structs'],
    difficulty: 'medium',
  },
  {
    id: 'swift-classes-009',
    front: 'When should you use a class instead of a struct in Swift?',
    back: 'Use a class when you need: inheritance, reference semantics (shared identity), deinitializers, or Objective-C bridging. Classes are appropriate for delegate objects, view controllers, and long-lived state. For everything else, prefer structs for their safety and performance characteristics.',
    tags: ['swift', 'classes'],
    difficulty: 'medium',
  },
  {
    id: 'swift-arc-010',
    front: 'How does Automatic Reference Counting (ARC) work in Swift?',
    back: 'ARC tracks strong references to each class instance. When the count reaches zero, the instance is deallocated. Retain cycles occur when two objects hold strong references to each other. Break cycles with `weak` (becomes nil when deallocated) or `unowned` (must not outlive the referenced object).',
    tags: ['swift', 'memory', 'arc'],
    difficulty: 'medium',
  },
  {
    id: 'swift-closures-011',
    front: 'What are closures in Swift and what is capture semantics?',
    back: 'Closures are self-contained blocks of code that capture values from their surrounding context. A closure captures references to variables, not copies. This can cause retain cycles. Use `[weak self]` or `[unowned self]` in capture lists to break cycles. Closures are reference types.',
    tags: ['swift', 'closures'],
    difficulty: 'medium',
  },
  {
    id: 'swift-computed-012',
    front: 'What are computed properties in Swift?',
    back: 'Computed properties calculate their value on access using `get` and optionally `set`. They have no stored backing -- they derive value from other properties or external state. Useful for calculated fields, validation, and derived data. Can be defined on classes, structs, and enums.',
    tags: ['swift', 'properties'],
    difficulty: 'easy',
  },
  {
    id: 'swift-property-observers-013',
    front: 'What are property observers in Swift?',
    back: "`willSet` and `didSet` observe changes to a property\'s value. `willSet` runs before the new value is stored; `didSet` runs after. They work on stored properties and property declarations (not `lazy` or `inout` parameters). Common use: triggering UI updates, logging, or validation.",
    tags: ['swift', 'properties'],
    difficulty: 'medium',
  },
  {
    id: 'swift-result-builders-014',
    front: 'What are result builders in Swift?',
    back: 'Result builders (formerly function builders) enable declarative syntax for building complex return values. They use `@resultBuilder` attribute and define static methods like `buildBlock`, `buildIf`, `buildArray`. SwiftUI uses them extensively: `@ViewBuilder` creates view bodies. Custom builders can build any type.',
    tags: ['swift', 'result-builders'],
    difficulty: 'hard',
  },
  {
    id: 'swift-async-await-015',
    front: 'How does async/await work in Swift?',
    back: 'Mark a function `async` to make it asynchronous. It can suspend at `await` points without blocking threads. `await` pauses until the async operation completes. Structured concurrency with `Task` manages child tasks. `TaskGroup` runs concurrent child tasks. Async sequences use `for try await in`.',
    tags: ['swift', 'concurrency', 'async'],
    difficulty: 'medium',
  },
]
