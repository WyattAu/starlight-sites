---
title: 'University CS Flashcards: Algorithms'
description:
  '20 interactive flashcards on complexity classes, dynamic programming, NP-completeness, graph
  algorithms, string matching, and FFT.'
tags: [University, Computer Science, Flashcards]
---

import FlashcardDeck from '@components/FlashcardDeck';
import { flashcards1, flashcards2, flashcards3, flashcards4 } from './flashcards-algorithms-data';

## Computer Science -- Algorithms Flashcards
20 interactive flashcards for university-level Algorithms. Press Space to flip, rate 1-4.
---

  {
    id: 'uni-cs-algorithms-001',
    front: 'Define Big-O, Big-Omega, and Big-Theta asymptotic notations.',
    back: 'Big-O: f(n) = O(g(n)) if ∃c, n₀ such that f(n) ≤ cg(n) for all n ≥ n₀ (asymptotic upper bound). Big-Omega: f(n) = Ω(g(n)) if ∃c, n₀ such that f(n) ≥ cg(n) for all n ≥ n₀ (lower bound). Big-Theta: f(n) = Θ(g(n)) if f = O(g) and f = Ω(g) (tight bound, meaning f and g grow at the same rate up to constants).',
    tags: ['complexity'],
    difficulty: 'easy',
  },
  {
    id: 'uni-cs-algorithms-002',
    front: 'List common time complexity classes and give an example algorithm for each.',
    back: 'O(1): array access. O(log n): binary search. O(n): linear scan. O(n log n): merge sort, heap sort. O(n²): bubble sort, insertion sort (worst case). O(n³): matrix multiplication (naive), Floyd-Warshall. O(2ⁿ): subset enumeration. O(n!): generating all permutations. In practice, O(n log n) is often the target for sorting and searching problems.',
    tags: ['complexity'],
    difficulty: 'easy',
  },
  {
    id: 'uni-cs-algorithms-003',
    front: 'State the comparison-based sorting lower bound of Ω(n log n).',
    back: 'Any comparison-based sorting algorithm requires Ω(n log n) comparisons in the worst case. Proof via decision tree: n! possible orderings, a binary tree of height h has at most 2ʰ leaves, so 2ʰ ≥ n! implies h ≥ log₂(n!) = Ω(n log n). Algorithms achieving this bound: merge sort, heap sort, quicksort (expected). Non-comparison sorts (counting, radix, bucket) can beat O(n log n).',
    tags: ['sorting'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-algorithms-004',
    front: 'State the Master Theorem for divide-and-conquer recurrences.',
    back: 'For T(n) = aT(n/b) + f(n) where a ≥ 1, b > 1: let d = log_b(a). (1) If f(n) = O(n^c) where c < d: T(n) = Θ(n^d). (2) If f(n) = Θ(n^d log^k n): T(n) = Θ(n^d log^(k+1) n). (3) If f(n) = Ω(n^c) where c > d, and af(n/b) ≤ kf(n) for some k < 1: T(n) = Θ(f(n)). Covers most standard recurrences like merge sort T(n) = 2T(n/2) + O(n).',
    tags: ['divide-and-conquer'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-algorithms-005',
    front: 'Define dynamic programming and its two key properties.',
    back: 'Dynamic programming solves problems by: (1) Optimal substructure: an optimal solution contains optimal solutions to subproblems. (2) Overlapping subproblems: the same subproblems are solved multiple times. DP stores solutions to subproblems in a table (bottom-up) or uses memoisation (top-down) to avoid redundant computation. Examples: Fibonacci, shortest paths, knapsack, edit distance.',
    tags: ['dynamic-programming'],
    difficulty: 'easy',
  },
];

  {
    id: 'uni-cs-algorithms-006',
    front: 'Describe the greedy algorithm paradigm and the greedy choice property.',
    back: "A greedy algorithm makes the locally optimal choice at each step, hoping this leads to a globally optimal solution. The greedy choice property: a locally optimal choice is part of some globally optimal solution. Matroid framework: greedy works optimally for matroid structures. Examples: Huffman coding, activity selection, Kruskal's MST. Greedy does not always yield optimal solutions (e.g. 0-1 knapsack).",
    tags: ['greedy'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-algorithms-007',
    front: 'Define NP-completeness and the P vs NP question.',
    back: 'P: problems solvable in polynomial time (deterministic). NP: problems verifiable in polynomial time (given a certificate). NP-complete: problems in NP to which every NP problem can be reduced in polynomial time. P vs NP: is P = NP? (open problem; believed to be false). If any NP-complete problem is in P, then P = NP. NP-hard: at least as hard as every NP problem (may not be in NP).',
    tags: ['complexity-theory'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-algorithms-008',
    front: 'Define polynomial-time reduction and explain its role in proving NP-completeness.',
    back: 'A problem A is polynomial-time reducible to B (A ≤ₚ B) if there is a polynomial-time computable function f mapping instances of A to instances of B such that x ∈ A iff f(x) ∈ B. Role in NP-completeness: to prove B is NP-hard, reduce a known NP-hard problem A to B. If B ∈ NP, then B is NP-complete. Transitivity: if A ≤ₚ B and B ≤ₚ C, then A ≤ₚ C.',
    tags: ['complexity-theory'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-algorithms-009',
    front: 'Compare BFS, DFS, Dijkstra, Bellman-Ford, and Floyd-Warshall algorithms.',
    back: 'BFS: unweighted shortest path, O(V+E), queue-based. DFS: graph exploration, O(V+E), stack-based. Dijkstra: single-source shortest path (non-negative edges), O((V+E)log V) with min-heap. Bellman-Ford: handles negative edges, detects negative cycles, O(VE). Floyd-Warshall: all-pairs shortest path, O(V³), handles negative edges (no negative cycles). All compute shortest paths with different constraints and efficiencies.',
    tags: ['graph-algorithms'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-algorithms-010',
    front: "Describe Kruskal's and Prim’s algorithms for minimum spanning trees.",
    back: 'Kruskal: sort edges by weight, add edges one by one using Union-Find if they connect different components. O(E log E) = O(E log V). Correctness follows from the cut property. Prim: grow MST from a starting vertex, always add the lightest edge connecting the tree to a non-tree vertex. O((V+E) log V) with min-heap. Both produce an MST. Kruskal is edge-focused; Prim is vertex-focused.',
    tags: ['graph-algorithms'],
    difficulty: 'medium',
  },
];

  {
    id: 'uni-cs-algorithms-011',
    front: 'Describe the KMP and Rabin-Karp string matching algorithms.',
    back: 'KMP: preprocesses the pattern to compute the failure function (longest proper prefix that is also suffix), achieving O(n + m) time by avoiding re-examining characters. Rabin-Karp: uses hashing to compare pattern with text substrings in O(1) expected time per position, O(n + m) expected overall, but O(nm) worst case. Both avoid the O(nm) worst case of naive matching.',
    tags: ['string-algorithms'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-algorithms-012',
    front: 'Describe amortized analysis and the potential method.',
    back: 'Amortized analysis gives the average cost per operation over a sequence, even when individual operations are expensive. Potential method: define a potential function Φ mapping data states to non-negative reals. Amortized cost of i-th operation = actual cost cᵢ + Φᵢ − Φᵢ₋₁. Total: Σ aᵢ = Σ cᵢ + Φₙ − Φ₀ ≥ Σ cᵢ (since Φ ≥ 0). Application: dynamic array resizing has O(1) amortised insertion cost.',
    tags: ['amortized-analysis'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-algorithms-013',
    front: 'Define approximation algorithms and state key performance guarantees.',
    back: 'An approximation algorithm for an NP-hard optimisation problem runs in polynomial time with a provable guarantee on solution quality. For minimisation: algorithm returns solution of cost ≤ α · OPT. For maximisation: cost ≥ (1/α) · OPT (or ≥ α · OPT, α < 1). PTAS: for any ε > 0, achieves (1+ε)-approximation in poly(n, 1/ε). FPTAS: O(1/ε)·poly(n). Examples: 2-approx for Vertex Cover, 1.5-approx for TSP (metric).',
    tags: ['approximation'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-algorithms-014',
    front: 'Distinguish Las Vegas and Monte Carlo randomised algorithms.',
    back: 'Las Vegas: always correct, running time is random. Example: randomised quicksort (expected O(n log n)). Monte Carlo: always terminates in bounded time, output may be incorrect with some probability. Example: Miller-Rabin primality test. One-sided error Monte Carlo: always correct in one direction (yes/no) but may err in the other. Amplification: repeating a Monte Carlo algorithm reduces error exponentially.',
    tags: ['randomised-algorithms'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-algorithms-015',
    front: 'Describe the Boolean circuit model of computation.',
    back: 'A Boolean circuit is a directed acyclic graph where input nodes are variables, internal nodes are AND/OR/NOT gates, and one output node. Circuit Cₙ has n input bits. A language L is decided by a circuit family {Cₙ} if x ∈ L iff C_|x|(x) = 1. Circuit complexity: minimum size (number of gates) of circuits computing a function. P/poly: languages decidable by polynomial-size circuit families. Important: P ⊆ P/poly, and P/poly contains undecidable languages.',
    tags: ['models-of-computation'],
    difficulty: 'hard',
  },
];

  {
    id: 'uni-cs-algorithms-016',
    front: 'Describe streaming algorithms and their constraints.',
    back: 'Streaming algorithms process a sequence of data items making only one (or few) passes, using space sublinear in the input size (often polylogarithmic). Key examples: majority element (Boyer-Moore), frequency estimation (Count-Min sketch), distinct elements (Flajolet-Martin, HyperLogLog), heavy hitters. Trade-off: limited space vs. approximation quality. Lower bounds via communication complexity.',
    tags: ['streaming'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-algorithms-017',
    front: 'Describe the Fast Fourier Transform (FFT) and its computational significance.',
    back: 'FFT computes the Discrete Fourier Transform (DFT) of n points in O(n log n) time instead of O(n²) by exploiting the divide-and-conquer structure: DFTₙ splits into DFTₙ/₂ on even-indexed and odd-indexed elements (Cooley-Tukey). Applications: polynomial multiplication in O(n log n), signal processing, image compression (JPEG), convolution, pattern matching. The DFT converts between time and frequency domains.',
    tags: ['fft'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-algorithms-018',
    front: 'Describe hash tables and the key trade-offs between chaining and open addressing.',
    back: 'Hash table: maps keys to array indices via hash function h(k). Expected O(1) insert, search, delete. Chaining: each slot holds a linked list of colliding elements. Load factor α = n/m can exceed 1. Open addressing: all elements in the array; collision resolved by probing (linear, quadratic, double hashing). Load factor α must be < 1. Chaining is simpler and degrades gracefully; open addressing has better cache performance.',
    tags: ['data-structures'],
    difficulty: 'easy',
  },
  {
    id: 'uni-cs-algorithms-019',
    front: 'Describe binary heaps and their use in priority queues.',
    back: "A binary min-heap is a complete binary tree where each node ≤ its children. Stored in an array: parent(i) = ⌊i/2⌋, left(i) = 2i, right(i) = 2i+1. Operations: insert O(log n), extract-min O(log n), peek O(1), build-heap O(n). Used for: priority queues, Dijkstra's algorithm, Huffman coding, heap sort. A max-heap reverses the ordering. Variants: d-ary heaps, Fibonacci heaps (amortised O(1) decrease-key).",
    tags: ['data-structures'],
    difficulty: 'easy',
  },
  {
    id: 'uni-cs-algorithms-020',
    front: 'Describe the concept of online algorithms and competitive analysis.',
    back: 'An online algorithm processes input piece by piece, making irrevocable decisions without seeing future input (e.g. paging, caching). Competitive analysis: compare an online algorithm ALG to the optimal offline algorithm OPT. ALG is c-competitive if for every input sequence, cost(ALG) ≤ c · cost(OPT) + k. Example: LRU is k-competitive for paging (k = cache size). Competitive ratio provides a worst-case guarantee without probabilistic assumptions.',
    tags: ['online-algorithms'],
    difficulty: 'medium',
  },
];
<FlashcardDeck deckId="uni-cs-algorithms" title="University CS -- Algorithms" description="Complexity, dynamic programming, NP-completeness, graph algorithms, string matching" cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]} />;
