---
title: 'University CS Flashcards: Theory of Computation'
description:
  '20 interactive flashcards on automata, formal languages, Turing machines, decidability, P vs NP,
  and complexity classes for exam preparation.'
tags: [University, Computer Science, Flashcards]
---

import FlashcardDeck from '@components/FlashcardDeck';
import { flashcards1, flashcards2, flashcards3, flashcards4 } from './flashcards-theory-data';

## Computer Science -- Theory of Computation Flashcards
20 interactive flashcards for university-level Theory of Computation. Press Space to flip, rate 1-4.
---

  {
    id: 'uni-cs-theory-001',
    front: 'Define a Deterministic Finite Automaton (DFA).',
    back: 'A DFA is a 5-tuple M = (Q, Σ, δ, q₀, F) where: Q is a finite set of states, Σ is the input alphabet, δ: Q × Σ → Q is the transition function, q₀ ∈ Q is the start state, F ⊆ Q is the set of accepting states. For each state and input symbol, exactly one transition is defined. A string w is accepted iff processing w from q₀ ends in a state in F. The language L(M) = {w ∈ Σ* : M accepts w}.',
    tags: ['automata'],
    difficulty: 'easy',
  },
  {
    id: 'uni-cs-theory-002',
    front: 'Define a Non-deterministic Finite Automaton (NFA) and contrast with DFA.',
    back: 'An NFA N = (Q, Σ, δ, q₀, F) where δ: Q × (Σ ∪ {ε}) → P(Q) allows: (1) Multiple transitions for the same symbol (non-deterministic choice). (2) ε-transitions (transitions without consuming input). A string is accepted if some computation path leads to an accepting state. Every NFA has an equivalent DFA (subset construction): the DFA state set is P(Q), potentially up to 2^|Q| states.',
    tags: ['automata'],
    difficulty: 'easy',
  },
  {
    id: 'uni-cs-theory-003',
    front: 'State the equivalence of DFA and NFA.',
    back: 'For every NFA N, there exists a DFA D such that L(N) = L(D). Construction (subset construction): each DFA state corresponds to a set of NFA states. δ_D(S, a) = ∪(q∈S) δ_N(q, a). Start state: ε-closure of {q₀}. Accepting states: any DFA state containing an NFA accepting state. Exponential worst case (2^|Q| states) but often practical. DFA and NFA recognise exactly the regular languages.',
    tags: ['automata'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-theory-004',
    front: 'Define regular expressions and state their equivalence to finite automata.',
    back: "Regular expressions over Σ are defined inductively: ∅ is a regex, ε is a regex, a is a regex for each a ∈ Σ. If R₁, R₂ are regexes, then (R₁ ∪ R₂), (R₁ · R₂), and (R₁*) are regexes. Kleene's theorem: a language is regular iff it can be described by a regular expression. Every regex has an equivalent NFA (Thompson’s construction) and vice versa (state elimination method). Regex operations: union (+), concatenation, Kleene star (*).",
    tags: ['regular-languages'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-theory-005',
    front: 'State the Pumping Lemma for regular languages and explain its use.',
    back: 'If L is regular, then ∃p (pumping length) such that for any s ∈ L with |s| ≥ p, s can be split as s = xyz where: (1) |xy| ≤ p, (2) |y| > 0, (3) xyⁱz ∈ L for all i ≥ 0. Use: to prove a language is NOT regular, show that for any choice of p, there exists s ∈ L that violates the conditions. Examples of non-regular: {aⁿbⁿ}, {ww}, {aᵖ : p prime}. The lemma is necessary but not sufficient for regularity.',
    tags: ['regular-languages'],
    difficulty: 'medium',
  },
];

  {
    id: 'uni-cs-theory-006',
    front: 'Define a Context-Free Grammar (CFG) and its components.',
    back: 'A CFG G = (V, Σ, R, S) where: V is a finite set of variables (non-terminals), Σ is the terminal alphabet (V ∩ Σ = ∅), R is a finite set of production rules of the form A → α where A ∈ V and α ∈ (V ∪ Σ)*, S ∈ V is the start variable. A string w ∈ Σ* is derived if S ⇒* w via a sequence of productions (replacing one variable at a time). The language L(G) = {w ∈ Σ* : S ⇒* w}.',
    tags: ['context-free'],
    difficulty: 'easy',
  },
  {
    id: 'uni-cs-theory-007',
    front: 'Define Chomsky Normal Form (CNF) for context-free grammars.',
    back: 'A CFG is in CNF if every production is of one of three forms: (1) A → BC (two variables), (2) A → a (single terminal), or (3) S → ε (only the start symbol, and S does not appear on the right of any rule). Every CFG has an equivalent grammar in CNF. Transformation steps: eliminate ε-productions (except S → ε), eliminate unit productions (A → B), convert remaining rules. CNF is used in the CYK parsing algorithm (O(n³)).',
    tags: ['context-free'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-theory-008',
    front: 'Define a Pushdown Automaton (PDA) and its relation to CFLs.',
    back: 'A PDA M = (Q, Σ, Γ, δ, q₀, ⊔, F) where Γ is the stack alphabet, ⊔ is the initial stack symbol. δ: Q × (Σ ∪ {ε}) × (Γ ∪ {ε}) → P(Q × (Γ ∪ {ε})) specifies transitions based on current state, input symbol, and stack top. A string is accepted if the PDA can reach an accepting state with empty input (accept-by-state) or empty stack (accept-by-empty-stack). A language is context-free iff some PDA recognises it.',
    tags: ['context-free'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-theory-009',
    front: 'State the Pumping Lemma for context-free languages.',
    back: 'If L is a CFL, then ∃p such that for any s ∈ L with |s| ≥ p, s can be split as s = uvxyz where: (1) |vy| > 0, (2) |vxy| ≤ p, (3) uvⁱxyⁱz ∈ L for all i ≥ 0. Use: to prove L is not context-free. Key difference from regular pumping lemma: two pumped substrings (v and y) which can be pumped independently. Examples of non-CFL: {aⁿbⁿcⁿ}, {ww}. Again necessary but not sufficient.',
    tags: ['context-free'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-theory-010',
    front: 'Define a Turing machine (TM) and its components.',
    back: 'A TM M = (Q, Σ, Γ, δ, q₀, q_accept, q_reject) where: Q is the state set, Γ is the tape alphabet (blank symbol ⊔ ∈ Γ, Σ ⊂ Γ), δ: Q × Γ → Q × Γ × {L, R} (read, write, move left or right), q₀ is start state, q_accept and q_reject are halting states. The TM has an infinite tape, a read/write head, and a finite control. It reads/writes, moves left/right, and transitions based on current state and tape symbol. Accepts by reaching q_accept.',
    tags: ['turing-machines'],
    difficulty: 'easy',
  },
];

  {
    id: 'uni-cs-theory-011',
    front: 'State the Church-Turing thesis.',
    back: "Any function that can be computed by an effective procedure (algorithm) can be computed by a Turing machine. This is a thesis, not a theorem -- it cannot be proved because 'effective procedure' is an informal concept. Evidence: many equivalent models (TMs, lambda calculus, μ-recursive functions, Post systems, RAM machines) all define the same class of computable functions. The thesis establishes TMs as the standard model of computation.",
    tags: ['computability'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-theory-012',
    front: 'Define decidability and semi-decidability (recursive vs recursively enumerable).',
    back: 'A language L is decidable (recursive) if there is a TM that halts on all inputs and accepts exactly L (always gives a yes/no answer). L is semi-decidable (recursively enumerable, r.e.) if there is a TM that accepts exactly L, but may loop forever on strings not in L. Every decidable language is r.e. A language is co-r.e. if its complement is r.e. L is decidable iff both L and its complement are r.e.',
    tags: ['computability'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-theory-013',
    front: 'Prove the undecidability of the Halting Problem.',
    back: 'Halting Problem: HALT = {(M, w) : TM M halts on input w}. Proof by contradiction (diagonalisation): assume a decider H for HALT exists. Construct a TM D that on input (M): runs H on (M, M); if H says M halts on M, D loops forever; if H says M does not halt on M, D halts. Then D(D) halts iff D(D) does not halt -- contradiction. Therefore HALT is undecidable.',
    tags: ['computability'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-theory-014',
    front: "State Rice's theorem.",
    back: "Any non-trivial semantic property of the language recognised by a Turing machine is undecidable. A property is semantic if it depends only on the language L(M), not on the specific machine M. Non-trivial: some TMs have the property and some do not. Examples of undecidable by Rice's: is L(M) empty? Is L(M) = Σ*? Is L(M) regular? Is L(M) finite? These are all non-trivial semantic properties.",
    tags: ['computability'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-theory-015',
    front: 'Define P and NP complexity classes and the P vs NP question.',
    back: 'P = {L : L is decidable by a deterministic TM in polynomial time O(nᵏ)}. NP = {L : L is decidable by a non-deterministic TM in polynomial time, or equivalently, there exists a polynomial-time verifiable certificate for membership}. P vs NP: does P = NP? If P = NP, then every problem whose solution can be verified in polynomial time can also be solved in polynomial time. Believed to be false, but unproven.',
    tags: ['complexity'],
    difficulty: 'medium',
  },
];

  {
    id: 'uni-cs-theory-016',
    front: 'Define NP-completeness via Cook-Levin theorem and polynomial-time reductions.',
    back: "Cook-Levin theorem: SAT (Boolean satisfiability) is NP-complete. A language L is NP-complete if: (1) L ∈ NP, and (2) every language in NP polynomial-time reduces to L. To prove L is NP-complete: show L ∈ NP and reduce a known NP-complete problem to L (using SAT, 3-SAT, VERTEX-COVER, etc.). NP-complete problems are the 'hardest' problems in NP: if any one is in P, then P = NP.",
    tags: ['complexity'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-theory-017',
    front: 'List and define the main complexity classes P, NP, co-NP, PSPACE, EXPTIME.',
    back: 'P: polynomial-time deterministic. NP: polynomial-time non-deterministic (verifiable). co-NP: complements of NP languages (complements verifiable). PSPACE: polynomial-space (includes NP, co-NP). EXPTIME: exponential-time deterministic. Known inclusions: P ⊆ NP ∩ co-NP ⊆ NP ⊆ PSPACE ⊆ EXPTIME. PSPACE ⊂ EXPTIME (proved). P ≠ EXPTIME. Whether any of P ⊂ NP, NP ⊂ PSPACE is strict is open.',
    tags: ['complexity'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-theory-018',
    front: 'Define Boolean satisfiability (SAT) and its significance.',
    back: 'SAT = {φ : φ is a satisfiable Boolean formula in CNF}. A formula φ is satisfiable if some assignment of truth values to variables makes φ true. Cook-Levin (1971): SAT is NP-complete -- the first problem proven NP-complete. 3-SAT (each clause has exactly 3 literals) is also NP-complete and is often used as the starting point for NP-completeness reductions. k-SAT for k ≥ 3 is NP-complete; 2-SAT is in P.',
    tags: ['complexity'],
    difficulty: 'medium',
  },
  {
    id: 'uni-cs-theory-019',
    front:
      'Distinguish recursive (decidable) and recursively enumerable (semi-decidable) languages.',
    back: 'Recursive (decidable): TM always halts, correctly accepts/rejects. Complement of a recursive language is recursive. R.e. (semi-decidable): TM accepts strings in L, may loop on strings not in L. The complement of an r.e. language is not necessarily r.e. A language is recursive iff both it and its complement are r.e. Examples: HALT is r.e. but not recursive. Σ* and ∅ are recursive. The halting set and the set of provable statements in first-order logic are r.e. but not recursive.',
    tags: ['computability'],
    difficulty: 'hard',
  },
  {
    id: 'uni-cs-theory-020',
    front: 'Define an oracle Turing machine and oracle complexity classes.',
    back: "An oracle Turing machine Mᴼ has access to an oracle set O ⊆ Σ*: it can query 'is x ∈ O?' in one step, receiving a yes/no answer. This models computation with access to a subroutine solving a potentially hard problem. Oracle classes: Pᴼ (P with oracle O), NPᴼ. Baker-Gill-Solovay theorem: there exist oracles A, B such that Pᴬ = NPᴬ and Pᴮ ≠ NPᴮ, showing that relativisation techniques alone cannot resolve P vs NP.",
    tags: ['advanced-complexity'],
    difficulty: 'hard',
  },
];
<FlashcardDeck deckId="uni-cs-theory" title="University CS -- Theory of Computation" description="Automata, formal languages, Turing machines, decidability, complexity classes" cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]} />;
