---
title: 'University Maths Flashcards: ODEs'
description:
  '20 interactive flashcards on ordinary differential equations: solution methods, Laplace
  transforms, existence theorems, and numerical methods.'
tags: [University, Mathematics, Flashcards]
---

import FlashcardDeck from '@components/FlashcardDeck';
import { flashcards1, flashcards2, flashcards3, flashcards4 } from './12_flashcards-odes-data';

## Mathematics -- Ordinary Differential Equations Flashcards
20 interactive flashcards for university-level ODEs. Press Space to flip, rate 1-4.
---

  {
    id: 'uni-maths-odes-001',
    front: 'Define an ordinary differential equation (ODE) and its order.',
    back: "An ODE is an equation involving a function of a single independent variable and its derivatives: F(t, y, y', y'', ..., y⁽ⁿ⁾) = 0. The order is n, the highest derivative that appears. A first-order ODE involves only y'. The general solution of an nth-order ODE contains n arbitrary constants.",
    tags: ['definitions'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-odes-002',
    front:
      'Define an initial value problem (IVP) and distinguish it from a boundary value problem.',
    back: 'An IVP specifies the ODE plus initial conditions at a single point: y⁽ⁿ⁾(t₀) = y₀, y⁽ⁿ⁻¹⁾(t₀) = y₁, ..., y(t₀) = yₙ. A boundary value problem (BVP) specifies conditions at two or more distinct points. IVPs typically have unique solutions locally (by existence-uniqueness theorems); BVPs may have zero, one, or many solutions.',
    tags: ['definitions'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-odes-003',
    front: 'Describe the method of separation of variables for first-order ODEs.',
    back: 'Applicable when dy/dt = f(t)g(y). Separate: dy/g(y) = f(t) dt, then integrate both sides: ∫ dy/g(y) = ∫ f(t) dt + C. Must account for equilibrium solutions where g(y) = 0. Example: dy/dt = ty becomes dy/y = t dt, so ln|y| = t²/2 + C, giving y = Ae^(t²/2).',
    tags: ['solution-methods'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-odes-004',
    front:
      'Describe the integrating factor method for first-order linear ODEs dy/dt + p(t)y = q(t).',
    back: 'Multiply both sides by the integrating factor μ(t) = exp(∫ p(t) dt). Then d/dt[μ(t)y] = μ(t)q(t). Integrate: y = (1/μ) ∫ μ·q dt + C/μ. The key insight is that the LHS becomes an exact derivative. Condition: p, q must be continuous on an interval for existence of solution.',
    tags: ['solution-methods'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-005',
    front: 'Define an exact equation and state the test for exactness.',
    back: 'An equation M(x,y)dx + N(x,y)dy = 0 is exact if ∂M/∂y = ∂N/∂x. Then there exists a function F(x,y) with ∂F/∂x = M and ∂F/∂y = N, and the solution is F(x,y) = C. If not exact but (∂M/∂y − ∂N/∂x)/N depends only on x, an integrating factor μ(x) = exp(∫ [(∂M/∂y − ∂N/∂x)/N] dx) makes it exact.',
    tags: ['solution-methods'],
    difficulty: 'medium',
  },
];

  {
    id: 'uni-maths-odes-006',
    front:
      "Describe the structure of solutions to second-order linear homogeneous ODEs ay'' + by' + cy = 0.",
    back: 'Assume y = e^(rt), giving characteristic equation ar² + br + c = 0. Three cases: (1) Distinct real roots r₁ ≠ r₂: y = C₁e^(r₁t) + C₂e^(r₂t). (2) Repeated root r: y = C₁e^(rt) + C₂te^(rt). (3) Complex roots α ± βi: y = e^(αt)(C₁ cos βt + C₂ sin βt). Superposition principle applies.',
    tags: ['solution-methods'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-007',
    front:
      'Define the characteristic equation and its role in solving constant-coefficient linear ODEs.',
    back: "For an nth-order linear ODE with constant coefficients aₙy⁽ⁿ⁾ + ... + a₁y' + a₀y = 0, the characteristic (auxiliary) equation is aₙrⁿ + aₙ₋₁rⁿ⁻¹ + ... + a₁r + a₀ = 0. Each root rᵢ of multiplicity m contributes m linearly independent solutions: e^(rᵢt), te^(rᵢt), ..., t^(m−1)e^(rᵢt). The general solution is a linear combination.",
    tags: ['solution-methods'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-008',
    front: 'Define the Wronskian and state its role in determining linear independence.',
    back: "For functions y₁, y₂, ..., yₙ, the Wronskian is W(t) = det[yⱼ⁽ⁱ⁻¹⁾(t)] (n×n determinant). Abel's identity: for a linear ODE y'' + p(t)y' + q(t)y = 0, W(t) = Ce^(−∫ p dt). The Wronskian being nonzero at one point is equivalent to linear independence of solutions (for solutions of a linear ODE).",
    tags: ['theory'],
    difficulty: 'hard',
  },
  {
    id: 'uni-maths-odes-009',
    front: "Describe the method of undetermined coefficients for ay'' + by' + cy = g(t).",
    back: 'Guess a particular solution yₚ based on the form of g(t): polynomial → polynomial of same degree; e^(αt) → Ae^(αt); sin βt or cos βt → A sin βt + B cos βt. If g(t) matches a homogeneous solution, multiply guess by t (or t^s for multiplicity s). Find coefficients by substituting into the ODE. Limited to specific g(t) forms.',
    tags: ['solution-methods'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-010',
    front:
      'Describe the method of variation of parameters for second-order linear non-homogeneous ODEs.',
    back: "For y'' + p(t)y' + q(t)y = g(t) with known linearly independent solutions y₁, y₂ of the homogeneous equation, seek yₚ = u₁(t)y₁ + u₂(t)y₂. Solve the system: u₁'y₁ + u₂'y₂ = 0, u₁'y₁' + u₂'y₂' = g(t). Then u₁' = −y₂g/W, u₂' = y₁g/W. General method, not restricted to specific g(t) forms.",
    tags: ['solution-methods'],
    difficulty: 'hard',
  },
];

  {
    id: 'uni-maths-odes-011',
    front: 'Define the Laplace transform L{f(t)} and state its existence condition.',
    back: 'L{f(t)} = F(s) = ∫₀^∞ e^(−st) f(t) dt, defined for s > a where f is piecewise continuous on [0, ∞) and |f(t)| ≤ Me^(at) (exponential order). Key property: L converts differential equations in t into algebraic equations in s, making it powerful for IVPs with piecewise or discontinuous forcing functions.',
    tags: ['laplace-transform'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-012',
    front: 'State the Laplace transforms of derivatives.',
    back: "L{f'(t)} = sF(s) − f(0). L{f''(t)} = s²F(s) − sf(0) − f'(0). In general: L{f⁽ⁿ⁾(t)} = sⁿF(s) − sⁿ⁻¹f(0) − sⁿ⁻²f'(0) − ... − f⁽ⁿ⁻¹⁾(0). These formulas incorporate initial conditions directly into the transformed equation, which is why Laplace transforms are ideal for initial value problems.",
    tags: ['laplace-transform'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-013',
    front: 'Define the inverse Laplace transform and describe common techniques.',
    back: 'f(t) = L⁻¹{F(s)} recovers the original function. Techniques: (1) Partial fraction decomposition for rational functions. (2) Completing the square for terms like (s+2)/[(s+2)²+9]. (3) Convolution theorem: L⁻¹{F(s)G(s)} = (f * g)(t) = ∫₀ᵗ f(τ)g(t−τ) dτ. (4) Tables of standard transforms.',
    tags: ['laplace-transform'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-014',
    front: 'Define a transfer function H(s) and its relationship to ODE systems.',
    back: 'For a linear ODE system with input u(t) and output y(t), the transfer function H(s) = Y(s)/U(s) = L{impulse response}. It encodes the input-output relationship entirely in the s-domain. Poles of H(s) (values where denominator = 0) determine stability: left half-plane poles → stable, right half-plane → unstable, imaginary axis → marginally stable.',
    tags: ['laplace-transform'],
    difficulty: 'hard',
  },
  {
    id: 'uni-maths-odes-015',
    front: 'State the convolution theorem for Laplace transforms.',
    back: 'L{f * g} = L{f(t)} · L{g(t)} = F(s) · G(s), where the convolution is (f * g)(t) = ∫₀ᵗ f(τ)g(t − τ) dτ. Convolution is commutative: f * g = g * f, associative, and distributive over addition. Applications: finding inverse transforms of products, solving ODEs with complicated forcing functions by decomposing into known responses.',
    tags: ['laplace-transform'],
    difficulty: 'hard',
  },
];

  {
    id: 'uni-maths-odes-016',
    front: 'State the Picard-Lindelöf (existence and uniqueness) theorem for first-order IVPs.',
    back: "If f(t, y) is continuous in a rectangle R containing (t₀, y₀) and Lipschitz continuous in y on R (i.e. |f(t,y₁) − f(t,y₂)| ≤ L|y₁ − y₂|), then the IVP y' = f(t,y), y(t₀) = y₀ has a unique solution on some interval [t₀ − h, t₀ + h]. The Lipschitz condition holds if ∂f/∂y is continuous on R.",
    tags: ['theory'],
    difficulty: 'hard',
  },
  {
    id: 'uni-maths-odes-017',
    front: 'Describe what a phase portrait represents for a system of two first-order ODEs.',
    back: "A phase portrait is a plot of trajectories in the phase plane (x, y) for the autonomous system x' = f(x,y), y' = g(x,y). Each trajectory traces the evolution of the state (x(t), y(t)). Features: equilibrium points (where f = g = 0), separatrices, limit cycles, and the qualitative behaviour (spirals, nodes, saddles) near equilibria determined by the Jacobian eigenvalues.",
    tags: ['qualitative-analysis'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-018',
    front:
      "Classify equilibrium points of x' = f(x), y' = g(x,y) based on eigenvalues of the Jacobian J.",
    back: 'For J at an equilibrium point with eigenvalues λ₁, λ₂: (1) Both real, same sign, nonzero: node (stable if negative, unstable if positive). (2) Both real, opposite signs: saddle (unstable). (3) Complex conjugate α ± βi: spiral (stable if α < 0, unstable if α > 0). (4) Purely imaginary: centre (neutral). (5) Repeated eigenvalue: proper/improper node or degenerate.',
    tags: ['qualitative-analysis'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-odes-019',
    front: "Describe Euler's method for numerically solving y' = f(t, y), y(t₀) = y₀.",
    back: 'Iterative formula: yₙ₊₁ = yₙ + h · f(tₙ, yₙ), where h is the step size and tₙ = t₀ + nh. Geometrically: follows the tangent line at each point. Error: local truncation error O(h²), global error O(h) (first-order). Very simple but inaccurate for large h; conditionally stable. Not recommended for stiff equations.',
    tags: ['numerical-methods'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-odes-020',
    front: 'Describe the concept behind fourth-order Runge-Kutta (RK4) methods.',
    back: 'RK4 approximates yₙ₊₁ = yₙ + (h/6)(k₁ + 2k₂ + 2k₃ + k₄), where k₁ = f(tₙ, yₙ), k₂ = f(tₙ + h/2, yₙ + hk₁/2), k₃ = f(tₙ + h/2, yₙ + hk₂/2), k₄ = f(tₙ + h, yₙ + hk₃). Combines four slope estimates with weights matching a fourth-order Taylor expansion. Global error O(h⁴). Industry standard for moderate accuracy.',
    tags: ['numerical-methods'],
    difficulty: 'medium',
  },
];
<FlashcardDeck deckId="uni-maths-odes" title="University Maths -- Ordinary Differential Equations" description="Solution methods, Laplace transforms, existence theorems, numerical methods" cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]} />;
