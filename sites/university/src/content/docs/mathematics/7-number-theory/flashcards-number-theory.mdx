---
title: 'University Maths Flashcards: Number Theory'
description:
  '20 interactive flashcards on divisibility, modular arithmetic, Fermat and Euler theorems,
  quadratic reciprocity, and RSA cryptography.'
tags: [University, Mathematics, Flashcards]
---

import FlashcardDeck from '@components/FlashcardDeck';
import { flashcards1, flashcards2, flashcards3, flashcards4 } from './flashcards-number-theory-data';

## Mathematics -- Number Theory Flashcards
20 interactive flashcards for university-level Number Theory. Press Space to flip, rate 1-4.
---

  {
    id: 'uni-maths-number-theory-001',
    front: 'State the Division Algorithm.',
    back: 'For integers a and b with b > 0, there exist unique integers q (quotient) and r (remainder) such that a = bq + r, where 0 ≤ r < b. The remainder r = a mod b. This is a consequence of the well-ordering principle and is foundational to all of elementary number theory.',
    tags: ['divisibility'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-number-theory-002',
    front: 'Describe the Euclidean algorithm for finding gcd(a, b).',
    back: "To find gcd(a, b) with a ≥ b > 0: a = bq₁ + r₁, b = r₁q₂ + r₂, r₁ = r₂q₃ + r₃, ..., rₙ₋₁ = rₙqₙ₊₁ + 0. Then gcd(a, b) = rₙ. Each step reduces the problem. The algorithm terminates in at most 2 log₂(b) steps (Lamé's theorem). Also works for finding gcd of polynomials.",
    tags: ['divisibility'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-number-theory-003',
    front: "State Bézout's identity.",
    back: 'For integers a, b (not both zero), there exist integers x, y such that ax + by = gcd(a, b). The coefficients x, y can be found using the extended Euclidean algorithm (back-substitution through the Euclidean algorithm). Corollary: gcd(a, b) = 1 iff there exist x, y with ax + by = 1.',
    tags: ['divisibility'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-number-theory-004',
    front: 'State key properties of the greatest common divisor gcd(a, b).',
    back: '(1) gcd(a, b) = gcd(b, a). (2) gcd(a, 0) = |a|. (3) gcd(a, b) divides any linear combination ma + nb. (4) If d | a and d | b, then d | gcd(a, b). (5) gcd(a, b) = gcd(a, b + ka) for any integer k. (6) lcm(a, b) · gcd(a, b) = |ab|. (7) gcd(ka, kb) = k · gcd(a, b).',
    tags: ['divisibility'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-number-theory-005',
    front: 'Define a prime number and state the fundamental properties.',
    back: "p > 1 is prime if its only positive divisors are 1 and p. Equivalently: p has exactly two positive divisors, or p > 1 and whenever p | ab then p | a or p | b (Euclid's lemma). Every integer n > 1 has a prime divisor (by well-ordering). There are infinitely many primes (Euclid’s proof by contradiction: suppose p₁, ..., pₙ are all primes; consider p₁p₂...pₙ + 1).",
    tags: ['primes'],
    difficulty: 'easy',
  },
];

  {
    id: 'uni-maths-number-theory-006',
    front: 'State the Fundamental Theorem of Arithmetic (unique factorisation).',
    back: "Every integer n ≥ 2 can be written uniquely (up to order) as a product of primes: n = p₁^α₁ · p₂^α₂ · ... · pₖ^αₖ, where p₁ < p₂ < ... < pₖ are primes and αᵢ ≥ 1. Uniqueness is proved using Euclid's lemma. Consequence: the prime factorisation of n is the 'DNA' of n, determining all divisibility properties.",
    tags: ['primes'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-007',
    front: 'Describe the Sieve of Eratosthenes and state its complexity.',
    back: 'To find all primes ≤ n: list 2, 3, ..., n. Cross out multiples of 2 (starting from 4), then multiples of 3 (starting from 9), then of 5, 7, ..., up to √n. Remaining uncrossed numbers are prime. Time complexity: O(n log log n), which is nearly linear. Space: O(n). Still the most efficient method for generating all small primes.',
    tags: ['primes'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-008',
    front: 'State the rules of modular arithmetic for operations in ℤₙ.',
    back: 'For a ≡ b (mod n) and c ≡ d (mod n): (1) a + c ≡ b + d (mod n). (2) a − c ≡ b − d (mod n). (3) ac ≡ bd (mod n). (4) aᵐ ≡ bᵐ (mod n) for m ≥ 0. (5) Division: if gcd(a, n) = 1, then a⁻¹ exists mod n, and ac ≡ bd (mod n) implies c ≡ d (mod n). Cancellation fails without coprimality.',
    tags: ['modular-arithmetic'],
    difficulty: 'easy',
  },
  {
    id: 'uni-maths-number-theory-009',
    front: "State Fermat's Little Theorem and give a computational application.",
    back: 'If p is prime and p ∤ a, then a^(p−1) ≡ 1 (mod p). Equivalently: aᵖ ≡ a (mod p) for all a. Applications: (1) Computing modular inverses: a⁻¹ ≡ a^(p−2) (mod p). (2) Primality testing: Fermat test checks if a^(p−1) ≡ 1 (mod p); if not, p is definitely composite. (3) Simplifying large powers modulo a prime.',
    tags: ['modular-arithmetic'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-010',
    front: "Define Euler's totient function φ(n) and state its formula.",
    back: 'φ(n) = |{k ∈ ℕ : 1 ≤ k ≤ n, gcd(k, n) = 1}|, the number of integers in [1, n] coprime to n. For prime p: φ(p) = p − 1. For n = p₁^α₁ · ... · pₖ^αₖ: φ(n) = n ∏(1 − 1/pᵢ) = p₁^(α₁−1)(p₁−1) · ... · pₖ^(αₖ−1)(pₖ−1). φ is multiplicative: φ(mn) = φ(m)φ(n) when gcd(m, n) = 1.',
    tags: ['multiplicative-functions'],
    difficulty: 'medium',
  },
];

  {
    id: 'uni-maths-number-theory-011',
    front: "State Euler's theorem.",
    back: "If gcd(a, n) = 1, then a^(φ(n)) ≡ 1 (mod n). This generalises Fermat's little theorem (since φ(p) = p − 1). Applications: RSA decryption relies on Euler’s theorem with n = pq, where φ(n) = (p−1)(q−1). Modular inverse: a⁻¹ ≡ a^(φ(n)−1) (mod n). The order of a modulo n divides φ(n).",
    tags: ['modular-arithmetic'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-012',
    front: "State Wilson's theorem.",
    back: 'p is prime iff (p − 1)! ≡ −1 (mod p). For composite n > 4, (n − 1)! ≡ 0 (mod n) (since n has a proper divisor ≤ √n ≤ n/2, which appears in the product). Necessity proof: in ℤₚ, each a ∈ {1, ..., p−1} pairs with its inverse; only 1 and p−1 are self-inverse, so product = −1.',
    tags: ['primes'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-013',
    front: 'State the Chinese Remainder Theorem (CRT) for pairwise coprime moduli.',
    back: 'If n₁, n₂, ..., nₖ are pairwise coprime (gcd(nᵢ, nⱼ) = 1 for i ≠ j), then the system of congruences x ≡ aᵢ (mod nᵢ) for i = 1, ..., k has a unique solution modulo N = n₁n₂...nₖ. Constructive proof: let Nᵢ = N/nᵢ, find yᵢ with Nᵢyᵢ ≡ 1 (mod nᵢ), then x = Σ aᵢNᵢyᵢ (mod N).',
    tags: ['modular-arithmetic'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-014',
    front: 'Define quadratic residues and non-residues modulo an odd prime p.',
    back: 'a is a quadratic residue (QR) mod p if a ≢ 0 (mod p) and ∃x with x² ≡ a (mod p). Otherwise a is a quadratic non-residue (QNR). There are exactly (p−1)/2 QRs and (p−1)/2 QNRs in ℤₚ*. The set of QRs forms a subgroup of ℤₚ*. If a ≡ b² (mod p), then a is a QR. −1 is a QR mod p iff p ≡ 1 (mod 4).',
    tags: ['quadratic-residues'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-015',
    front: 'Define the Legendre symbol (a/p) and state its properties.',
    back: "For odd prime p and a ∈ ℤ: (a/p) = 1 if a is QR mod p, −1 if a is QNR mod p, 0 if p | a. Properties: (1) (a/p)(b/p) = (ab/p) (multiplicative). (2) a^(p−1)/2 ≡ (a/p) (mod p) (Euler's criterion). (3) (a²/p) = 1 for p ∤ a. (4) (−1/p) = (−1)^((p−1)/2). (5) (2/p) = (−1)^((p²−1)/8).",
    tags: ['quadratic-residues'],
    difficulty: 'hard',
  },
];

  {
    id: 'uni-maths-number-theory-016',
    front: 'State the Law of Quadratic Reciprocity.',
    back: 'For distinct odd primes p and q: (p/q)(q/p) = (−1)^(((p−1)/2)·((q−1)/2)). Equivalently: (p/q) = (q/p) unless both p ≡ 3 (mod 4) and q ≡ 3 (mod 4), in which case (p/q) = −(q/p). Combined with the supplementary laws for (−1/p) and (2/p), this allows computation of any Legendre symbol.',
    tags: ['quadratic-residues'],
    difficulty: 'hard',
  },
  {
    id: 'uni-maths-number-theory-017',
    front: 'Define a primitive root modulo n and state when one exists.',
    back: 'g is a primitive root mod n if the order of g in (ℤ/nℤ)* equals φ(n), i.e. g generates the multiplicative group. Primitive roots exist iff n = 2, 4, pᵏ, or 2pᵏ, where p is an odd prime. When a primitive root exists, (ℤ/nℤ)* is cyclic. If g is a primitive root mod p, the QRs are exactly g², g⁴, ..., g^(p−1).',
    tags: ['primitive-roots'],
    difficulty: 'hard',
  },
  {
    id: 'uni-maths-number-theory-018',
    front: 'Define the discrete logarithm problem and explain its significance.',
    back: "Given a primitive root g modulo a prime p and a value y, find x such that gˣ ≡ y (mod p). This is the discrete logarithm problem (DLP). No efficient classical algorithm is known (best is sub-exponential). The DLP is the basis of Diffie-Hellman key exchange, ElGamal encryption, and DSA. Quantum computers can solve DLP in polynomial time using Shor's algorithm.",
    tags: ['cryptographic-applications'],
    difficulty: 'hard',
  },
  {
    id: 'uni-maths-number-theory-019',
    front: 'Outline the RSA cryptosystem: key generation, encryption, and decryption.',
    back: "Key generation: choose distinct primes p, q; n = pq, φ(n) = (p−1)(q−1). Choose e with gcd(e, φ(n)) = 1. Compute d = e⁻¹ (mod φ(n)). Public key: (e, n). Private key: (d, n). Encryption: c = mᵉ mod n. Decryption: m = cᵈ mod n. Correctness follows from Euler's theorem: m^(ed) ≡ m (mod n). Security relies on difficulty of factoring n.",
    tags: ['cryptographic-applications'],
    difficulty: 'medium',
  },
  {
    id: 'uni-maths-number-theory-020',
    front: 'Describe how to approach linear Diophantine equations ax + by = c.',
    back: 'Solutions exist iff gcd(a, b) | c. Find one particular solution (x₀, y₀) using the extended Euclidean algorithm. The general solution: x = x₀ + (b/d)t, y = y₀ − (a/d)t, where d = gcd(a, b) and t ∈ ℤ. For more variables, reduce using successive pairwise gcd computations. Application: solving ax ≡ c (mod b).',
    tags: ['diophantine-equations'],
    difficulty: 'medium',
  },
];
<FlashcardDeck deckId="uni-maths-number-theory" title="University Maths -- Number Theory" description="Divisibility, modular arithmetic, Fermat/Euler theorems, quadratic reciprocity, RSA" cards={[...flashcards1, ...flashcards2, ...flashcards3, ...flashcards4]} />;
